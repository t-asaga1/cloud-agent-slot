# 完成までのロードマップ(義経物語)

作成: AGENT #012(2026-07-10)
前提: `docs/SPEC.md`(Excel 仕様 + 確定事項)が仕様の正。`docs/DEVELOPMENT_PLAN.md` は初期の全体計画で、本ファイルは「現時点から完成まで」の実行プラン。

## 現在地(完了済み)

| 領域 | 状態 |
|---|---|
| コアロジック(役抽せん・払出・乱数) | 完了(Excel 仕様準拠、テスト済み) |
| 抽せんテーブル(モード / 背景 / 前兆 / AT) | 完了(データ駆動、行合計検証テスト済み) |
| リール制御 | **完了(STEP 1)**: 図柄 8 種 + Excel 20 コマ配列、有効 5 ライン、全役の停止制御(網羅テスト済み) |
| 状態遷移(ステートマシン) | **完了(STEP 2)**: `state.ts` の `advanceGame`(モード / 前兆 / 背景 / AT / エンディング)+ `play.ts` + シミュレーション検算 |
| 素材 | 筐体・図柄 8 種・背景動画 7 本は実素材取り込み済み。BGM/SE・連続演出動画は仮素材 |
| UI | 通しフローデモ(ステートマシン駆動 + ステージ自動切替 + デバッグパネル + オート消化。本格 UI は STEP 3) |
| exe 化(Tauri) | 未着手 |

## 残りステップ(推奨順)

1 AGENT = 1 やり取りの粒度で分割している。各ステップは前のステップに依存するため、原則この順で進める。

### STEP 1: リール制御の本対応(`reel.ts` 書き直し)— 大 → **1a〜1f の 6 サブステップに分割** — **全サブステップ完了(2026-07-11)**

ゲームの土台。これが暫定のままだと UI・状態遷移のテストが全部やり直しになるため最優先。

**分割の経緯**: STEP 1 を丸ごと 1 AGENT で実施したランが規模超過で ERROR 終了したため(基盤要因)、
**1 サブステップ = 1 AGENT = 1 PR** の粒度へ分割した(AGENT #015 で計画)。
原則 1a → 1f の順で実施し、各 PR を 1 本ずつマージしてから次へ進む。
各サブステップは「テスト・lint・ビルドがグリーンの状態」で終わるよう設計している
(途中サブステップでは停止制御が暫定のままでもよいが、壊れた状態でのマージは不可)。

#### STEP 1a: 図柄 8 種化 + Excel 配列差し替え(データ層)— 小〜中 — **完了(AGENT #017)**

- `ReelSymbol` を 8 種へ変更: 赤7 / 黒バー / 白バー / ベル / スイカ / チェリー / リプレイ / ブランク
  (`SEVEN_WHITE` 廃止・`BAR` → 黒バー/白バーへ分離・ブランク新設。ID 命名は実装 AGENT が決定)
- `REEL_LAYOUT` を SPEC「3.」の 20 コマ配列へ差し替え。SPEC の表は**コマ番号 20 → 1 の降順**で
  記載されているため、コマ番号と配列 index の対応規約をコード内コメントに明記する
- `src/assets/index.ts` の `SYMBOL_IMAGES` を正式マッピングへ(SEVEN_WHITE→白バー画像の仮割り当て解消、
  `SYMBOL_BLANK_URL` の組み込み)。**型変更で `src/assets/index.ts` と `App.tsx` のコンパイルが通らなく
  なるため、旧分割案の「1d: 画像割り当て解消」はここで同時に実施する**(機械的な差し替えのみで小さい)
- 配列検算テストの新設(SPEC「3. 配列分析」のテスト化): 各リールの図柄個数 / 左リプレイ最大間隔 6 /
  ベルの 100% 引き込み配置 / 赤7・黒バー・白バー各 1 個 / ブランク 左 2・中 0・右 1
- 旧配列前提の停止制御網羅テスト(`reel.test.ts`)は新配列では成立しないため、**TODO コメント付きで
  一時 skip**(1c〜1e で新テストへ置換し、1e で skip 残ゼロにする)。停止制御ロジック自体は触らない
- 完了条件: `npm test` / `npm run lint` / `npm run build` グリーン

#### STEP 1b: 5 ライン判定(表示判定層)— 中 — **完了(AGENT #018)**

- 有効ライン定数の定義: 上段・中段・下段・右下がり・右上がりの 5 ライン(窓 3 段 × 3 リールの座標で定義)
- `judgeDisplay` を 5 ライン対応へ書き直し: 全ラインを走査してリプレイ / ベル / スイカ / 赤7(リーチ目)の
  揃いを判定。スイカの弱・強は内部当選役で区別(現行方式を踏襲)。チェリーはライン非依存で
  左リール窓内を判定(中段=中段チェリー / 上下段=角チェリー)
- 複数ライン同時成立時の表示役の優先順位を定義する(払出は 1 役分)
- 押し順ベルの停止形(上段 1 枚 / 斜め 13 枚)と `payout.ts` の `bellSuccess` の対応関係を整理し、
  判定結果から払出区分を導けるようにする(実配線は 1c)
- 判定単体テストを新配列の実座標で作成
- この段階では停止制御(`resolveStop`)は暫定のままでよい(網羅テストは skip のまま)
- 完了条件: 判定テスト含め全体グリーン

#### STEP 1c: 停止制御 I — 基本役(ハズレ / リプレイ / 押し順ベル)— 中〜大 — **完了(AGENT #019)**

- 5 ライン対応の新停止制御エンジンの骨格を作る(現行の引き込み優先度探索を 5 ラインへ拡張するか、
  役別停止テーブル方式にするかは実装 AGENT が選定し、方針を `reel.ts` 冒頭コメントに記録する)
- ハズレ: 5 ラインのいずれにも役図柄を揃えず、左リール窓内にチェリーも出さない
- リプレイ: 横・斜め 5 ライン併用で**全押下位置から 100% 引き込み**(確定事項。左リプレイは中段のみだと
  間隔 6 コマで不可 → 上下段停止の併用が必須)
- 押し順ベル: 左第一停止=上段ベル停止で 1 枚 / 中・右第一停止=斜め揃いで 13 枚
  (押し順 6 通り × 押下位置で網羅)
- 上記 3 役分の網羅テストを新設(当選役 × 全 20³ 押下位置 × 押し順 6 通り)
- **リスク早期検証**: 最大スベリ 4 コマで 5 ライン全ての蹴飛ばし(非当選役の揃い回避)が全押下位置で
  可能かは配列依存。ハズレの網羅テストで不可能な押下位置が見つかった場合は、その場しのぎをせず
  出目仕様をユーザーへ確認する
- 完了条件: 対象 3 役の網羅テストグリーン(レア役の停止制御は暫定のままでよい)
- **実施結果(AGENT #019)**: 蹴飛ばしは全押下位置・全押し順で可能(ハズレ網羅テストで確認)。
  ただし**リプレイのみ配列上の理論限界を発見**: 左リールを最後に止める押し順
  ([中→右→左]・[右→中→左])では左リプレイ最大間隔 6 コマの制約により、どの停止戦略でも
  100% 引き込みが不可能(取りこぼし理論最小 134〜135 / 8000 押下位置。因果最適探索で確認)。
- **理論限界はユーザー指示の配列変更で解消(AGENT #020・2026-07-11)**: 左リール コマ 14 を
  ブランク → リプレイへ変更(SPEC 確定事項 17)。左リプレイの最大間隔が 6 → 5 コマとなり、
  全押し順・全押下位置で 100% 引き込み(網羅テストで検証済み)

#### STEP 1d: 停止制御 II — レア役(チェリー / スイカ / チャンス目)— 中〜大 — **完了(AGENT #021)**

- 角チェリー=左リール上段or下段停止 / 中段チェリー=左リール中段停止。引き込めない押下位置は取りこぼし
- 弱スイカ=斜め優先(困難時は平行)/ 強スイカ=平行優先(困難時は斜め)。取りこぼし許容
- チャンス目=スイカがテンパイし、かつ引き込める位置でも引き込まない(テンパイはずし)
- 対象役の網羅テストを新設(1c と同粒度)
- 完了条件: 対象役の網羅テストグリーン
- **実施結果(AGENT #021)**: 1c の探索エンジンの出目合法判定(`classifyFinal`)へ各役の目標停止形を
  追加するだけで実現(エンジン本体は変更なし)。チャンス目は「スイカテンパイはずし目」を停止形として
  確定し、表示判定(`judgeDisplayDetail`)が内部当選チャンス目 + テンパイ時に `CHANCE_ME` を返す
  (払出 3 枚が配線される)。全 20³ 押下位置 × 押し順 6 通りの網羅テストで検証済み。
  スイカ・チャンス目の押し順別成功数は回帰検出用に固定値でテスト化

#### STEP 1e: 停止制御 III — リーチ目 7 揃い + DDT + 総合網羅テスト — 中 — **完了(AGENT #022)**

- リーチ目: 赤7 を狙えば 7 揃い(いずれかの有効ライン)。**目押しを外した場合は取りこぼし**
  (代替リーチ目停止なし・払出なし。SPEC 回答 14)
- DDT(左リール黒バー狙い): 通常時は黒バーが下段付近に停止し、チェリー・スイカ成立時は
  チェリー停止 or スイカまでスベる(SPEC「3.」確定)
- 総仕上げ: 全役 × 全 20³ 押下位置 × 押し順 6 通りの総合網羅テストを完成させ、
  1a で skip した旧テストを完全に置換・削除する(**skip 残ゼロ**)
- スベリ 4 コマ以内の保証と、非当選役を 5 ラインのいずれにも揃えないことを全域で検証
- 完了条件: テスト全件グリーン、skip・TODO 残ゼロ
- **実施結果(AGENT #022)**: 候補評価に「目押し保証ランク(`aimedRank`)」を追加し、
  リーチ目は 3 リールとも赤7 狙い(目押し成功 = 各リール 5 箇所 × 5³ × 押し順 6 通り)で
  **100% 7 揃い**を網羅テストで検証(狙いを外した場合はクリーンなハズレ目で取りこぼし)。
  DDT は評価キー最下位(スベリ最小の直前)に「黒バーを窓内のできるだけ下段寄りへ」の
  選好(`ddtScore`)を追加(左第一停止の黒バー狙いで、ハズレ時は黒バー下段停止・
  スイカ成立時はスイカが窓内へスベる。チェリーはコマ 13 を窓に引き込める押下位置 12〜13
  のみチェリー停止 = 察知可能。押下位置 14 以降は物理的に届かず取りこぼし)。
  旧 skip テスト 10 件は削除し、全役共通不変条件の総合網羅テストへ置換(**skip 残ゼロ**)。
  スイカ・チャンス目の固定値テストは変化なし(DDT 選好は蹴飛ばし・引き込みへ影響しない)

#### STEP 1f: UI 追随 + ブラウザ確認 — 小 — **完了(AGENT #023)**

- `App.tsx` の 1G 消化デモを 5 ライン / 8 図柄の判定結果へ追随(表示役・払出枚数の整合)
- ブラウザで実機確認: 全 8 図柄の表示(ブランク含む)、代表的な停止形
  (リプレイの斜め揃い / ベルの上段・斜め / チェリー角・中段 等)のスクリーンショット
- `docs/ROADMAP.md` の STEP 1 完了マークと `docs/HANDOVER.md` の更新
- **実施結果(AGENT #023)**: デモ UI に押し順セレクト(6 通り)/ 目押しモード
  (適当押し・赤7 狙い・DDT 黒バー狙い)/ 成立役の強制指定(停止形確認用)を追加し、
  `resolveSpin` の判定結果(揃ったライン・押し順ベル払出区分)をステータス・履歴へ表示。
  揃った有効ライン上の図柄はリール窓で金枠ハイライト。ブラウザで代表停止形
  (ベル上段 1 枚・斜め 13 枚 / リプレイ斜め / チェリー角・中段 / リーチ目 7 揃い中段 /
  DDT 黒バー下段停止)を実機確認(console エラーなし)

> 旧分割案(1a: 配列+図柄 8 種 / 1b: 5 ライン判定 / 1c: 停止制御 / 1d: 画像割り当て+UI 確認)との対応:
> 新 1a = 旧 1a + 旧 1d の画像割り当て解消 / 新 1b = 旧 1b / 新 1c〜1e = 旧 1c の 3 分割 / 新 1f = 旧 1d の UI 確認。

### STEP 2: 1 ゲーム通しフロー(`state.ts` ステートマシン)— 大 → **2a〜2f の 6 サブステップに分割**(AGENT #024 で計画)— **全サブステップ完了(2026-07-12)**

`mode.ts` / `background.ts` / `omen.ts` / `at.ts` の部品を組み合わせ、レバーオン 1 回で「役抽せん → モード移行 → 前兆管理 → 背景移行 → AT 管理」が全部進む純関数のステートマシンを作る。

**分割の方針**: STEP 1 と同じく **1 サブステップ = 1 AGENT = 1 PR** の粒度。原則 2a → 2f の順で実施し、
各 PR を 1 本ずつマージしてから次へ進む。各サブステップは「テスト・lint・ビルドがグリーンの状態」で
終わるよう設計している(途中サブステップでは後続機能がスタブのままでもよいが、壊れた状態でのマージは不可)。

**着手前の確認事項 — 解消済み(2026-07-11)**: 前兆タイムライン・30G カウンタの境界条件等の未定義点は、
AGENT #025 が質問リスト Q1〜Q11(推奨案付き)として整理し(履歴: `docs/handover/025_step2a_preflight_questions.md`)、
**ユーザー回答は全 11 問とも推奨案どおりで確定。SPEC「13. 確定事項」の 18〜28 へ追記済み**(AGENT #027)。
各サブステップの対応: A 群 Q1〜Q6 = 確定 18〜23(2b)/ B 群 Q7〜Q8 = 確定 24〜25(2c)/
C 群 Q9〜Q10 = 確定 26〜27(2d・2e)/ D 群 Q11 = 確定 28(2a の出力設計 + STEP 4)。

**全体設計(2a で確定させ、以後のサブステップは従う)**:

- `src/core/state.ts` に**純関数のステートマシン**を実装する: `GameState`(モード / 背景 / 前兆 / AT /
  各種カウンタを含む単一の状態オブジェクト)+ `advanceGame(state, input, rng) → { state, events }` の形。
  `events`(モード移行・前兆突入・AT 突入等の発生イベント列)は UI の演出表示とテストの検証に使う。
- **リール制御と疎結合にする**: ステートマシン本体は「成立役 + 払出結果」を入力に取り、押下位置・押し順・
  `resolveSpin` との結合は UI / シミュレーション共用のラッパー関数で行う(通常時 = 左第一 1 枚、
  AT 中 = ナビ準拠 13 枚、の打ち方ポリシーもラッパー側)。
- **1G 内の処理順序を最初に固定する**(例: 役抽せん → [AT 中以外] モード移行抽せん → 偽前兆突入抽せん →
  前兆 / 連続演出 / AT の進行 → 「次ゲーム」で効く背景移行の予約 → ゲーム数・差枚の集計)。
  「次のゲーム」で効く契機(偽前兆当せん・本前兆移行)は予約フラグとして状態に持ち越す。
- 実装中に出た仕様の解釈(下記の各サブステップに記載)はコード内コメント + HANDOVER に記録し、
  ユーザー確認が必要なものは PR / 応答で明示する。

#### STEP 2a: 状態モデル + 通常時のモード移行(骨格)— 小〜中 — **完了(AGENT #027)**

- `GameState` 型の全体設計: 通常 / 前兆(偽・本)/ 連続演出 / AT / 上位 AT / エンディングの全フェーズを
  表現できる形(判別可能ユニオン + カウンタ類)をここで確定し、以後のサブステップで骨格を変えない
- `initGameState(rng)`: ゲーム開始時のモード初期抽せん(`drawInitialMode('GAME_START')`)+
  背景初期抽せん(`drawInitialBackground`)
- 通常時 1G の骨格: 成立役入力 → `drawModeTransition`(ハズレは維持 = 回答 8、本前兆滞在中は停止 = 回答 9)
  → 本前兆移行の検出(前兆スケジュール開始は 2b。まずはモードが変わるだけでよい)
- 総ゲーム数・差枚(払出結果入力の集計)・リプレイの次ゲーム BET 不要の扱いをここで実装
- 単体テスト: 固定シード RNG での遷移検証 / ハズレ維持 / 本前兆中の抽せん停止 / 初期抽せんの分布(大量試行)
- 完了条件: `npm test` / `npm run lint` / `npm run build` グリーン
- **実施結果(AGENT #027)**: `src/core/state.ts` を新設。フェーズは判別可能ユニオン `Phase`
  (`NORMAL` / `OMEN`(偽・本、経過 G・総 G・連続演出種別)/ `RENZOKU`(種別・何 G 目)/
  `AT`(通常・上位、パート・連チャン・継続率・V ストック・継続確定)/ `ENDING`)で、
  偽前兆が「偽前兆 + モード」(確定 9)であることからモードとフェーズを直交して保持。
  1G 内の処理順序(払出 → モード移行 → [2b] 前兆 → [2c] 背景 → 集計)と「次ゲーム」契機の
  予約フラグ `pendingBackgroundTrigger` をヘッダーコメントで固定し、2b〜2d の挿入スロットを明示。
  毎 G の出力(`AdvanceResult`)に演出層向け情報を全部含める設計(確定 28。`events` は 2b〜2d で
  ユニオンへ追加予定の種別もコメントで予告)。AT 中・エンディング中のモード移行抽せん停止
  (確定 11)も実装・テスト済み。テスト 139 パス(state 14 件追加)

#### STEP 2b: 前兆管理(偽前兆・本前兆・連続演出・書き換え)— 中〜大 — **完了(AGENT #028)**

- 偽前兆突入の配線(`drawFakeOmen`: 弱スイカ・角チェリー・チャンス目の 1/10、強スイカ・中段チェリーは
  本前兆非当選時 100% = 回答 3)。偽前兆はモードと独立の「偽前兆 + モード」状態(回答 9)
- 本前兆移行時 / 偽前兆突入時: 前兆ゲーム数抽せん(偽 7〜9G / 本 7〜10G)+ 発展連続演出の抽せん(A/B/C)
- 前兆ゲーム数の消化 → 連続演出 4G(`RENZOKU_GAMES`)→ 解決: 本前兆 = AT 当選(**AT 開始は 2d まで
  スタブフラグでよい**。解決したことを `events` に出す)/ 偽前兆 = 連続演出失敗して通常へ戻る
- 偽前兆中のモード移行抽せん継続 + レア役で本前兆へ移行した場合の**偽前兆 → 本前兆の書き換え**(回答 9)。
  書き換え時の前兆スケジュールの扱い(進行中の前兆残り G 数・連続演出種別を引き継ぐか、
  本前兆用に再抽せんするか)は実装 AGENT が決めてコメント + HANDOVER に記録し、ユーザーへ確認を出す
- 本前兆中に成立したレア役の扱い(モード抽せん停止のため移行なし。偽前兆抽せんも不要)をテストで固定
- 単体テスト: 前兆 G 数の消化と発展タイミング / 書き換え / 偽前兆失敗の解決 / 振分けの分布(大量試行)
- 完了条件: グリーン(AT 突入はスタブでよい)
- **実施結果(AGENT #028)**: 2a の骨格(型・処理順序・予約フラグ)を変えず `advanceGame` の
  挿入スロット (3)(4) へ実装。書き換えの詳細は質問リストで回答済み(確定 21 = G 数・演出とも
  引継ぎ・再抽せんなし・契機 4 予約なし)のため追加のユーザー確認は不要だった。
  実装上の解釈(コード内コメント + HANDOVER 記録): 当せんゲーム = `OMEN { game: 0 }` で
  返し次ゲームが前兆 1G 目(確定 18)/ 連続演出 4G 目に `RENZOKU_RESULT`(成否告知)を
  発行し同ゲームの返り state で本 = AT スタブ・偽 = NORMAL + `FAKE_OMEN_FAIL` 予約 /
  書き換えゲーム自体も前兆 1G 分として進行 / AT スタブは `AtPhase`(partGame 0・renchan 1・
  continueRate 0 は 2d で `drawContinueRate` に置換)/ `initGameState` で初期モードが
  本前兆ならその場でスケジュール抽せんし最初のゲームが前兆 1G 目(契機 4 予約なし)。
  テスト 156 パス(state 31 件 = +17)

#### STEP 2c: 背景移行の統合(30G カウンタ + 4 契機 + 優先順位)— 中 — **完了(AGENT #029)**

- 同一背景の経過ゲーム数カウンタ(`BACKGROUND_ELAPSED_GAMES` = 暫定 30。回答 15)。
  **背景移行時(契機を問わず)にリセット**
- 4 契機の発火タイミングの配線: (1) 30G 経過 / (2) 偽前兆当せんの**次ゲーム** / (3) 偽前兆の連続演出
  失敗後のゲーム / (4) 本前兆へモード移行した**次ゲーム**。(2)(4) は 2b の予約フラグから発火する
- 優先順位: 偽前兆当せん(2)が 30G 経過(1)より優先(回答 15)。それ以外の契機が同一ゲームで
  重なるケース(例: (3) と (1))の優先も定義してテストで固定し、HANDOVER に記録する
- AT 中(上位含む)は背景移行抽せん停止(回答 11)。本前兆モード滞在中は HONZENCHO_NEXT 以外の契機が
  発生しないこと(モード移行抽せん停止のため)をテストで確認
- 単体テスト: カウンタのリセット条件 / 優先順位 / 各契機で参照するテーブル(モード × 契機)の正しさ
- 完了条件: グリーン
- **実施結果(AGENT #029)**: `advanceGame` のスロット 5 へ背景移行を実装(骨格・型は無変更)。
  発火は「ゲーム開始時点の予約(契機 2/3/4)> 30G 経過(契機 1)」の優先で 1 ゲーム 1 抽せん。
  契機 1 は「このゲームが通常時(開始・終了ともフェーズ NORMAL)」のときのみ有効
  (前兆中は停止 = 確定 25 / AT・エンディング中は停止 = 確定 11)で、判定は
  カウンタ + 1 ≥ 30(停止期間の持ち越し後は次の通常ゲームで発火)。抽せん実施で
  自背景維持でもカウンタリセット(確定 24)。`BACKGROUND_CHANGE` イベント
  (契機・from・to 付き)は背景が実際に変わったときのみ発行。偽→本書き換えと契機 2 が
  同一ゲームで重なった稀ケースは背景維持 + カウンタのみリセット(実装解釈。SPEC「5.」
  実装注記参照)。詳細規約は `state.ts` ヘッダー「背景移行の実装規約」に記録。
  テスト 171 パス(state 46 件 = +15)

#### STEP 2d: AT 管理(セット・連チャン・上位 AT・エンディング・終了処理)— 大 — **完了(AGENT #030)**

- AT 突入(2b のスタブ解消): 連続演出成功 → `drawContinueRate`(66/79/84/88%)、初回セット = 1 連目(回答 11)
- 小役パート 10G: レア役等で V ストック抽せん(`drawVStock`。**複数ストック可** = 回答 11)
- バトルパート 8G: 開始時に継続率による継続抽せん(V ストックがあれば消費して継続確定、の消費規則は
  実装 AGENT が決めて記録)。パート中の小役は**継続未確定なら継続抽せん / 確定済みなら V ストック抽せん**(回答 11)
- セット継続 → 連チャン数 +1 して小役パートへ。**10 連で上位 AT へ移行**(連チャン数リセット・継続率 93%
  固定 = 回答 12)。上位 AT で再度 10 連するとエンディング(回答 12)
- バトル敗北 = AT 終了(**上位 AT でも即終了・降格なし** = 回答 12)。エンディング後も AT 終了扱い
- AT 終了処理: 「AT 終了後」テーブルでモード再抽せん(`drawInitialMode('AT_END')`)+ 背景再抽せん
  (`drawInitialBackground`)→ 通常時へ。AT 中はモード・背景移行抽せん停止(回答 11)を通しで確認
- 押し順ナビ情報(AT 中は全ナビ)を状態 or events に持たせる(UI・シミュレーションの打ち方ポリシー用)
- 単体テスト: セット遷移 / V ストックの獲得・消費 / 連チャンカウントと 10 連移行 / エンディング /
  終了処理後のモード・背景 / 差枚集計(純増 ≒ +6.4 枚/G の粗い検算)
- 完了条件: グリーン(スタブ・TODO 残ゼロ)
- **実施結果(AGENT #030)**: `advanceGame` のスロット 4 へ AT / エンディングの進行を実装
  (2a の骨格・型は無変更。`atStubPhase` を `enterAt` = `drawContinueRate` 抽せんへ置換)。
  実装上の決定(`state.ts` ヘッダー「AT 管理の実装規約」+ HANDOVER に記録):
  V ストックの消費規則 = バトル開始時に継続率抽せん → 漏れたらストック 1 個消費で継続確定
  (当せん時はストック温存)/ 10 連目のセットは「必ず移行」= 継続抽せん・消費なしの継続確定
  扱いで、8G 目の解決時に 通常 → 上位 AT(連チャン 1 へリセット・0.93 固定・ストック持越し)
  / 上位 → エンディング / エンディングは `ENDING_GAMES` = 暫定 1G(STEP 4 で尺確定まで)。
  AT 終了処理は敗北・エンディングとも同ゲーム内で「AT 終了後」テーブルのモード → 背景を
  再抽せんし、`backgroundGames` を 0 へリセット(本前兆を引いたら `initGameState` と同じく
  その場で前兆スケジュール開始・契機 4 予約なし)。押し順ナビは `isNaviActive(state)` で導出
  (2e の打ち方ポリシー用)。イベント 6 種(AT_START / V_STOCK_GAIN / AT_SET_CONTINUE /
  UPPER_AT_ENTER / ENDING_START / AT_END)を追加し、大量試行で継続率振分けと
  AT 中純増 ≒ +6.4 枚/G(粗い検算: ナビ遵守・取りこぼしなし前提)を検証。
  テスト 192 パス(state 67 件 = +21)
- **確定 29〜31 の反映(AGENT #032)**: 2026-07-12 のユーザー回答で実装解釈 4 点を訂正
  (SPEC 確定事項 29〜31)。V ストックは**バトル開始時に先に消化**(ストックがあるときは
  継続率抽せんなし。`V_STOCK_USE` イベント新設)/ **10 連目も敗北あり**(必ず移行の特例を廃止)
  / 10 連目の勝利で**エンディング 10G(押し順ナビあり)を経由**して 通常 AT → 必ず上位 AT
  (ストック持越し)・上位 AT → AT 終了(`EndingPhase` に `after`・`vStock` を追加、
  `UPPER_AT_ENTER` はエンディング消化ゲームに発行)/ `ENDING_GAMES` = 10・`isNaviActive` は
  ENDING も true

#### STEP 2e: 通しフロー統合 + 大量試行シミュレーション検算 — 中〜大 — **完了(AGENT #031)**

- 役抽せん(`drawRole`)+ 打ち方ポリシー(通常 = 左第一 / AT = ナビ準拠)+ 払出 + ステートマシンを
  繋いだヘッドレス 1G 実行ヘルパーを実装(UI とシミュレーションで共用)
- 通しフロー統合テスト: 通常 → 前兆 → AT → セット継続 → 上位 AT → エンディング → AT 終了後の
  再抽せん → 通常、の一連遷移をイベント列で検証
- 大量試行シミュレーション(目安 100 万 G 以上): 初当り確率 / AT 平均連チャン数 / AT 純増(想定 +6.4 枚/G)/
  通常時純増(想定 −1.8 枚/G)/ コイン持ち(想定 約 27G/50 枚。回答 16)/ 機械割 を計測
- SPEC に想定値がある項目は突き合わせて検算。**機械割・初当り確率の想定値は SPEC 未記載**のため、
  計測値を docs(SPEC.md への追記 or 専用レポート)に記録してユーザーへ妥当性確認を出す
- 乖離が出た場合は実装バグか仕様側の想定かを切り分ける(勝手に仕様値を変えない)
- 回帰検出用に固定シードのシミュレーション統計テストを追加(実行時間と相談して G 数・許容幅を調整)
- 完了条件: グリーン + 計測レポート
- **実施結果(AGENT #031)**: `src/core/play.ts`(ヘッドレス 1G 実行 `playGame` + 打ち方ポリシー
  `decidePush` = 通常時 左第一・適当押し / AT 中ベルのみナビ遵守 = 中第一)と
  `src/core/simulate.ts`(区間別統計の収集 `simulate` + 整形 `formatStats`)を新設し、
  `npm run sim -- <ゲーム数> <シード>`(`scripts/run_simulation.ts`・vite-node)で実行可能に。
  通しフロー統合テスト(`flow.test.ts` 7 件 = 通常 → 前兆 → 連続演出 → AT → 10 連 → 上位 AT →
  エンディング → AT 終了 → 通常のイベント列・フェーズ検証)と固定シード 10 万 G の回帰統計テスト
  (`simulate.test.ts` 8 件)を追加。**100 万 G × 3 シードの計測結果は `docs/SIMULATION_REPORT.md`**:
  通常時純増 −1.86 枚/G(想定 −1.8)・AT 中純増 +6.37 枚/G(想定 +6.4)・コイン持ち 26.9G
  (想定 27G)は想定と一致。テスト 216 パス(play 9 + flow 7 + simulate 8 件 = +24 追加)
- **確定 29〜31 反映後の再計測(AGENT #032)**: 初版の機械割 約 152〜157% は「このバランスで
  問題ない」と回答済みだったが、確定 29〜31(V ストック先消化・10 連敗北あり・エンディング 10G)
  の反映で **機械割 約 129〜131% / 平均連チャン 約 5.5 セット / 上位 AT 移行率 約 13〜15% /
  エンディング完走率 約 7〜9%** へ変化(`docs/SIMULATION_REPORT.md` を更新して再確認を依頼中)。
  flow.test.ts はシード 62 へ取り直し、simulate.test.ts の固定値も再取得

#### STEP 2f: UI 最小連動 + ブラウザ確認 — 小〜中 — **完了(AGENT #033)**

- `App.tsx` をステートマシン駆動へ: レバーオン 1 回で `advanceGame` が進み、**背景状態に応じて
  ステージ動画が自動切替**(手動ステージセレクトはデバッグ用に残してよい)
- ステータス表示(デバッグパネルでよい): モード / 背景 / 前兆状態(種別・残り G・連続演出)/
  AT 状態(セット G 数・連チャン数・継続率・V ストック・AT 中純増)/ 発生イベント
- 既存の成立役強制指定と組み合わせ、UI 操作だけで「レア役 → 前兆 → AT 突入」を再現できるようにする
- ブラウザ実機確認: 前兆 → 連続演出 → AT 突入 → セット継続 → AT 終了 → 通常復帰の一連、
  背景 30G 移行、AT 中のステージ自動切替。スクリーンショット・録画を残す
- 本格 UI(リール回転アニメ・停止ボタン・ナビ表示・BGM 連動)は STEP 3 で実施(ここでは最小限)
- 完了条件: グリーン + 実機確認記録、`docs/ROADMAP.md` の STEP 2 完了マークと HANDOVER 更新
- **実施結果(AGENT #033)**: `App.tsx` を `initGameState` + `advanceGame` 駆動へ全面書き換え
  (コアロジックは無変更。UI 層 = `App.tsx` + `App.css` のみ)。状態 → ステージの自動切替
  `stageForState`(通常 = 背景 5 種 / AT = 階層 × パート別 4 種 / エンディング = 暫定で直前
  AT 階層のバトルステージ継続。専用演出は STEP 4)、デバッグパネル(モード / 背景 +30G
  カウンタ / フェーズ / 総 G / 差枚 / ナビ + AT 詳細 = 連チャン・継続率・V ストック・継続確定 /
  エンディング詳細 = 行き先・持越しストック)、`GameEvent` 全 14 種の日本語イベントログ
  (直近 14 件)、押し順「自動」(通常 = 左第一 / ナビ中のベル = 中第一)、オート消化
  (160ms 間隔)を実装。ブラウザ実機確認済み(録画 + スクリーンショット): 中段チェリー →
  本前兆 7G → 連続演出 C 4G 成功 → AT 突入(継続率 79%・ステージ自動切替)→ オート消化 →
  バトル敗北 → 通常復帰、ハズレ強制での 30G 背景移行イベント連発、console エラーなし

### STEP 3: 遊技 UI の本実装(Phase 4)— 大 → **3a〜3e の 5 サブステップに分割**(AGENT #034 で計画)— **全サブステップ完了(2026-07-12)**

- リール回転アニメーション + 停止ボタン 3 つ(タイミング目押し。押下瞬間のコマ = 押下位置)
- 押し順ナビ(AT 中)、払出・クレジット・獲得枚数表示
- ステートマシンと背景動画・BGM の連動(モード/背景/AT で自動切替)
- 前兆・連続演出の画面表示(現状は仮テキスト演出でよい)

**分割の方針**: STEP 1・2 と同じく **1 サブステップ = 1 AGENT = 1 PR** の粒度。原則 3a → 3e の順で実施し、
各 PR を 1 本ずつマージしてから次へ進む。各サブステップは「テスト・lint・ビルドがグリーン +
ブラウザ実機確認済み」で終わるよう設計している(STEP 3 は UI 中心のため、全サブステップで
実機確認 + スクリーンショット / 録画を必須とする)。

**全体設計(3a で確定させ、以後のサブステップは従う)**:

- **コアロジック(`src/core/`)は原則無変更**。停止制御は既存の `resolveStop`(1 リールずつ呼べる)を
  停止ボタン押下ごとに呼ぶ構成へ切り替えるだけで、インターフェース変更は不要
  (現行 2f は `resolveSpin` で 3 リール一括解決している)。
- **UI 層に「遊技サイクル」の小さなステートマシンを新設**(1G の内部進行。`advanceGame` の外側):
  レバー待ち → レバーオン(役抽せん + ナビ表示)→ 全リール回転 → 停止ボタンで 1 リールずつ停止
  (押下瞬間に中段にあるコマ = 押下位置 → `resolveStop`)→ 全停止(表示判定・払出表示)→
  `advanceGame` で状態進行 → レバー待ちへ戻る。**役抽せんはレバーオン時**(AT 中のベルナビを
  リール停止前に表示するため)、**`advanceGame` は全停止後**(表示役・bellSuccess が入力のため)。
- 押し順は「停止ボタンを押した順」が正になる(現行の押し順セレクトによる事前指定から置き換え。
  強制役・オート消化などのデバッグ機能は維持)。
- `App.tsx` が肥大化しているため、サブステップの過程で `src/ui/` へのコンポーネント分割を行ってよい
  (リール窓 / 遊技パネル / デバッグパネル等。分割の単位は実装 AGENT が決めて記録)。
- ステージ・BGM 切替は引き続き `stageForState` に集約する(2f で確立済み)。

**着手前の実装デフォルト(推奨案。ユーザーから変更指示がなければこれで進める)**:

1. リール回転速度は**約 750ms/周(実機の約 80rpm 相当)**で定数化(調整可能にする)。
2. 操作はマウスクリック + **キーボード**(推奨: Space = レバーオン / Z・X・C = 左・中・右停止ボタン)。
3. BET 操作は独立ボタンにせず**レバーオンに内包**(3 枚掛け固定。リプレイは自動 BET 表示)。
4. オート消化(自動レバー + 自動停止 = 適当押し)は本 UI でも維持(大量ゲーム確認用)。
5. 押し順ナビの表示形式は**リール窓上のナビ数字(例: 中第一 = 「2 1 3」)**の仮デザイン(STEP 4 で差し替え可)。

#### STEP 3a: 遊技サイクル UI ステートマシン + 停止ボタン(骨格)— 中〜大 — **完了(AGENT #035)**

- UI 内の遊技サイクル(上記の全体設計)を実装: レバーオン → 回転中 → 停止ボタン 3 つで
  1 リールずつ `resolveStop` → 全停止 → 払出 → 次ゲームへ
- リール回転中の表示は**簡易でよい**(コマ送り・ぼかし等。滑らかなスクロール描画は 3b)
- 停止ボタンは回転中の未停止リールのみ有効。押下瞬間に中段にあるコマ = 押下位置。
  押した順がそのまま押し順(`PushOrder`)になる
- キーボード操作(Space / Z・X・C)を実装
- 2f のデバッグ機能(強制役・オート消化・リセット・ステージ手動切替)は維持
  (目押しモードセレクト(`pickPushes` / `aimedPush`)は停止ボタン化に伴い廃止 or
  オート消化専用へ縮退。扱いは実装 AGENT が決めて記録)
- 完了条件: グリーン + ブラウザで停止ボタン操作による 1G 消化サイクルが回ること(録画)
- **実施結果(AGENT #035)**: 遊技サイクルの純ロジックを `src/ui/gameCycle.ts` へ新設
  (`startSpin` / `pressStop` = `resolveStop` を 1 リールずつ呼ぶ / `finishSpin` = 表示判定 /
  `spinningPosition` = 回転の時間モデル 750ms/周)。**押し順は「停止ボタンを押した順」で確定**し、
  第一停止時点で未確定の残り 2 リールは「左→右昇順」の仮押し順で `resolveStop` を呼ぶ
  (`provisionalPushOrder`)。この対話式停止でも出目の合法性(スベリ 4 コマ以内 /
  非当選図柄を揃えない / リプレイ・ベル 100% 引き込み / ベルの押し順別停止形 /
  クリーンな取りこぼし)が保たれることを全役 × 全押し順 6 通り × 全 20³ 押下位置の
  網羅テスト(`gameCycle.test.ts`)で検証。`App.tsx` に停止ボタン 3 つ + キーボード
  (Space / Z・X・C)+ 回転中のコマ送り + ぼかし表示 + 回転中のベルナビ仮表示を実装。
  目押しモード・押し順セレクトは**オート消化専用へ縮退**(手動時はタイミング押しが正)。
  ブラウザ実機確認 + 録画済み(ボタン・キーボード両操作 / 変則押し右→中→左 /
  押し順ベル 1 枚・13 枚の作り分け / オート消化)

#### STEP 3b: リール回転アニメーションの本実装 — 中 — **完了(AGENT #036)**

- コマ帯(20 コマ × 3 リール)の連続スクロール描画(`requestAnimationFrame` ベース。
  回転速度は定数 約 750ms/周)
- 停止ボタン押下 → スベリ(押下位置から停止位置まで回転を継続)→ 停止、の描画。
  最大 4 コマのスベリが視覚的に分かること
- 押下瞬間のコマ取得(アニメーション時刻 → 中段コマ番号)の正確性を単体テスト +
  実機確認(赤7 を狙って止める目押しが実用的に可能なこと)
- 完了条件: グリーン + ブラウザで目押し(赤7 狙い 7 揃い / DDT 黒バー狙い)の実機確認(録画)
- **実施結果(AGENT #036)**: `src/ui/reelAnimation.ts` を新設(React 非依存の純ロジック)。
  連続位置 `continuousPosition`(小数コマ)は `floor` すると 3a の離散モデル
  `spinningPosition`(押下位置の判定)と常に一致することを網羅テストで固定し、
  「描画で中段にあるコマ = 押下位置」の正確性を保証。スベリは `planSlip` / `slipPosition`
  (スベリ 1〜4 = 同速で前進を継続 ≤ 150ms / スベリ 0 = 1 コマ未満の戻りで中央へ)。
  描画は 5 コマのコマ帯(`reelStrip`)を translateY で下方向スクロールし、
  `requestAnimationFrame` で毎フレーム再描画(コマ送り setInterval を置換)。
  最終リールはスベリ完了を待って 1G を締める。ブラウザ実機確認 + 録画済み
  (赤7 狙い 7 揃い(2 コマ手前・ビタ・4 コマ手前 = 最大スベリ)/ 8 コマ手前 =
  目押し外しの取りこぼし / DDT 黒バー下段停止 / マウス・キーボード操作。
  console エラーなし)

#### STEP 3c: 払出・クレジット・獲得枚数・押し順ナビ表示 — 中 — **完了(AGENT #037)**

- クレジット / BET / 払出枚数の表示(7seg 風の仮デザイン)。3 枚掛け固定・リプレイ自動 BET・
  クレジットはひとまず上限なし(不足時の補充も自動。実機的な精算は STEP 6 の永続化と合わせて検討)
- AT 中の獲得枚数表示(AT 開始からの純増。`AT_START` でリセット)
- 押し順ナビ表示(AT・エンディング中のベル当選時、レバーオンでリール窓上にナビ数字を表示。
  全停止で消す)。ナビ無視のペナルティは実装しない(確定 7 = 将来実装)
- 完了条件: グリーン + ブラウザで AT 中のナビ表示 → ナビどおり停止 → 13 枚払出の実機確認(録画)
- **実施結果(AGENT #037)**: メーター管理の純ロジックを `src/ui/counters.ts` へ新設
  (`meterOnLever` = BET 徴収・リプレイ自動 BET・不足時 50 枚単位の自動補充 + 払出表示リセット /
  `meterOnFinish` = 払出加算 + AT 獲得枚数の更新。`AT_START` で 0 リセットし、ゲーム開始時点の
  フェーズが AT / エンディングのゲームの純増を加算 = 上位 AT・エンディングを跨いで継続)。
  `App.tsx` の筐体下に 7seg 風メーターパネル(CREDIT / BET / WIN + REPLAY ランプ +
  AT 中のみ AT 獲得枚数)を追加し、押し順ナビは各リール窓上部の数字表示
  (何番目に押すか。リール停止ごとに消灯)へ置換(3a の仮表示 `navi-overlay` を廃止)。
  ブラウザ実機確認 + 録画済み(AT 中のナビ 2-1-3 表示 → ナビ順停止で数字消灯 →
  ベル揃い WIN 13・CREDIT +10・AT獲得 +10/G × 2 ゲーム / リプレイの REPLAY ランプ +
  クレジット不変 / 初期 CREDIT 50 → レバーで 47。console エラーなし)

#### STEP 3d: 演出表示(前兆・連続演出・AT・エンディングの仮演出)+ SE/BGM 連動強化 — 中 — **完了(AGENT #038)**

- `GameEvent` → 画面演出のマッピング層を新設(STEP 4 でシナリオテーブル(確定 28)へ
  差し替える前提の構造にする): 前兆中の仮テロップ / 連続演出 4G の仮画面(種別 A/B/C・n/4G・
  最終 G の成否告知)/ AT 突入・セット継続・上位 AT 突入・エンディングの仮カットイン
  (テキスト + 既存仮素材でよい)
- SE の整理(レバー・停止・払出・告知)と BGM 切替の調整(フェード等)
- 完了条件: グリーン + ブラウザで「レア役 → 前兆 → 連続演出 → AT 突入」の演出表示を実機確認(録画)
- **実施結果(AGENT #038)**: 演出マッピング層 `src/ui/direction.ts`(React 非依存)を新設。
  出力は宣言的データのみ: `StateOverlay`(フェーズ由来の常時表示 = 前兆テロップ / 連続演出
  全画面 n/4G / エンディングバナー)+ `Cutin`(イベント由来のワンショット = 成否告知・
  AT 突入・セット継続・V ストック・上位 AT・エンディング・AT 終了。発行順にキュー表示)。
  描画は `src/ui/DirectionLayer.tsx`(液晶へ重ねる。カットインは durationMs ずつ順次表示 +
  表示開始時に SE)。**STEP 4 のシナリオ方式への差し替えは direction.ts の対応表の置き換え
  だけでよい**(型と描画側は不変)。内部情報イベント(モード移行・偽前兆突入・書き換え・
  背景移行)は演出にしない。SE は**サウンドキュー層 `src/ui/sound.ts`(用途 ID → ファイルの
  対応表)経由に一本化**し、BGM(`STAGE_BGMS`)とともに後から実素材へ差し替え可能。
  仮 SE 2 種(`se_telop` / `se_fail`)を追加生成し、BGM 切替はクロスフェード
  (`platform/audio.ts`)。単体テスト 12 件(`direction.test.ts`)+ ブラウザ実機確認・録画
  (リーチ目 → 本前兆テロップ → 連続演出 A 4G → 勝利! → AT突入!(継続率付き)→
  V ストック獲得!、コンソールエラーなし)

#### STEP 3e: デバッグ UI 整理 + 総合ブラウザ確認 + 動作確認手順書 — 小〜中 — **完了(AGENT #039)**

- デバッグパネル・イベントログ・強制役セレクト等を折りたたみ(開発用に残す。デフォルト閉)
- 通常プレイ視点でのレイアウト調整(筐体 + 遊技パネル中心の画面構成)
- 通しプレイのブラウザ総合確認(通常 → 前兆 → AT → 上位 AT → エンディング → 通常復帰。録画)
- ユーザー向け動作確認手順書 `docs/STEP3_VERIFICATION.md` を作成(STEP1 版の形式を踏襲)
- 完了条件: グリーン + 総合実機確認記録、`docs/ROADMAP.md` の STEP 3 完了マークと HANDOVER 更新
- **実施結果(AGENT #039)**: デバッグ系 UI(ステータス / 強制役・押し順・目押しセレクト /
  ステージ手動切替・リセット / イベントログ / ゲーム履歴)を `<details>` の
  「デバッグ(開発用)」へ集約(デフォルト閉)。通常プレイ視点は筐体 + メーター +
  停止ボタン + レバーオン・オート消化・BGM を中央 1 カラムに配置(コアロジック・
  UI 純ロジックは無変更。`App.tsx` + `App.css` のみ)。ブラウザ総合確認 + 録画済み
  (手動 3G 消化 / 中段チェリー強制 + オート消化で 前兆テロップ → 連続演出 4G →
  AT 突入 → 10 連 → エンディング 10G → 上位AT → 10 連 → 2 回目エンディング →
  AT終了(エンディング完走)→ 通常復帰。console エラーなし)。
  手順書 `docs/STEP3_VERIFICATION.md` を作成(自動チェック + チェックリスト 15 項目)

### STEP 4: 演出の作り込み — 大 → **4a〜4f の 6 サブステップに分割**(AGENT #040 で計画)

STEP 3e(デバッグ UI 整理 + 総合確認)は完了済みのため、着手可能。
演出リスト確定版・実素材(SPEC「14. 未確定事項」1・4 と下記依頼リスト)の入稿待ちと並行できるよう、
「仮素材・仮シナリオテーブルで進める実装(4a〜4e)」と「実素材差し替え(4f)」を分離している。

**分割の方針**: STEP 1〜3 と同じく **1 サブステップ = 1 AGENT = 1 PR**。原則 4a → 4e の順で実施し、
各 PR を 1 本ずつマージしてから次へ進む。**4f(実素材差し替え)のみ素材入稿のタイミングに依存する**ため、
入稿があり次第 4b〜4e のどの時点でも先行して実施してよい(`incoming/` の取り込み自体は従来どおり
「入稿されたら最優先」のルールを維持)。各サブステップは「テスト・lint・ビルドがグリーン +
UI を触るものはブラウザ実機確認(録画)済み」で終わるよう設計する。

**全体設計(4a で確定させ、以後のサブステップは従う)**:

- 演出は**シナリオ方式(確定 28)**: 前兆当せん時(前兆種別・総 G 数・連続演出種別の確定時)に
  「各 G の演出(予告 ID・強度)の並び」を一括抽せんし、毎 G はシナリオテーブル参照で表示する。
- レイヤーは 3 層を維持する: **シナリオ抽せん = `src/core/`**(乱数を使い、テストで振分け検算する)/
  **演出の見た目(文言・画像・時間・SE)= `src/ui/direction.ts` の対応表**(3d で確立)/
  **描画 = `DirectionLayer.tsx`**(宣言的データを表示するだけ)。SE は `playCue`・BGM は
  `STAGE_BGMS` 経由の規約(3d)も維持する。
- `GameState` の骨格(2a)は変えない。前兆フェーズ(`OmenPhase` / `RenzokuPhase`)への
  シナリオ情報の追加は**フィールド追加のみ**で行う(判別可能ユニオンの構造・処理順序は不変)。
- 演出リスト・シナリオテーブルの中身が未確定の項目は、**4a の推奨案(仮テーブル)で実装を進め、
  確定版の回答・入稿があり次第データ差し替えで追随する**(構造は変えない。STEP 2 の
  質問リスト Q1〜Q11 と同じ「推奨案付きで確認 → 回答待ちでも進める」方式)。

#### STEP 4a: 演出仕様の整理 + シナリオテーブル設計 + 質問リスト — 小〜中(ドキュメント中心)— **完了(AGENT #042)**

- Excel の演出構成シート(演出構成全体 / 各背景シート)を精読し、**演出 ID の一覧表**へ整理:
  通常 5 背景 × 12 種(固有予告 5 + 連続演出 A/B + 共通予告 4 + 連続演出 C。前兆背景のみ 6 種)/
  AT・上位 AT の小役パート予告 3 種 + バトルパート約 20 パターン(SPEC「7.」8G 構成表)
- **シナリオテーブルのデータスキーマを設計**(前兆種別(本/偽)× 総 G 数(7〜10)→ 各 G の
  予告 ID・強度の割付。強度カーブ・連続演出 A/B/C の 4G 構成を含む)
- **質問リスト(推奨案付き)を作成**しユーザーへ確認: 予告の振分け値 / 通常時(非前兆)の
  予告発生有無 / バトル 8G のパターン振分け(チャンス系の出現率)/ 偽→本書き換え時の
  シナリオ扱い(引継ぎ or 差し替え)/ エンディング演出の尺(`ENDING_GAMES` = 10 の確定)等
- 完了条件: 演出一覧 + スキーマ + 質問リスト + 仮シナリオテーブル案を docs へ記録(コード変更なし)
- **実施結果(AGENT #042)**: 成果物は **`docs/DIRECTION_SPEC.md`**(演出 ID 一覧 / シナリオスキーマ /
  仮振分けテーブル / 仮素材ムービー命名規約(142 本)/ 質問リスト Q12〜Q20)。
  着手時のユーザー指示で予告の役割が確定(**SPEC 確定 33**): 通常 4 背景の固有 5 + 共通 4 は
  各演出とも**弱 / 強の 2 パターン** / 前兆背景の固有 1・2・3 は**期待度の差**(弱・中・本前兆確定)/
  AT・上位 AT の小役予告は 固有 1 = ベルナビ(押し順示唆)・固有 2 = レア役示唆・
  固有 3 = 強予告(V ストック濃厚)/ **予告は全てムービー**で、小役示唆系は最終的に図柄画像を表示。
  スキーマは「スロット(固有 1〜5 / 共通 1〜4)× 強度レベル L0〜L3(なし / 弱 / 強 / 確定。
  L3 は本前兆のみ)」の背景非依存表現とし、具体ムービーの解決は表示時に
  「現在の背景 × スロット × レベル」で行う(前兆背景はスロット無視でレベル → 固有 1/2/3 へ)。
  AT 小役予告は毎 G・成立役ベース / バトル 8G はバトル開始時の継続確定状態でルート一括抽せん
  (8G 目復活判定のみ実結果)の方式を設計。振分け数値は Q12〜Q20 の推奨案 = 仮テーブルで
  4b〜4e を先行し、回答があり次第データ差し替え

#### STEP 4b: シナリオ抽せん機構(コア)— 中 — **完了(AGENT #043)**

- `src/core/scenario.ts` 新設: シナリオテーブル(データ駆動。4a の仮案でよい)+
  前兆当せん時の一括抽せん関数
- `state.ts` の前兆スケジュール抽せん(スロット 3。`initGameState`・AT 終了時の本前兆リドロー含む)へ
  配線し、フェーズ(`OmenPhase` / `RenzokuPhase`)と毎 G 出力へシナリオ情報を追加
- 偽→本書き換え時のシナリオ扱い(4a の回答 or 仮案)を実装
- 乱数消費順序が変わるため `flow.test.ts` のシード・`simulate.test.ts` の固定値を取り直す
  (HANDOVER 記載の既知の注意点)
- 単体テスト: 振分け検算 / シナリオ長と前兆 G 数の一致 / 書き換え時の扱い
- 完了条件: グリーン(UI・direction.ts は未変更でよい)
- **実施結果(AGENT #043)**: 着手時に **Q12〜Q20 の全回答を受領(SPEC 確定 34)**し、
  予告を「小役示唆系(固有 1〜3 / 共通 1・2)= 毎 G・成立役ベース」と「前兆系(固有 4・5 /
  共通 3・4)= シナリオ方式」の 2 系統へ分離して DIRECTION_SPEC を全面更新
  (バトルは約 20 ルート + 勝利/敗北/復活/継続率別振分け = Q18 回答)。
  `src/core/scenario.ts` に `drawOmenScenario`(強度カーブ L0〜L3 + スロット + チャンスアップ)/
  `drawKoyakuHint` / `drawAtYokoku` / `drawBattleRoute` / `drawRevival` を実装
  (後者 4 つは `advanceGame` の乱数列を汚さない独立関数)。`scheduleOmen` へ配線し
  `OmenPhase.scenario` / `RenzokuPhase.chanceUps` を追加(書き換え時は引継ぎ = Q16)。
  `flow.test.ts` シード 62 → 30・`simulate.test.ts` 固定値取り直し。テスト 293 件(+19)グリーン。
  1M G シミュレーションで機械割 130.60%(確定 32 の範囲を維持 = 出玉への影響なし)を確認

#### STEP 4c: 通常時・前兆中の予告演出表示 — 中 — **完了(AGENT #044)**

- `direction.ts` を「イベント → 仮演出」から「**シナリオテーブル → 演出**」の解決へ置き換え
  (3d ヘッダー記載の差し替え方針どおり。`StateOverlay` / `Cutin` の型と描画側は不変)
- 予告 ID 別の表示部品(現行テロップを置換。テキスト + 静止画/既存動画 + SE の仮素材でよい)
- ブラウザ実機確認: 前兆 7〜10G でシナリオどおりの予告が発生すること(本/偽で強度が変わること)。録画
- 完了条件: グリーン + 実機確認記録
- **実施結果(AGENT #044)**: 予告ムービー仮素材 51 本(通常 4 背景固有 1〜5 × 弱/強 = 40 +
  共通 1〜4 × 弱/強 = 8 + 前兆背景ラダー 1〜3 = 3)を `scripts/gen_placeholder_assets.py`
  (`yokoku` 対象を追加)で生成し `src/assets/video/yokoku/` + `manifest.json` へ登録。
  `direction.ts` を「シナリオ → 演出」解決へ置換: **前兆シナリオ予告はレバーオン時に
  「これから回すゲーム」のステップ(`stepAt(scenario, phase.game + 1)`)を
  「現在の背景 × スロット × レベル」で解決**(`scenarioYokokuAtLeverOn`。通常 4 背景 =
  L1 → 弱素材 / L2・L3 → 強素材、前兆背景 = スロット無視でレベル → 固有 1/2/3 の
  期待度ラダー = 確定 33)。**小役示唆予告はレバーオン時に成立役から `drawKoyakuHint` を
  UI 専用 rng で抽せん**(通常時も出す = Q12)し、ムービー + 成立役の図柄画像
  (CSS 遅延フェードイン = 確定 33)で表示。競合規約(シナリオ予告優先 / 前兆背景では
  小役示唆なし / 連続演出・AT・エンディング中はなし = DIRECTION_SPEC 2.1)を実装。
  3d の仮テロップ(`TELOP_TEXTS`)は廃止(予告のない G は静かに進む)。
  デバッグパネルへ「予告演出」行と前兆シナリオの一括表示(L1固4 等)を追加。
  テスト 304 件(+30)グリーン。ブラウザ実機確認 + 録画(本前兆 10G のシナリオどおりの
  予告発生・L0 ゲームは予告なし・前兆背景の期待度ラダー・小役示唆予告の図柄表示・
  連続演出 → AT 突入の回帰)

#### STEP 4d: 連続演出 A/B/C の作り込み + 成否告知 — 中

- 連続演出 4G の画面構成(A/B = 背景固有 / C = 背景不問)を 4a の構成表どおりに実装
  (現行の全画面仮表示を置換。素材は仮でよい)
- 成否告知(勝利/敗北)の演出強化と、偽→本書き換え時の演出継続(確定 21(c))の見え方を確認
- ブラウザ実機確認: A/B/C 各 4G + 成否告知 + 書き換えパターン。録画
- 完了条件: グリーン + 実機確認記録

#### STEP 4e: AT・上位 AT・エンディング演出 — 中〜大

- 小役パート予告 3 種 / **バトルパート 8G 構成**(SPEC「7.」の表: 導入 → 台詞 → 攻撃決め →
  攻撃 → 判定 → 帰結 → 最終。継続/敗北の分岐と 8G 目の第 3 リール停止時の復活告知)。
  上位 AT は共闘版(4G 目ダブル攻撃 / 6G 目ヒット判定 / 8G 目復活判定)
- セット継続・バトル敗北・上位 AT 突入・エンディング(10G。`EndingPhase.after` で
  突入前/完走を描き分け)の演出。エンディング尺の確定(4a の回答)で `ENDING_GAMES` を調整
- ブラウザ実機確認: AT 突入 → セット継続 → 10 連 → エンディング → 上位 AT → 完走の通し。録画
- 完了条件: グリーン + 実機確認記録

#### STEP 4f: 実素材の差し替え + 総合確認 — 小〜中(素材入稿に依存。先行実施可)

- `incoming/` 入稿素材の取り込み(`scripts/import_incoming_assets.py` の対応表を拡張):
  連続演出・カットイン・告知・エンディングの動画 / AT・上位 AT の小役・バトル個別背景動画
  (現状共用中)/ BGM・SE の実素材
- 対応表の張り替えで差し替え完了(3d で確立済み: SE = `SOUND_CUES` / BGM = `STAGE_BGMS` /
  演出動画 = `EFFECT_VIDEOS`・シナリオテーブルの参照素材)。`manifest.json` へ出所登録
- 総合ブラウザ確認 + ユーザー向け動作確認手順書 `docs/STEP4_VERIFICATION.md`(STEP1/3 版の形式を踏襲)
- 完了条件: グリーン + 実機確認記録、`docs/ROADMAP.md` の STEP 4 完了マークと HANDOVER 更新

### STEP 5: exe パッケージング(Tauri)+ CI — 中 → 2 サブステップの想定

**5a と 5b で分割する想定**(着手前に計画 AGENT が詳細を確定してよい):

- **5a: Tauri 導入 + ローカル動作確認**: デスクトップシェルで Web 版と同一動作すること。
  `src/platform/` ラッパー(音声・動画・(将来)永続化)のデスクトップ動作確認と必要な実装差し替え
- **5b: CI ビルド + 配布**: GitHub Actions(Windows ランナー)で exe を自動ビルドし Release へ添付。
  ビルド手順・動作確認手順のドキュメント化
- リスク: VM(Linux)では Windows exe の実機確認ができないため、5b はユーザーの Windows 環境での
  確認を依頼する前提で手順書を整備する

### STEP 6: 仕上げ — 小〜中(項目ごとに独立。優先順はユーザー指示で決める)

- **6a**: ゲーム履歴・スランプグラフ・オートプレイ(シミュレーションモード)
- **6b**: 遊技データの永続化(継続プレイ。`src/platform/` 経由で Web = localStorage / exe = ファイル)
- **6c**: ペナルティ実装(通常時の変則押し。確定 7 = 将来実装の解禁)
- **6d**: 白バー・ブランクの役割追加(ユーザーの構想確定待ち = SPEC 未確定 2。リーチ目構成の
  拡張になる場合は停止制御網羅テストの取り直しを伴う)

## ユーザーに依頼したいもの(素材・仕様)

| # | 内容 | 使うステップ |
|---|---|---|
| 1 | 演出リスト確定版(各背景の予告演出・連続演出 A/B/C の内容と振分け)。**予告の役割・体系は確定 33 で確定済み**。残りは振分け値等 = `docs/DIRECTION_SPEC.md` の Q12〜Q20 への回答 | STEP 4a〜4e(未確定でも仮案で先行し、確定後にデータ差し替え) |
| 2 | AT 小役パート / バトルパートの個別背景動画(上位 AT も同様) | STEP 4f |
| 3 | 連続演出・カットイン・告知・エンディングの動画素材 | STEP 4f |
| 4 | BGM・SE の実素材(現状サイン波合成の仮素材) | STEP 4f |
| 5 | 白バー・ブランクの役割(リーチ目構成に使う等の構想) | STEP 6d |
| 6 | 背景移行 30G の確定値(現状 30 のまま。`BACKGROUND_ELAPSED_GAMES` で変更容易) | いつでも |

素材の入稿はこれまで通り `incoming/` へのプッシュでよい(手順は `incoming/README.md`)。仕様の回答はテキストで OK。

## 進め方のルール(再掲)

- 1 AGENT に対するやり取りは 1 回。大きい STEP は「実装 → 指摘反映」で 2 AGENT に分けてもよい
- STEP 1 は 1a〜1f、STEP 2 は 2a〜2f、STEP 3 は 3a〜3e、STEP 4 は 4a〜4f のサブステップへ分割済み。**1 サブステップ = 1 AGENT = 1 PR** で順番に進める(STEP 4f のみ素材入稿に合わせて先行実施可)
- 各 STEP 完了ごとに PR を 1 本ずつマージする(`docs/HANDOVER.md` が競合しやすいため)
- 数値(確率・配当・配列)は必ず SPEC.md と実装・テストを一致させる
