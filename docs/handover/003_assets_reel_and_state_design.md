# 引継ぎ資料(最新)

- 作成者: AGENT #003(素材入稿手順の整備・リール制御方針・状態構成の仕様化)
- 作成日: 2026-07-04
- 履歴コピー: `docs/handover/003_assets_reel_and_state_design.md`

## プロジェクト概要

パチスロアプリケーションを開発するプロジェクト。
**1 AGENT につきユーザーとのやり取りは 1 回のみ**。作業終了時に必ずこのファイルを更新し、`docs/handover/` に履歴コピーを残すこと(詳細ルールは `AGENTS.md` 参照)。

## これまでの経緯

1. ユーザーがプロジェクトルール(1 AGENT 1 やり取り + 引継ぎ資料の作成)を制定。
2. AGENT #001 が開発計画を `docs/DEVELOPMENT_PLAN.md` として文書化(TS + React + Vite + Vitest、core/UI 分離、Phase 0〜5)。
3. AGENT #002 が Web + exe(Tauri)両対応方針と `docs/ASSET_GUIDELINES.md`(素材管理ルール)を策定。
4. ユーザーから以下の指示・情報提供があり、AGENT #003 が対応:
   - **素材を提供する意向**(筐体画像、状態別のループ背景動画+演出動画 1 本、リール図柄、状態別 BGM、基本 SE)→ `incoming/` ディレクトリ経由の入稿フローを `docs/ASSET_GUIDELINES.md` に整備。動画(WebM)の管理規定を追加し、バイナリ上限を 300 MB 目安に引き上げ。
   - **リールは各 20 コマ**。
   - **内部抽選状態は 12 種**(通常 3 / CZ 2 / 前兆 1 / ボーナス 2 / AT 4)。レア役・ゲーム数で遷移。
   - **演出(背景・BGM)も同数の状態を持つが内部状態と 1:1 ではない** → 演出ステージを別レイヤー(`src/presentation/`)で管理し、ステージ移行抽選で対応付ける方針に。
   - 背景・演出は基本的に動画で保持。
   - 以上を `docs/SPEC.md`(骨子・ドラフト)、`docs/DEVELOPMENT_PLAN.md`(Phase 0/2/3/5 更新)に反映。

## 現在の状態

- コードは未実装。リポジトリにはドキュメントのみ。
  - `AGENTS.md` … プロジェクトルール
  - `docs/DEVELOPMENT_PLAN.md` … 開発計画(リール制御は「引き込み優先度探索方式」で進める方針を Phase 2 に明記)
  - `docs/ASSET_GUIDELINES.md` … 素材管理ルール(`incoming/` 入稿フロー、動画 WebM 規定を含む)
  - `docs/SPEC.md` … 機種仕様書ドラフト(内部状態 12 種の一覧、演出ステージ対応表、20 コマ前提。確率等は TBD)
- 決定済みの方針:
  - Web アプリ主軸 + Tauri で exe 配布(Phase 4.5)。
  - 素材はユーザー入稿を第一とし、`incoming/` → AGENT が変換して `src/assets/` に取り込む。動画は WebM(VP9)、音声は OGG。
  - リール 20 コマ、内部状態 12 種、演出は別レイヤー(ステージ移行抽選)。

## 次の AGENT へのタスク

ユーザーの指示内容を最優先とした上で、次を実施:

1. **`incoming/` に素材が入稿されていたら最優先で取り込む**(`docs/ASSET_GUIDELINES.md` の手順に従い変換・配置・manifest 登録)。
2. `docs/SPEC.md` の TBD を確定させる(ユーザー回答があれば反映。なければ図柄・リール配列・確率の叩き台を作って提案)。
3. 仕様が固まり次第 Phase 1(プロジェクト雛形 + core の rng/lottery/payout 実装とテスト)に着手。
4. 作業終了時に本ファイルを更新し、`docs/handover/004_*.md` に履歴を残す。

## 注意事項

- ブランチは `cursor/<説明>-<suffix>` 形式で作成し、PR を出すこと(suffix は環境から指示される)。
- `AGENTS.md` のルール(テスト実行、不要ファイル削除禁止、PR 用の簡潔なまとめ)を守ること。
- ブラウザ API(音声・動画再生・永続化)は `src/platform/` のラッパー経由で使うこと(exe 対応のため)。
- 素材を追加する際は必ず `docs/ASSET_GUIDELINES.md` に従うこと。
- 過去の PR(#1〜#3)が未マージの場合、ドキュメントの最新状態は本ブランチ系列にある点に注意(main にはまだ docs が無い可能性がある)。
