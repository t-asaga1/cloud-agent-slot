# AGENT #087: 会話予告 12 枚をキャラデザイン準拠で作り直し + 同時表示仕様を確定

- 作成日: 2026-07-17
- ユーザー指示: 「背景固有予告 3 を作り直し。キャラが設定画から変わってしまっている
  (義経がポニーテール化 / 頼朝は別デザイン化)。表情・ポーズは変えてよいが
  キャラクターデザインは高解像度キャラ設定から変えない。
  一言目と二言目は**同時表示**(レバオンで一言目 → 一言目は消えず第 1 停止で二言目。
  枠の表示は重なってよい)。第 3 停止の全画面は基本 OK だがキャラデザインを維持。
  必要なら fal.ai の GPT Image 2 で作成してよい」
- アプリのコード変更なし。**再生成のみでユーザー確認待ち = 組込み未実施**。

## 1. 作り直しの内容

- **生成手段を内蔵 GenerateImage → fal.ai の `openai/gpt-image-2/edit` へ変更**
  (ユーザー承認済み。Secret `FAL_KEY2` 使用)。
- 新スクリプト **`scripts/gen_yokoku_kaiwa.mjs`**(コミット済み):
  - 参照画像 = 設定資料ペア(`incoming/reference/設定資料/<キャラ>_顔.png` + `_全身.png`)
    + 一言目は承認済み枠(`incoming/yokoku/会話予告_枠.png`)/ 二言目は一言目の生成画像
    (連鎖参照)/ 全画面は設定資料ペアのみ。
  - キャラごとに「維持する特徴 + **描いてはいけない特徴(負の指定)**」をプロンプトへ明記:
    義経 = ポニーテール・結んだ髪禁止 / 頼朝 = 黒髪・短髪・烏帽子・髭禁止
    (白銀長髪 + 額の紋様 + 角飾り付き黒冠を必須指定)/ 静 = 髪を長くしない /
    弁慶 = 頭巾 + 大数珠必須。
  - 使い方: `node scripts/gen_yokoku_kaiwa.mjs <キャラ> <バリエーション>`
    (出力 = `/tmp/kaiwa_gen/`。fal.ai 課金のため再実行は必ずユーザー承認後)。
- **12 枚(4 キャラ × 一言目/二言目/全画面。1536x1024 = 3:2)を全て再生成し
  `incoming/yokoku/` の同名ファイルを差し替え**(枠は承認済みのため再利用・変更なし)。
- 義経一言目のみ 1 回目で枠がレターボックス化(上下黒帯)したため
  「枠を四辺いっぱいに描く」をプロンプトへ追記して再生成(採用 = v2)。計 13 回の生成。
- 全 12 枚で設定資料との一致を目視検品済み
  (義経 = 茶髪ショート / 頼朝 = 白銀長髪 + 額の紋様 + 角飾り付き黒冠 /
  静 = 紺ボブ + 立烏帽子 / 弁慶 = 白頭巾 + 緑大数珠)。

## 2. 表示仕様の確定(ユーザー回答。12.7 の 3 段階を更新)

- **一言目と二言目は同時表示**: レバオンで一言目 → 第 1 停止で一言目を**消さずに**
  二言目を追加表示する。**枠自体の表示は重なってもよい**。
- 第 3 停止の全画面(強)は従来どおり。
- 正 = `docs/YOKOKU_PRODUCTION_PLAN.md`「12.8」。

## 3. 組込みランへの引継ぎ(ユーザー OK 後)

- WebP 化: **全画面 3 枚は中央クロップだと頭部(義経の髪・頼朝の冠)が僅かに切れるため
  上端基準(上 0 / 下 160px)クロップ推奨**。一言目・二言目は枠ごと縮小表示するため
  3:2 のままでもよい。
- 表示実装: レバオン = 一言目ウィンドウ / 第 1 停止 = 二言目ウィンドウを追加表示
  (小役成立時)/ 第 3 停止 = 全画面(強)+ 台詞のアプリ側テキスト描画 + 台詞テーブル。
- 確認事項(従来どおり): どの背景でどのキャラを出すか(登場キャラルール・頼朝の扱い)/
  弱の振分け解釈 / 組込み後の原本削除 + `yokoku_<bg>_koyu3_*` ムービー仮素材の扱い。

## 4. テスト

- テスト 408 パス・lint グリーン(アプリのコード変更なしのため現状確認のみ)。
