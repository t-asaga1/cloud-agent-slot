# 引継ぎ資料(最新)

- 作成者: AGENT #011(残り未確定事項 3 件の回答反映 + 大容量ファイルのプッシュ手順ガイド)
- 作成日: 2026-07-10
- 履歴コピー: `docs/handover/011_remaining_answers_and_push_guide.md`

## プロジェクト概要

パチスロアプリケーション「**義経物語**」を開発するプロジェクト。
**1 AGENT につきユーザーとのやり取りは 1 回のみ**。作業終了時に必ずこのファイルを更新し、`docs/handover/` に履歴コピーを残すこと(詳細ルールは `AGENTS.md` 参照)。

## 最重要: 仕様の正

- **`docs/specs/義経物語_仕様_260709.xlsx` が正式仕様(マスター)**。
- ただし **2026-07-10 のユーザー回答 16 件(`docs/SPEC.md`「13. 確定事項」)は Excel より優先**(Excel 誤植の訂正を含む)。主な訂正:
  - AT 終了後モード振分けの残り 0.0001 は地獄へ加算(地獄 0.3156)。
  - 本前兆移行時の背景テーブルの静・弁慶行は自背景 0.25(Excel の 0.5 は誤植)。
  - 偽前兆の突入条件はモード移行シートが正(レア役 1/10。「天国アップ時の 1/5」は不採用)。
- 有効ラインは横 3 + 斜め 2 の 5 ライン。タイミング目押しあり。チェリー・スイカ・**リーチ目 7 揃い**は取りこぼし許容。設定差なし。

## これまでの経緯

1. AGENT #001〜#005: 開発計画策定(TS + React + Vite + Vitest)、Web + exe(Tauri)両対応方針、素材入稿フロー(`incoming/`)、Phase 1 コアロジック、main への統合。
2. AGENT #006: Excel 仕様書の入稿方法を整備(PR #9)。
3. AGENT #007〜#008: 筐体画像取り込み、仮素材 40 件生成、Phase 2 リール制御(旧叩き台)、PR 競合解消。
4. AGENT #009: ユーザー入稿の Excel 本仕様書(全 16 シート)を読み取り・検算し `docs/SPEC.md` へ全転記。不整合・質問 17 件をユーザーへ送付(PR #10 ブランチ)。
5. AGENT #010: 回答 13 件を `docs/SPEC.md` へ反映し、`src/core/` を Excel 仕様へ整合(役 8 種・単一確率テーブル・押し順ベル払出)。Phase 3 の抽せんテーブル(`mode.ts` / `background.ts` / `omen.ts` / `at.ts`)をデータ駆動で実装(PR #11 ブランチ)。
6. **AGENT #011(今回)**: 残っていた未確定事項 3 件への回答を受領し反映(PR #11 ブランチの続きとして作業):
   - **リーチ目 7 揃い**: 目押しを外したら**取りこぼし扱い**(払出なし・代替リーチ目停止もなし)→ SPEC「13. 確定事項」回答 14。
   - **背景移行の優先順位**: 30G 経過と偽前兆当せんが同時なら**偽前兆当せん優先**。**30G カウンタは背景移行時にリセット**。30G は後から変更の可能性あり → 回答 15。`background.ts` に `BACKGROUND_ELAPSED_GAMES = 30` として定数化し、契機名を `ELAPSED_30G` → `ELAPSED` に改名(値変更に備える)。
   - **純増想定**: AT 中 ≒ +6.4 枚/G・通常時 ≒ −1.8 枚/G(コイン持ち約 27G/50 枚)で**問題なし** → 回答 16。
   - ユーザーからの質問「git コマンド / GitHub Desktop でのプッシュ方法」への回答として、`incoming/README.md` に **25 MiB 超ファイルのプッシュ手順**(GitHub Desktop の操作手順・git コマンド例・PAT 認証の注意)を追記。
   - テスト 81 件全パス、lint / build 成功。

## 現在の状態

- Phase 1〜2 完了 + Phase 3 の抽せんテーブル層が完了(状態遷移の通しフロー = ステートマシンは未実装)。
- **`reel.ts` は暫定**: 旧叩き台の配列(白7 あり)+ 中段 1 ラインのまま、役名だけ新仕様に適合(弱・強スイカは同一停止形、リーチ目は赤7 中段揃いで暫定表現、チャンス目はハズレと同じ制御)。
- 演出リスト(各背景の予告・連続演出)は「仮」のまま。背景動画・図柄素材は未入稿。**ユーザーがこの後、大きい動画を git / GitHub Desktop でプッシュしてくる可能性が高い**(手順は `incoming/README.md` に案内済み。main 直接コミットで来る想定)。
- 仕様の未確定事項は `docs/SPEC.md`「14.」の 3 件のみ(演出リスト確定版・白バー/ブランクの役割・30G の暫定値)。

## 次の AGENT へのタスク

ユーザーの指示内容を最優先とした上で、次を実施:

1. **`incoming/` に素材(背景動画等)が入稿されていたら最優先で取り込む**(`docs/ASSET_GUIDELINES.md`)。大きい動画は WebM(VP9)720p ループへ再圧縮して `src/assets/` へ。背景 9 種(SPEC「5.」)に合わせて仮素材の構成変更も必要。
2. **`reel.ts` の本対応**: Excel 配列(SPEC「3.」)への差し替え + 有効ライン横 3 + 斜め 2 + 確定した停止挙動(リプレイ 5 ライン 100% 引き込み、押し順ベルの上段/斜め停止、DDT 黒バー制御、リーチ目 7 揃い=目押し失敗時は取りこぼし)。網羅テストの再設計込みで大きめの作業。
3. **Phase 3 の通しフロー実装**: `mode.ts` / `background.ts` / `omen.ts` / `at.ts` の部品を組み合わせた 1 ゲーム消化のステートマシン(`state.ts`)。確定事項(SPEC「13.」)の 9〜12、14〜15(前兆中の挙動、偽前兆→本前兆書き換え、AT セット管理・10 連・上位 AT・エンディング、背景移行の優先順位と 30G カウンタリセット)を実装しテストする。
4. 作業終了時に本ファイルを更新し、`docs/handover/012_*.md` に履歴を残す。

## 注意事項

- ブランチは `cursor/<説明>-<suffix>` 形式で作成し、**PR は必ず base を `main` にして作成すること**(suffix は環境から指示される)。
- **PR は 1 本ずつマージすること**(本ファイルが競合しやすい)。競合したら main をブランチへマージして解消する。
- 今回の作業ブランチ(PR #12)は **PR #10・PR #11 の内容を全て含む上位互換**。PR #12 のみマージし、PR #10 / #11 はクローズすること。
- `AGENTS.md` のルール(テスト実行、不要ファイル削除禁止、PR 用の簡潔なまとめ)を守ること。
- **確率・配当・配列などの数値は SPEC.md(Excel + 確定事項)と `src/core/` の実装を必ず一致させる**。抽せんテーブルは分母付き整数(10000 / 100 / 1000)で持ち、行合計 = 分母をテストで検証している。
- **背景移行の 30G は `BACKGROUND_ELAPSED_GAMES`(`src/core/background.ts`)で定数化済み**。値の変更指示が来たら定数と SPEC・テストをセットで更新する。
- 実在機種の素材・画像の流用は禁止(リール配列シートの図柄画像は参考イメージ)。
- ブラウザ API(音声・動画再生・永続化)は `src/platform/` のラッパー経由で使うこと(exe 対応のため)。
- 素材追加時は `docs/ASSET_GUIDELINES.md` に従い `manifest.json` に出所を登録すること。
