# 引継ぎ資料(最新)

- 作成者: AGENT #052(STEP 5b 着手前の事前確認)
- 作成日: 2026-07-14
- 履歴コピー: `docs/handover/052_step5b_readiness_check.md`

## プロジェクト概要

パチスロアプリケーション「**義経物語**」を開発するプロジェクト。
**1 AGENT につきユーザーとのやり取りは 1 回のみ**。作業終了時に必ずこのファイルを更新し、`docs/handover/` に履歴コピーを残すこと(詳細ルールは `AGENTS.md` 参照)。

## 最重要: 仕様の正

- **`docs/specs/義経物語_仕様_260709.xlsx` が正式仕様(マスター)**。
- ただし **2026-07-10 のユーザー回答 16 件(`docs/SPEC.md`「13. 確定事項」)は Excel より優先**(Excel 誤植の訂正を含む)。主な訂正:
  - AT 終了後モード振分けの残り 0.0001 は地獄へ加算(地獄 0.3156)。
  - 本前兆移行時の背景テーブルの静・弁慶行は自背景 0.25(Excel の 0.5 は誤植)。
  - 偽前兆の突入条件はモード移行シートが正(レア役 1/10。「天国アップ時の 1/5」は不採用)。
- 有効ラインは横 3 + 斜め 2 の 5 ライン。タイミング目押しあり。チェリー・スイカ・**リーチ目 7 揃い**は取りこぼし許容。設定差なし。

## これまでの経緯

1. AGENT #001〜#005: 開発計画策定(TS + React + Vite + Vitest)、Web + exe(Tauri)両対応方針、素材入稿フロー(`incoming/`)、Phase 1 コアロジック、main への統合。
2. AGENT #006: Excel 仕様書の入稿方法を整備(PR #9)。
3. AGENT #007〜#008: 筐体画像取り込み、仮素材 40 件生成、Phase 2 リール制御(旧叩き台)、PR 競合解消。
4. AGENT #009: ユーザー入稿の Excel 本仕様書(全 16 シート)を読み取り・検算し `docs/SPEC.md` へ全転記。
5. AGENT #010〜#011: 回答 16 件を `docs/SPEC.md` へ反映し、`src/core/` を Excel 仕様へ整合。Phase 3 の抽せんテーブル(`mode.ts` / `background.ts` / `omen.ts` / `at.ts`)をデータ駆動で実装(PR #12 マージ済み)。
6. AGENT #012: ユーザーが GitHub Desktop で main へ直接プッシュした `incoming/` の実素材を取り込み:
   - **背景動画 7 本**(義経/静/弁慶/夕方/前兆/AT/上位AT)を WebM(VP9)720p・音声なしへ再圧縮し `src/assets/video/stage/` へ(各 2.2〜4.8 MB)。前兆背景の元素材は 4:3(1440x1080)のため中央クロップで 16:9 化。
   - **図柄画像 8 種**(赤7/黒バー/白バー/ベル/スイカ/チェリー/リプレイ/ブランク)を透過余白トリム + 400x200 コンテインフィットで透過 WebP へ変換し `src/assets/images/reels/` へ。
   - 変換スクリプトは `scripts/import_incoming_assets.py`(再実行可能)。
   - **ステージ(背景)構成を旧 12 種 → SPEC「5.」の背景 9 種へ変更**(`STAGE_IDS`: 義経/静/弁慶/夕方/前兆/AT小役/ATバトル/上位AT小役/上位ATバトル)。AT・上位 AT の小役/バトルは**専用素材が未入稿のため動画を共用**。
   - BGM 仮素材を新 9 ステージ名で再生成(`scripts/gen_placeholder_assets.py` を新構成へ更新。図柄・背景動画は入稿済みのため同スクリプトの生成対象から除外)。
   - 旧仮素材(ステージ動画 12 本・BGM 12 本・図柄 7 種)と `incoming/` の元ファイルを削除し、`manifest.json` を全面更新(入稿素材は `origin: "user-provided"`)。
   - テスト 81 件全パス、lint / build 成功。ブラウザで全 9 ステージの動画再生・図柄表示を確認済み。
7. AGENT #013: PR #14 のマージコンフリクトの原因調査と解消:
   - **原因**: 同じ素材取り込みタスクを 2 つのエージェントが並行実施し、PR #13(`cursor/import-incoming-assets-a95a`)と PR #14(`cursor/asset-intake-and-roadmap-9d85`)が同一ファイル群(図柄 WebP・`index.ts`・`manifest.json`・`App.tsx`・`HANDOVER.md` 等)を別内容で変更。PR #13 が先に main へマージされたため、PR #14 が CONFLICTING になった。
   - **解消**: PR #14 ブランチへ main をマージし、重複ファイルは全て main(= PR #13)側を採用。PR #14 固有の成果物 `docs/ROADMAP.md`(完成までの STEP 1〜6 実行プラン)のみ取り込み、main の現状(図柄改名は未実施)に合わせて修正。PR #14 の `reel.ts` 図柄 8 種化(改名のみ)は採用せず STEP 1 でまとめて実施する方針。
8. AGENT #014: STEP 1 を依頼した 2 つのラン(「Step1 リール制御本対応」「Step1 再発防止」)が ERROR 終了した原因を、Cloud Agent 診断 API のトランスクリプト分析で調査(詳細は `docs/handover/014_step1_error_reinvestigation.md`。PR #16 マージ済み):
   - **原因**: リポジトリ・環境・プロンプトの誤りではない。STEP 1 の巨大スコープを 1 応答で計画しようとしてモデルの思考生成が 1 回 5〜6 万字・12〜26 分に肥大化し、最終的に応答生成が繰り返し失敗して Cursor Cloud 側でランが打ち切られた(「unexpected error repeatedly」)。
   - **両ランともコード変更・コミット・ブランチ作成ゼロ**で、失われた成果物はなし。再試行は安全。
   - **再発防止策**: STEP 1 をサブタスクへ分割して 1 ラン 1 サブタスクで依頼する。依頼文に「事前調査は最小限にし、早い段階で小さくコミット・プッシュ」と明記。再発するならモデル変更も有効。
9. AGENT #015: 上記の再発防止策を受け、**STEP 1 を 1a〜1f の 6 サブステップへ分割する計画を策定し `docs/ROADMAP.md` を更新**(PR #17。実装は未着手):
   - **1a**: 図柄 8 種化 + Excel 20 コマ配列差し替え + `SYMBOL_IMAGES` 正式マッピング + 配列検算テスト(旧停止制御網羅テストは一時 skip)
   - **1b**: 5 ライン(横 3 + 斜め 2)の表示判定 `judgeDisplay` 書き直し + 判定単体テスト
   - **1c**: 停止制御 I = ハズレ / リプレイ(5 ライン 100% 引き込み)/ 押し順ベル(左=上段 1 枚・正解=斜め 13 枚)+ 網羅テスト
   - **1d**: 停止制御 II = 角・中段チェリー / 弱スイカ(斜め優先)・強スイカ(平行優先)/ チャンス目(テンパイはずし)+ 網羅テスト
   - **1e**: 停止制御 III = リーチ目 7 揃い(目押し外しは取りこぼし)+ DDT 黒バー挙動 + 総合網羅テスト完成(skip 残ゼロ)
   - **1f**: UI 追随(`App.tsx` の 1G 消化デモ)+ ブラウザ実機確認
   - 分割の根拠: 1a は型変更で `src/assets/index.ts` / `App.tsx` の修正が必然的に連動するため旧分割案の「1d: 画像割り当て解消」を 1a へ統合。旧 1c(停止制御)が最大の作業のため役グループ別に 3 分割。各サブステップは「テスト・lint・ビルドがグリーン」で終わる設計(詳細は `docs/ROADMAP.md` STEP 1 参照)。
10. AGENT #016: PR #17 のマージコンフリクトの原因調査と解消:
   - **原因**: PR #17(AGENT #015 の STEP 1 分割計画)は PR #16 マージ前の main(b37321c)から分岐しており、PR #16(AGENT #014 の ERROR 原因調査)が先に `docs/HANDOVER.md` を更新して main へマージされたため、両 PR が同ファイルの同じ箇所(作成者欄・経緯 8 項目目・次タスク欄)を別内容で変更して CONFLICTING になった。PR #13/#14 と同型の「HANDOVER.md 競合」で、並行して走った 2 ランがどちらも自分を「AGENT #014」と名乗っていたことも一因。
   - **解消**: PR #17 ブランチへ main をマージし、両方の成果を統合(経緯 8 = ERROR 調査、9 = STEP 1 分割計画として並記)。分割計画側のエージェントを #015 へ改番し、履歴を `docs/handover/015_step1_split_plan.md` へ改名。
11. **AGENT #017(今回)**: **STEP 1a = 図柄 8 種化 + Excel 配列差し替え(データ層)を実装**:
   - `ReelSymbol` を 8 種へ変更: `SEVEN_RED` / `BAR_BLACK` / `BAR_WHITE` / `BELL` / `WATERMELON` / `CHERRY` / `REPLAY` / `BLANK`(`SEVEN_WHITE` 廃止、`BAR` を黒/白へ分離、ブランク新設)。
   - `REEL_LAYOUT` を SPEC「3.」の Excel 20 コマ配列へ差し替え。**コマ番号規約 = index 0 がコマ番号 1(index = コマ番号 - 1)**。SPEC の表はコマ 20 → 1 の降順記載で、リールは下方向回転(index p+1 が上段)のため、表の見た目と窓の並びが一致する(`reel.ts` の `REEL_LAYOUT` コメント参照)。
   - `src/assets/index.ts` の `SYMBOL_IMAGES` を正式 8 図柄マッピングへ(SEVEN_WHITE→白バー画像・BAR→黒バー画像の仮割り当てを解消、`SYMBOL_BLANK_URL` を廃止して `BLANK` をマップへ組み込み)。`App.tsx` はコード変更不要(型経由で追随)。
   - 配列検算テストを新設: SPEC 配列表(コマ 20→1)との全コマ一致 / 各リールの図柄個数 / 赤7・黒バー・白バー各 1 個・ブランク 左 2・中 0・右 1 / ベル全リール 100% 引き込み配置 / 左リプレイ最大間隔 6(コマ 12⇔18。窓内 7 コマ範囲なら全押下位置から到達可)/ 中・右スイカと左・中チェリーの取りこぼし発生配置。
   - 旧・中段 1 ライン前提の停止制御網羅テスト(2 describe)は新配列では成立しないため **TODO コメント付き `describe.skip`**(1c〜1e で新テストへ置換し 1e で skip 残ゼロ)。停止制御ロジック(`resolveStop` / `judgeDisplay`)自体は未変更。
   - テスト 77 パス + 10 skip、lint / build グリーン。ブラウザでレバーオン 10G 消化・図柄表示・履歴記録を確認(console エラーなし)。
12. **AGENT #018(今回)**: **STEP 1b = 表示判定の 5 ライン対応(`judgeDisplay` 書き直し)を実装**:
   - 有効ライン 5 本を `reel.ts` に定数定義: `LINES`(`TOP` / `MIDDLE` / `BOTTOM` / `DOWN_RIGHT` / `UP_RIGHT`)。各ラインは [左, 中, 右] の段オフセット(`RowOffset`: +1=上段 / 0=中段 / -1=下段)で表現。補助関数 `lineSymbols`(ライン上の図柄)/ `linesWithSymbol`(図柄が揃っているライン列挙)を新設。
   - `judgeDisplayDetail(positions, wonRole)` を新設し、表示役 + 揃ったライン(`lines`)+ 押し順ベル払出区分(`bellSuccess`)を返す。`judgeDisplay` は従来シグネチャのまま `role` のみ返すラッパーへ。
   - 判定仕様: 5 ライン走査でリプレイ / ベル / スイカ / 赤7(リーチ目)の揃いを判定。スイカの弱・強は内部当選役で区別(非当選時のスイカ揃いは表示役にしない)。チェリーはライン非依存で左リール窓内(中段=中段チェリー / 上下段=角チェリー)。ライン役 > チェリー。
   - **複数ライン同時成立時の優先順位を定義**: 内部当選役の図柄 > リーチ目 > リプレイ > ベル(`LINE_JUDGE_PRIORITY`。払出は 1 役分)。同一役の複数ライン揃いは `lines` に全列挙。
   - **押し順ベルと `payout.ts` の `bellSuccess` の対応整理**: 斜め揃い = 押し順正解 13 枚(true)/ 横揃い(上段等)= 不正解 1 枚(false)。`bellSuccessFromLines(lines)` で判定結果から払出区分を導ける(**実配線は 1c**。`App.tsx` は現状 `calcPayout(displayed, true)` のままで未配線)。
   - 判定単体テストを新配列の実座標で新設(横 3 ライン / 斜め 2 ライン / 7 揃い / スイカ弱・強 / 複数ライン同時揃い([1,12,3] のリプレイ 2 ライン、[16,2,3] の上段リプレイ + 下段ベル)/ チェリー角・中段 / ライン役とチェリーの優先 / 全 20³ 停止位置 × 代表 4 役の `judgeDisplay`・`judgeDisplayDetail` 整合性)。
   - テスト 89 パス + 10 skip(skip は 1a からの停止制御網羅テストのまま)、lint / build グリーン。ブラウザでレバーオン 12G 消化・図柄表示・履歴記録を確認(console エラーなし)。
13. **AGENT #019(今回)**: **STEP 1c = 停止制御 I(ハズレ / リプレイ / 押し順ベル)の 5 ライン対応 + 網羅テストを実装**:
   - **停止制御エンジンを「探索方式 + 到達保証の再帰評価」で全面書き直し**(方針は `reel.ts` 冒頭コメントに記録)。役別停止テーブル方式は保守性(20³ × 役 × 押し順の手作成が非現実的)から不採用。
   - 仕組み: スベリ 0〜4 の 5 候補を (1) `guaranteedRank`(残りリールがどの押下位置でも禁止出目を回避でき、可能なら当選形を完成できるかの最悪ケース評価。メモ化再帰)→ (2) `expectedRank`(押下位置一様ランダム + 本エンジン方策での期待ランク。テンパイ維持・取りこぼし許容役の引き込み優先を担う)→ (3) スベリ最小、の辞書式で選ぶ。
   - 出目の合法判定は `classifyFinal`: 非当選図柄の 3 つ揃い(全 5 ライン)は常に禁止 / チェリー非当選時の左窓チェリーは 100% 引き込み役(ベル・リプレイ)の完成形との同時のみ許容 / 押し順ベルは目標停止形(左第一=上段、中・右第一=斜め)以外の揃いを禁止。
   - **ハズレ**: 全押下位置 × 全押し順 6 通りで「何も揃わず左窓チェリーも出ない」ことを網羅テストで確認(ROADMAP 記載のリスク早期検証 = 蹴飛ばし全域可能を確認)。
   - **押し順ベル**: 左第一停止=上段揃い 1 枚(`bellSuccess: false`)/ 中・右第一停止=斜め揃い 13 枚(`bellSuccess: true`)を全域で作り分け。`SpinResult` に `lines` / `bellSuccess` を追加し、`App.tsx` の `calcPayout` へ `bellSuccess` を配線(デモは順押し固定のためベルは 1 枚)。
   - **リプレイ・重要発見(要ユーザー確認)**: 左リールを「最後に」止める押し順([中→右→左]・[右→中→左])では、左リプレイ最大間隔 6 コマの制約により**どの停止戦略でも 100% 引き込みが不可能**(先に止まる 2 リールが左の押下位置を知らずに段を確定させる必要があるため)。全停止戦略の因果最適探索で理論最小の取りこぼし = 134([中→右→左])/ 135([右→中→左])/ 8000 押下位置を確認し、エンジンはこの理論最小値に一致(網羅テストで数値固定)。取りこぼし時はクリーンなハズレ目で停止。左第一・中第一→左・右第一→左の 4 押し順は 100% 引き込み。
   - レア役(チェリー / スイカ / チャンス目 / リーチ目)は暫定実装のまま(引き込めれば任意ライン、不可なら取りこぼし。順押しでの引き込み維持は集計で確認済み)。旧網羅テストの skip 10 件は 1d〜1e で置換予定(TODO コメント更新済み)。
   - テスト 94 パス + 10 skip、lint / build グリーン。ブラウザでレバーオン 15G 消化を確認(ベル 1 枚・リプレイ 0 枚・ハズレ、console エラーなし)。
14. **AGENT #020(今回)**: **左リール コマ 14 をブランク → リプレイへ配列変更**(STEP 1d の前に、1c で発見した変則押しのリプレイ取りこぼし問題へ対処するユーザー指示):
   - `REEL_LAYOUT` 左リール index 13(コマ 14)を `BLANK` → `REPLAY` へ変更。左リプレイは 6 個・最大間隔 5 コマとなり、**全押下位置から中段へ 100% 引き込み可能**に。左ブランクは 2 → 1 個。
   - これにより 1c の「配列上の理論限界」(左リールを最後に止める押し順 [中→右→左]・[右→中→左] で取りこぼし理論最小 134〜135 / 8000)が解消。**リプレイは全押し順 6 通り × 全 20³ 押下位置で 100% 引き込み**を網羅テストで検証済み(取りこぼし許容の分岐・期待取りこぼし数テーブルはテストから削除)。
   - ハズレの蹴飛ばし(何も揃わず左窓チェリーなし)・押し順ベルの停止形作り分けも変更後配列の網羅テストで全域グリーン(停止制御エンジンは探索方式のためコード変更不要。配列データの 1 コマ変更のみ)。
   - `docs/SPEC.md` を更新: 配列表のコマ 14 に変更注記、「3.」冒頭の確定事項、配列分析、**確定事項 17 として追記**(Excel 配列からの変更である旨を明記)。`docs/ROADMAP.md` の STEP 1c 実施結果にも解消済みと記録。
   - Excel 仕様書(`docs/specs/義経物語_仕様_260709.xlsx`)自体は未修正のため、**リール配列シートだけは Excel と実装が 1 コマ異なる**(SPEC.md の確定事項 17 が正)。
   - テスト 94 パス + 10 skip、lint / build グリーン。ブラウザでレバーオン 15G 消化を確認(リプレイ・ベル・角チェリー・ハズレの履歴記録、console エラーなし)。
15. **AGENT #021(今回)**: **STEP 1d = 停止制御 II(角・中段チェリー / 弱・強スイカ / チャンス目)+ 網羅テストを実装**:
   - 1c の探索エンジン(`guaranteedRank` / `expectedRank` / `pickBestPosition`)は無変更のまま、出目の合法判定 `classifyFinal` へ各役の目標停止形を追加するだけで実現(引継ぎで示唆されたとおりの素直な拡張)。
   - **角チェリー**: 左窓の上段 or 下段チェリーのみ WIN(中段チェリー形は禁止出目)。**中段チェリー**: 中段のみ WIN(角は禁止出目)。引き込めない押下位置はクリーンなハズレ目 = 取りこぼし。引き込み可否は押下位置で機械的に決まる(角 12/20・中段 10/20)ことを網羅テストの完全オラクルで検証。
   - **弱・強スイカ**: 揃い方向の優先を `RANK_WIN_FALLBACK`(= 2。優先方向でない揃い)で表現。弱=斜め優先 / 強=平行優先で、優先方向が困難なら劣後方向、揃えられなければ取りこぼし。**優先方向にこだわって取りこぼすことはない**(方向優先なしの成功数と完全一致することを一時スクリプトで測定確認)。スイカの複数ライン同時揃いは配列上あり得ない(窓内にスイカ 2 個表示されるリールがない)。
   - **チャンス目**: 停止形を「スイカテンパイはずし目」(スイカがちょうど 2 コマ載ったラインがあり、何も揃わず左窓チェリーなし)として確定。スイカ 3 つ揃いは非当選図柄として常に禁止のため「引き込める位置でも引き込まない」は自動で成立。テンパイを作れない押下位置はクリーンなハズレ目 = 取りこぼし。`watermelonTenpai()` を新設し、`judgeDisplayDetail` が**内部当選チャンス目 + テンパイ時のみ `CHANCE_ME`** を返す(同型出目はハズレ等でも出るため当選役ゲート)→ 払出 3 枚が既存配線で機能。
   - 網羅テスト新設(1c と同粒度 = 全 20³ 押下位置 × 押し順 6 通り): チェリー 2 種の完全オラクル検証 / スイカの合法性 + 最終停止リールの局所最適性(優先方向に揃えられるのに劣後方向へ揃えない・揃えられるのに取りこぼさない)/ チャンス目の局所最適性 / **スイカを揃えられる押下位置ではチャンス目が必ずテンパイはずしになる**(全押し順で violation 0)/ 押し順別成功数の固定値(回帰検出用): スイカ 2200〜2600 / 8000(弱・強同数で方向内訳のみ入替)、チャンス目 6560〜6640 / 8000、チェリー角 4800・中段 4000 / 8000。
   - テスト 107 パス + 10 skip、lint / build グリーン。ブラウザで 5 レア役の揃い(角チェリー 2 枚・中段チェリー 2 枚・強スイカ 3 枚・チャンス目 3 枚)と取りこぼし(出目=ハズレ・0 枚)を実機確認(デモ用に成立役を一時固定して確認後、revert 済み。console エラーなし)。
16. **AGENT #022**: **STEP 1e = 停止制御 III(リーチ目 7 揃い + DDT 黒バー挙動)+ 総合網羅テスト完成(skip 残ゼロ)を実装**:
   - **リーチ目**: 候補評価に「目押し保証ランク(`aimedRank`)」を第 2 キーとして追加(`guaranteedRank` の亜種で、残りリールの押下位置を「赤7 を狙った位置」= 各リール 5 箇所に限った最悪ケース評価。メモ化再帰)。赤7 は各リール 1 個のため `guaranteedRank`(全 20 押下位置の最悪ケース)では候補間の差がつかず(常に取りこぼしが最悪)、`expectedRank` だけでは先に止まるリールが 7 を「後続リールの目押し位置から届かない段」へ置く取りこぼしが残っていた(1d 時点で目押し成功 125 通り中 100〜108 勝)。`aimedRank` 追加で **3 リールとも赤7 狙い(5³ × 押し順 6 通り = 750 通り)で 100% 7 揃い**を網羅テストで検証。狙いを外した押下位置は取りこぼし(クリーンなハズレ目。代替リーチ目停止なし = SPEC 回答 14)。中段引き込み不可でも上下段経由で有効ラインへ引き込める押下位置は揃う(全域成功数は 185〜225 / 8000 で押し順別に固定値テスト化)。
   - **DDT(左リール黒バー狙い)**: 評価キーの最下位(スベリ最小の直前)に「黒バーを窓内のできるだけ下段寄りに表示する」選好(`ddtScore`: 下段 0 / 中段 1 / 上段 2 / 窓外 3。左リールのみ)を追加。黒バーは左 1 個(コマ 19)のため、黒バー狙いの押下位置でだけ候補間で差がつき、他の押下位置では従来どおりスベリ最小(専用モード不要)。左第一停止の黒バー狙いで: ハズレは黒バー下段停止(押下位置 index 15〜18 → 停止位置 19)、リプレイは黒バー窓内のまま揃う、スイカ成立時はスイカが必ず窓内へスベる(左スイカ 100% 引き込み配置)。**チェリー(コマ 13)は黒バーの 5 コマ以上手前のため、黒バーを窓に引き込める押下位置からは物理的に届かず取りこぼし**(チェリー停止で察知できるのは押下位置 index 12〜13 = コマ 13〜14 で押した場合のみ。SPEC「3.」に実装注記を追記)。左リールを後に止める押し順では蹴飛ばし制約が優先され黒バー停止できない押下位置があるため、DDT の強い保証テストは左第一停止に限定。
   - **総合網羅テスト**: 旧・中段 1 ライン前提の skip テスト 10 件(`describe.skip`)を削除し、全役 × 全 20³ 押下位置 × 押し順 6 通りの共通不変条件(スベリ 4 コマ以内 / 非当選図柄をどのラインにも揃えない / 取りこぼし・ハズレはクリーンなハズレ目 / チェリー非当選時の左窓チェリーは 100% 引き込み役の完成形と同時のみ)へ置換。**skip・TODO 残ゼロ達成**。
   - スイカ・チャンス目の押し順別成功数の固定値テストは**変化なし**(`aimedRank` はリーチ目のみ、`ddtScore` は上位キー同値の候補間でのみ働くため引き込み・蹴飛ばしへ影響しない)。
   - テスト 125 パス(skip 0)、lint / build グリーン。ブラウザでリーチ目 7 揃い(中段・払出 3 枚)とハズレ時の DDT 黒バー下段停止を実機確認(デモ用に成立役・押下位置を一時固定して確認後、revert 済み。console エラーなし)。
17. **AGENT #023(今回)**: **STEP 1f = UI 追随 + ブラウザ確認を実施し、STEP 1 の全サブステップが完了**:
   - `App.tsx` の 1G 消化デモを 5 ライン判定結果へ追随。UI に **押し順セレクト(6 通り)**・**目押しモードセレクト**(適当押し(ランダム)/ 赤7 狙い(全リール)/ DDT(左リール黒バー狙い)。狙い図柄の 0〜4 コマ手前から押下位置を選ぶ `aimedPush`)・**成立役の強制指定セレクト**(抽せん(通常)+ 全 9 役。停止形確認のデモ用)を追加。過去エージェントが毎回行っていた「デモ用の一時コード固定 → revert」が不要になり、UI 操作だけで全役の停止形を再現できる。
   - `resolveSpin` の判定結果を表示へ配線: ステータスと履歴テーブルに **揃った有効ライン**(上段/中段/下段/右下がり/右上がり。複数同時揃いは併記)と押し順ベルの払出区分(押し順正解 13 枚 / 不正解 1 枚)を表示。**揃ったライン上の図柄はリール窓で金枠ハイライト**(`highlightRows`。チェリーは左リール窓内のチェリー位置を強調)。
   - コアロジック(`src/core/`)は無変更。変更は `App.tsx` + `App.css` のみ。
   - ブラウザ実機確認(スクリーンショット・録画あり): ベル上段揃い 1 枚(左第一)/ ベル斜め揃い 13 枚(中第一)/ リプレイ斜め揃い / 角チェリー(上段)2 枚と取りこぼし / 中段チェリー 2 枚 / リーチ目 7 揃い(赤7 狙い・中段・3 枚)/ DDT 黒バー下段停止(ハズレ)。console エラーなし。
   - テスト 125 パス(skip 0)、lint / build グリーン。
18. **AGENT #024**: **STEP 2 の細分化プラン策定 + STEP 1 完了時点の動作確認**(コード変更なし。ドキュメントのみ):
   - **STEP 2 を 2a〜2f の 6 サブステップへ分割し `docs/ROADMAP.md` を更新**(STEP 1 と同じ「1 サブステップ = 1 AGENT = 1 PR」粒度):
     - **2a**: `GameState` 型の全体設計(全フェーズを表現できる判別可能ユニオン。以後骨格を変えない)+ `initGameState` + 通常時のモード移行骨格 + ゲーム数/差枚集計
     - **2b**: 前兆管理(偽前兆突入・前兆 G 数 7〜10 + 連続演出 4G・偽→本の書き換え・解決。AT 開始は 2d までスタブ)
     - **2c**: 背景移行の統合(30G カウンタ + 4 契機の発火タイミング + 優先順位。(2)(4) は 2b の予約フラグから発火)
     - **2d**: AT 管理(継続率・V ストック・セット遷移・10 連 → 上位 AT・エンディング・AT 終了処理 + モード/背景再抽せん)
     - **2e**: 通しフロー統合 + 大量試行シミュレーション検算(初当り・純増 ±6.4/−1.8・コイン持ち 27G・機械割。想定値未記載の項目は計測してユーザーへ確認)
     - **2f**: UI 最小連動(ステートマシン駆動 + 背景でステージ自動切替 + デバッグパネル)+ ブラウザ確認
     - 全体設計方針も ROADMAP に明記: 純関数 `advanceGame(state, input, rng) → { state, events }` / リール制御とは疎結合(打ち方ポリシーはラッパー側)/ 1G 内の処理順序を 2a で固定 / 「次ゲーム」契機は予約フラグで持ち越し
   - **STEP 1 完了時点の動作確認手順書 `docs/STEP1_VERIFICATION.md` を新規作成**(ユーザー向け)。自動チェック(`npm test` 125 件 / lint / build)+ `npm run dev` でのブラウザ確認チェックリスト 14 項目(押し順ベル正解/不正解・リプレイ 100% 引き込み・ハズレ・レア役の揃いと取りこぼし・リーチ目 7 揃いと目押し外し・DDT・通常抽せん流し)+ STEP 2 以降のため未実装が正しい項目の一覧。
   - **手順書どおりに実機確認を実施**(computerUse で 47G 消化・録画あり): チェックリスト 10 項目すべて期待どおり(ベル上段 1 枚 × 3 / 斜め 13 枚 × 3 / リプレイ 5 連続引き込み / ハズレ 5G クリーン / 角チェリー揃い 2 枚と取りこぼしの両方 / 強スイカ下段 3 枚と取りこぼし / リーチ目 赤7 狙い 3 連続 7 揃い / 適当押しでほぼ取りこぼし / DDT 黒バー停止 / 通常抽せん 10G)。console エラーなし。
   - テスト 125 パス(skip 0)、lint グリーン(コード変更なしのため現状確認)。
19. **AGENT #025**: **STEP 2a 着手前の事前確認**(ユーザーからの「STEP 2a の前に確認・決めておくべきことはあるか?特に前兆まわりで、後で演出シナリオを組むために今決めるべきことは?」への回答。**コード変更なし・ドキュメントのみ**。PR #26):
   - SPEC と実装(`mode.ts` / `background.ts` / `omen.ts` / `at.ts`)を精査し、STEP 2a/2b で仕様が未定義な点を洗い出して**質問リスト Q1〜Q11(各項に推奨案付き)を作成**(本ファイル「STEP 2a 着手前の確認事項」参照)。中心は前兆タイムライン(前兆 G 数カウントの起点 / 偽→本書き換えの詳細 4 点 / 連続演出中の抽せん)と 30G カウンタの境界条件(自背景維持時のリセット等)。
   - **演出は「前兆当せん時にシナリオ一括決定(シナリオ方式)」を推奨**(Q11)。前兆管理サブステップではステートマシンの毎 G 出力に演出層が必要とする情報(モード / 背景 / 前兆種別・経過 G・総 G / 連続演出種別・何 G 目・成否 / 成立役)を全部含める設計とし、シナリオテーブル自体は演出リスト確定後の STEP 4 で差し込む。
20. **AGENT #026(今回)**: **PR #26 のマージコンフリクトの原因調査と解消**:
   - **原因**: AGENT #024(PR #25 = STEP 2 の 2a〜2f 分割 + STEP 1 動作確認)と AGENT #025(PR #26 = 質問リスト Q1〜Q11 + STEP 2 の 2a/2b 分割)が並行して走り、両方が `docs/HANDOVER.md` と `docs/ROADMAP.md` の同じ箇所(作成者欄・経緯 18・STEP 2 分割案・次タスク欄)を別内容で変更。PR #25 が先に main へマージされたため PR #26 が CONFLICTING になった(PR #13/#14・#16/#17 と同型の「並行ラン + HANDOVER/ROADMAP 競合」。両ランがどちらも自分を #024 と名乗っていた)。
   - **解消**: PR #26 ブランチへ main をマージして統合。**STEP 2 の分割は main 側の 2a〜2f(6 分割)を採用**(PR #26 側の 2a/2b 大分割は 6 分割に包含されるため破棄)し、PR #26 固有の成果 = **質問リスト Q1〜Q11** を全文維持して 2a〜2f プランと接続(ROADMAP の「分割の方針」に質問リスト参照を追記、Q11 の「2a では」の記述は 6 分割での該当サブステップ 2b へ読み替え)。質問リスト側のエージェントを #025 へ改番し、履歴を `docs/handover/025_step2a_preflight_questions.md` へ改名。
21. **AGENT #027**: **質問リスト Q1〜Q11 のユーザー回答反映 + STEP 2a = 状態モデル + 通常時モード移行(骨格)を実装**:
   - **ユーザー回答は全 11 問とも推奨案どおり(一部補足あり)で確定**。`docs/SPEC.md`「13. 確定事項」へ **18〜28** として追記(Q1=18 前兆 1G 目は当せんの次 G / Q2=19 通しタイムライン / Q3=20 リーチ目も同一フロー / Q4=21 書き換えは引継ぎ・引継ぎ・有効・契機 4 なし / Q5=22 前兆中の新規当せんは無視 / Q6=23 連続演出中もモード移行抽せん実施 / Q7=24 自背景維持でもカウンタリセット / Q8=25 契機 2/3/4 > 30G + 前兆中は 30G 停止 / Q9=26 設計値なし・シミュレーション結果をユーザーが判断(まずは動作すれば OK)・打ち方 = 通常時 左第一適当押し + AT 中 ナビ遵守 / Q10=27 小役 10G → バトル 8G・リプレイもレア役も 1G 消化 / Q11=28 シナリオ方式)。SPEC「14. 未確定事項」へ演出シナリオテーブルの中身(STEP 4 まで)と機械割の妥当性確認(2e 後)を追記。
   - **STEP 2a を実装**: `src/core/state.ts` を新設。
     - `Phase` = 判別可能ユニオン(`NORMAL` / `OMEN`(kind・game・totalGames・renzoku)/ `RENZOKU`(kind・renzoku・game)/ `AT`(tier・part・partGame・renchan・continueRate・vStock・continueConfirmed)/ `ENDING`)。**偽前兆は「偽前兆 + モード」(確定 9)のためモードとフェーズは直交して保持**(本前兆 = モード `HONZENCHO` + フェーズ `OMEN`(REAL))。
     - `GameState` = mode / background / backgroundGames(30G 契機用。移行時リセットの配線は 2c)/ phase / **pendingBackgroundTrigger(「次ゲーム」契機の予約フラグ。2b が設定・2c が発火)** / totalGames / netCoins / replayCarry。
     - `advanceGame(state, input, rng) → AdvanceResult`(純関数): **1G 内の処理順序をヘッダーコメントで固定**((1) 払出 → (2) モード移行抽せん → (3) 偽前兆突入[2b] → (4) 前兆/連続演出/AT 進行[2b・2d] → (5) 背景移行[2c] → (6) 集計)。2a では (1)(2)(6) + 本前兆移行の検出イベントを実装し、(3)(4)(5) は挿入スロットとしてコメントで明示。
     - 入力 `GameInput` = 内部当選役 + 表示役 + bellSuccess(**リール制御と疎結合**。打ち方ポリシーとの結合ラッパーは 2e)。出力は `state` + `events`(2a は `MODE_CHANGE` / `HONZENCHO_ENTER`。2b〜2d で追加予定の種別をコメントで予告)+ 成立役・表示役・払出のエコー(演出層向け情報を毎 G 全部含める = 確定 28)。
     - モード移行抽せんの停止条件: 本前兆滞在(確定 9・`drawModeTransition` 内)+ **AT 中・エンディング中(確定 11・`modeLotteryActive`)**。偽前兆(OMEN FAKE)・連続演出(RENZOKU FAKE)中は実施(確定 9・23)。
     - `initGameState(rng)` = モード初期抽せん + 背景初期抽せん。初期モードが本前兆(0.0132)でもフェーズは `NORMAL` で開始(前兆スケジュール開始 = フェーズ遷移は 2b)。
   - 単体テスト `state.test.ts` 14 件: 初期状態 / モード・背景の初期分布(大量試行・4σ)/ ハズレ維持 / 本前兆・AT・エンディング中の停止 / 偽前兆・連続演出中は実施 / `HONZENCHO_ENTER` イベント / 移行分布収束 / 集計(総 G・差枚・リプレイ持越し・取りこぼし時も内部当選役で抽せん)/ 純関数性。
   - テスト 139 パス(+14)、lint / build グリーン。UI 連動は 2f のため実施なし(コアロジックのみ)。
22. **AGENT #028**: **STEP 2b = 前兆管理(偽前兆突入・前兆 G 数 + 連続演出 4G・偽→本書き換え・解決)を実装**(2a の骨格 = 型・処理順序・予約フラグは無変更。`advanceGame` の挿入スロット (3)(4) へ実装を追加):
   - **偽前兆突入(スロット 3)**: フェーズ NORMAL のときのみ `drawFakeOmen`(弱スイカ・角チェリー・チャンス目 1/10 / 強スイカ・中段チェリーは本前兆非当選時 100% = 確定 3)。当せんで前兆スケジュール抽せん(`drawOmenGames` → `drawRenzoku` の順に乱数消費)+ `FAKE_OMEN_NEXT` 予約 + `FAKE_OMEN_ENTER` イベント。**前兆中(OMEN・RENZOKU)の新規当せんは「無視」= 抽せん自体を行わない**(確定 22。挙動として等価)。本前兆滞在(モード HONZENCHO)・AT・エンディング中も対象外。
   - **本前兆スケジュール(スロット 3)**: モード移行で本前兆へ移行したゲームにスケジュール抽せん + `HONZENCHO_NEXT` 予約。リーチ目も同一フロー(確定 20)。
   - **タイムライン規約(確定 18・19)**: 当せんゲームは `OMEN { game: 0 }` で返し、**次ゲームのスロット 4 で game 1 = 前兆 1G 目**(同一ゲーム内でスケジュール開始と進行の両方はしない)。前兆 `totalGames` 消化後の次ゲームが連続演出 1G 目(`RENZOKU_START`)。連続演出 4G 目に成否告知(`RENZOKU_RESULT`)し、同ゲームの返り state で本 = AT スタブ / 偽 = NORMAL へ戻し `FAKE_OMEN_FAIL` を予約(次 G に背景移行 = 契機 3。発火は 2c)。
   - **偽→本書き換え(確定 21)**: 偽前兆・連続演出中のモード移行(継続実施 = 確定 9・23)で本前兆へ移行したら kind のみ FAKE → REAL(G 数・経過 G・連続演出種別は引継ぎ・再抽せんなし = a/b。演出中も有効で最終 G の書き換えは同ゲームの解決が成功になる = c。契機 4 は予約しない = d)。`OMEN_REWRITE` イベント発行。書き換えゲーム自体も前兆 1G 分として進行する(実装解釈)。
   - **AT スタブ**: 連続演出成功で `AtPhase`(tier NORMAL・part KOYAKU・partGame 0 = 次 G から AT 1G 目・renchan 1・vStock 0)へ。**continueRate は 0 のプレースホルダ**(`drawContinueRate` での抽せん・セット進行は 2d)。AT 中はモード移行・偽前兆抽せんとも停止済み(確定 11)。
   - **`initGameState` の変更**: 初期モードが本前兆(0.0132)の場合はその場で本前兆スケジュールを抽せんし、**最初のゲームが前兆 1G 目**になる(実装解釈: 「当せん」= 初期抽せん。背景は HONZENCHO 用初期テーブルで抽せん済みのため契機 4 の予約はしない)。
   - `GameEvent` へ 4 種追加: `FAKE_OMEN_ENTER`(totalGames・renzoku 付き)/ `OMEN_REWRITE` / `RENZOKU_START` / `RENZOKU_RESULT`(success 付き)。
   - 単体テスト +17 件(state 31 件): スクリプト乱数 `seqRng`(値列を使い切ると throw = **「抽せんが行われない・再抽せんしない」ことを乱数消費数で検証**)で突入・スケジュール・通しタイムライン(本 = 成功 → AT / 偽 = 失敗 → NORMAL + 予約)・書き換え(前兆中・演出中・最終 G)・無視(確定 22)・本前兆スタートの初期化を固定 + 大量試行の分布検証(突入率 1/10・前兆 G 数・連続演出振分け・4σ)。
   - テスト 156 パス(+17)、lint / build グリーン。UI 連動は 2f のため実施なし(コアロジックのみ)。
23. **AGENT #029**: **STEP 2c = 背景移行の統合(30G カウンタ + 4 契機の発火タイミング + 優先順位)を実装**(2a の骨格 = 型・処理順序は無変更。`advanceGame` の挿入スロット (5) へ実装を追加):
   - **予約契機の発火(契機 2/3/4)**: スロット 5 で「ゲーム開始時点の予約」= `state.pendingBackgroundTrigger` を発火・消化する。スロット 3・4 がこのゲームで設定した新予約(ローカル変数を `reservedTrigger` へ分離)は次ゲームで発火(例: 契機 3 の発火ゲームで新規偽前兆に当せんした場合、このゲームで契機 3 を発火しつつ契機 2 を次ゲームへ予約 = テストで固定)。
   - **契機 1(30G 経過 = ELAPSED)**: 「このゲームが通常時(ゲーム開始・終了ともフェーズ NORMAL)」のときのみ有効。前兆中(偽・本・連続演出。**当せんゲーム・解決ゲームを含む**)は停止(確定 25)、AT・エンディング中は背景移行自体が停止(確定 11)。判定は `backgroundGames + 1 >= BACKGROUND_ELAPSED_GAMES`(= 同一背景 30G 目のゲームで抽せん)。停止期間中にカウンタが 30 を超えて持ち越された場合は次の通常ゲームで発火(`>=` 判定)。
   - **優先順位(確定 25)**: 予約契機(2/3/4)> 契機 1。同一ゲームで重なったら予約契機のみ 1 回抽せん(乱数消費数のテストで固定)。予約契機同士の衝突は構造上起こらない(予約スロット 1 つ・同一ゲームに 2 つ設定する分岐なし)。
   - **カウンタリセット(確定 24)**: 背景移行抽せんを実施したら結果が自背景(維持)でも `backgroundGames` を 0 へリセット。それ以外は毎 G インクリメント。**2d への注意: AT 終了時の背景再抽せんでもリセットすること**(`GameState.backgroundGames` の doc コメントに記載)。
   - **`BACKGROUND_CHANGE` イベント新設**(trigger・from・to 付き): 背景が実際に変わったときのみ発行(維持時はイベントなし・リセットのみ)。
   - **稀ケースの実装解釈(SPEC「5.」実装注記に記録)**: 偽→本書き換えと契機 2 の発火が同一ゲームで重なった場合(偽前兆当せんの次ゲームで本前兆へモード移行)、抽せんに使う滞在モードは当ゲームのモード移行後の値で、本前兆モードに契機 2 のテーブルが存在しないため**背景維持(乱数消費なし)+ カウンタのみリセット**(確定 21(d) の「書き換えで背景を動かさない」と整合)。
   - 背景移行の実装規約は `state.ts` ヘッダーコメント「背景移行の実装規約」へ記録。2b の既存テスト 2 件(通しタイムライン)は前兆 1G 目に契機 2/4 の乱数 1 消費が加わるため更新。
   - 単体テスト +15 件(state 46 件): 契機 2/3/4 の発火と `BACKGROUND_CHANGE` / 自背景維持のリセット / 30G 目の発火と 29G 目の不発火 / 持ち越し発火 / 優先順位(契機 3 + 30G 同時)/ 偽前兆当せんゲーム・前兆中・解決ゲームの契機 1 停止 / 契機 3 発火 + 新規当せんの同時 / AT・エンディング停止 / 書き換え + 契機 2 の稀ケース / モード × 契機のテーブル選択の分布検証(大量試行・4σ)。
   - テスト 171 パス(+15)、lint / build グリーン。UI 連動は 2f のため実施なし(コアロジックのみ)。
24. **AGENT #030**: **STEP 2d = AT 管理(継続率・V ストック・セット遷移・10 連 → 上位 AT・エンディング・AT 終了処理)を実装**(2a の骨格 = 型・処理順序は無変更。`advanceGame` のスロット 4 の AT / エンディング部分へ実装を追加。実装規約は `state.ts` ヘッダー「AT 管理の実装規約」に記録):
   - **AT 突入(2b のスタブ解消)**: 連続演出成功ゲームで `drawContinueRate`(66/79/84/88%)を抽せんし `AT_START` イベント発行(`enterAt`。partGame 0 = 次ゲームが小役パート 1G 目・初回セット = 1 連目 = 確定 11)。
   - **セット進行(確定 27)**: 小役 10G → バトル 8G。リプレイ・レア役も 1G 消化。パート切替・セット継続は「次ゲームが次パート 1G 目」(小役 10G 目の次がバトル 1G 目、バトル 8G 目(継続)の次が次セット小役 1G 目)。
   - **毎ゲームの抽せん(確定 11)**: 小役パート = 成立役で V ストック抽せん(`drawVStock`。複数可・獲得で `V_STOCK_GAIN`)。バトルパート = 継続未確定なら成立役で継続抽せん(`drawBattleContinue`)/ 確定済みなら V ストック抽せん。ハズレは乱数消費なし。
   - **V ストックの消費規則(実装決定)**: バトル開始時(バトル 1G 目の冒頭)にまず継続率で継続抽せん(`drawSetContinue`)→ 当せんなら継続確定(ストック温存)→ 漏れたらストック 1 個消費で継続確定 → どちらも漏れたら未確定のままバトル中の小役継続抽せんに委ねる(8G 目の小役復活も同ゲームでセット継続になる)。
   - **バトル 8G 目の解決**: 継続確定 → 連チャン +1 で次セット(`AT_SET_CONTINUE`)/ 未確定 → 敗北 = AT 終了(上位 AT でも即終了 = 確定 12)。
   - **10 連(確定 11・12 の実装解釈)**: renchan 10(`RENCHAN_LIMIT`)のセットは「必ず移行」= バトル開始の継続抽せん・ストック消費なしの継続確定扱いで、8G 目の解決時に 通常 AT → 上位 AT(`UPPER_AT_ENTER`。連チャン 1 へリセット・継続率 0.93 固定・**V ストックは持越し**)/ 上位 AT → エンディング(`ENDING_START`)。つまり 10 連目のセット自体は現階層で遊技し、その終了時に必ず移行する(9 連目までのバトル敗北で 10 連に到達できないことはある)。
   - **エンディング**: `ENDING { game: 0 }` → 次ゲームが 1G 目。`ENDING_GAMES` = **暫定 1G**(ムービー尺・演出は STEP 4 で確定するまでの仮置き。定数変更で調整可)。エンディング中は各種抽せん停止。
   - **AT 終了処理(敗北・エンディング共通。`finishAt`)**: 同ゲーム内で `drawInitialMode('AT_END')` → `drawInitialBackground` を再抽せんして通常へ。`backgroundGames` を 0 へリセット(2c の注意事項に対応)。本前兆を引いたら `initGameState` と同様その場で前兆スケジュール開始(次ゲームが前兆 1G 目・契機 4 予約なし)。`AT_END` イベント(reason: DEFEAT / ENDING + 再抽せん後のモード・背景)を発行し、`BACKGROUND_CHANGE` は発行しない(移行契機 4 種の抽せんではないため)。
   - **押し順ナビ**: `isNaviActive(state)`(= フェーズ AT)で導出(2e の打ち方ポリシー用。状態には持たない)。
   - 単体テスト +21 件(state 67 件): 小役進行・1G 消化 / ストック複数獲得 / バトル開始 3 分岐(率当せん・ストック消費・未確定)/ バトル中の継続・V ストック抽せん / セット継続・8G 目復活・敗北 / 10 連の継続抽せんなし・上位 AT 移行(ストック持越し)/ 上位の 0.93 参照・即終了・エンディング → AT 終了 / 終了処理の本前兆リドロー / `isNaviActive` / 大量試行(継続率振分け・**AT 中純増 ≒ +6.4 枚/G**(6.0〜6.9 の範囲で検証。ナビ遵守・取りこぼしなしの粗い検算 = 正確なシミュレーションは 2e))。既存テスト 5 件を 2d の挙動(継続率抽せんの乱数消費・AT_START イベント・エンディング 1G 化)へ更新。
   - テスト 192 パス(+21)、lint / build グリーン。UI 連動は 2f のため実施なし(コアロジックのみ)。
25. **AGENT #031(今回)**: **STEP 2e = 通しフロー統合 + 大量試行シミュレーション検算を実装**(コアは 2a〜2d で完成済みのため `src/core/` の既存ファイルは無変更。結合レイヤーの新設のみ):
   - **`src/core/play.ts` 新設 = ヘッドレス 1G 実行**: `playGame(state, rng, forcedRole?)` = 役抽せん(`drawRole`)→ 打ち方ポリシー(`decidePush`)→ リール制御(`resolveSpin`)→ ステートマシン(`advanceGame`)。UI(2f)とシミュレーションで共用。乱数消費順序 = 役 1 → 押下位置 3(左・中・右)→ `advanceGame` 内部(固定シードで完全再現)。
   - **打ち方ポリシー(確定 26)**: 通常時 = 左第一・適当押し(押下位置は全リール一様ランダム)/ AT 中(`isNaviActive`)= 押し順ベルのみナビ遵守で中第一(`NAVI_PUSH_ORDER` = PUSH_ORDERS[2]。正解 4 通りのどれでも 13 枚のため代表で固定)。**ベル以外はナビなし = AT 中もレア役は適当押しで取りこぼしあり**(引継ぎ時の注意どおり)。
   - **`src/core/simulate.ts` 新設 = 大量試行統計**: `simulate(games, seed)` が区間別(通常 = NORMAL/OMEN/RENZOKU、AT = AT/ENDING。ゲーム開始時点のフェーズで分類)の投入・払出・純増、機械割、初当り(AT_START 数)、セット数(AT_START + AT_SET_CONTINUE + UPPER_AT_ENTER)、上位 AT・エンディング回数、役別当選・表示回数を収集。`npm run sim -- <ゲーム数> <シード>` で実行(`scripts/run_simulation.ts`。vite-node 使用。100 万 G 約 13 秒)。
   - **通しフロー統合テスト(`flow.test.ts` 7 件)**: シード 16 の実プレイ 600G で「通常 → 前兆(全 G 消化)→ 連続演出 4G → AT 突入 → セット継続 9 回 → 10 連 → 上位 AT(0.93)→ 9 回継続 → 10 連 → エンディング → AT 終了(再抽せん)→ 通常」をイベント列 + 毎 G フェーズで検証。大域不変条件(AT_START = 連続演出成功数 / 差枚集計 = 毎 G 払出合計)も検証。**乱数消費順序を変える変更をしたらシード・G 数の取り直しが必要**(テスト冒頭コメントに記載)。
   - **固定シード回帰統計テスト(`simulate.test.ts` 8 件)**: シード 20260712 × 10 万 G の固定値(投入・払出・初当り 271 回・セット 2222・上位 80・エンディング 71 等)+ SPEC 想定値の範囲検証(通常 −1.8 / AT +6.4 / コイン持ち 27G)+ 役抽せん 4σ 検算 + 純関数性。`npm test` 全体は約 60 秒のまま。
   - **計測結果(100 万 G × 3 シード)は `docs/SIMULATION_REPORT.md` に記録**: 通常時純増 **−1.86 枚/G**(想定 −1.8)・AT 中純増 **+6.37 枚/G**(想定 +6.4)・コイン持ち **26.9G/50 枚**(想定 27G)は想定と一致。**機械割 約 152〜157% / 初当り 1/213〜222 / 平均連チャン 約 8 セット / 上位 AT 移行率 約 30% / エンディング到達率 約 26%(設計想定値なし)はユーザーへ妥当性確認を依頼中**(確定 26 のフォローアップ。レポート末尾に確認依頼 3 点を明記)。レア役の表示率(取りこぼしの補数)は停止制御網羅テストの理論値と一致(角チェリー 61% ≒ 12/20 等)し、結合の正しさの傍証。
   - テスト 216 パス(+24)、lint / build グリーン。UI 連動は 2f で実施。
26. **AGENT #032**: **STEP 2d の実装解釈 4 点へのユーザー回答(2026-07-12)を反映**(SPEC 確定事項 **29〜31** として追記。`src/core/state.ts` の AT 管理のみ変更・骨格は無変更):
   - **確定 29 = V ストック先消化**: バトル開始時に V ストックがあれば**先に 1 個消費して継続確定**(継続率抽せんは行わない。旧実装の「継続率抽せん → 漏れたら消費」から変更)。ストックがない場合のみ継続率で継続抽せん。`V_STOCK_USE` イベントを新設(消費後のストック数付き)。上位 AT 移行時のストック持越しは従来どおり(エンディングを経由しても保持)。
   - **確定 30 = 10 連目も敗北あり**: 「10 連目は必ず移行」の特例を廃止し、10 連目のセットもバトル開始・バトル中の継続処理は通常セットと同一。**10 連目のバトル勝利でエンディングへ**(`ENDING_START` に `after` = 行き先を追加)。エンディング消化後、通常 AT → **必ず上位 AT**(`UPPER_AT_ENTER` はエンディング消化ゲームに発行。連チャン 1・0.93 固定・ストック持越し)/ 上位 AT → AT 終了(確定 12 の「10 連でエンディング → AT 終了」に相当。**上位も敗北あり + エンディング経由とする実装解釈**を SPEC 確定 30 の注記に記録)。
   - **確定 31 = エンディングは 10G・押し順ナビあり**: `ENDING_GAMES` 1 → 10。`EndingPhase` に `after`(UPPER_AT / AT_END)と `vStock`(持越し用)を追加。`isNaviActive` が ENDING でも true(`play.ts` の打ち方ポリシーでエンディング中もベルナビ遵守)。エンディング中の各種抽せん停止は従来どおり。
   - **テスト更新**: state.test.ts の AT 系を新仕様へ(V ストック先消化・10 連敗北・エンディング 10G 経由の上位 AT 移行など)。`flow.test.ts` は通しフローが「AT 10 連勝利 → エンディング 10G → 上位 AT → 10 連勝利 → エンディング 10G → AT 終了」に変わったため**シード 62 / 450G へ取り直し**。`simulate.test.ts` の固定値も再取得(初当り 306・セット 1887・上位 50・エンディング突入 80(うち完走 30)等)。`simulate.ts` に `endingCompleteCount`(上位 10 連完走)を追加。
   - **再計測(100 万 G × 3 シード)で出玉が大きく変化**: 機械割 152〜157% → **約 129〜131%** / 平均連チャン 約 8 → **約 5.5 セット** / 上位 AT 移行率 約 30% → **約 13〜15%** / エンディング完走率 約 26% → **約 7〜9%**(V ストック先消化と 10 連敗北ありの影響。通常時純増 −1.86・AT 中純増 +6.4・コイン持ち 26.9G は変化なし)。`docs/SIMULATION_REPORT.md` を全面更新し、**新数値の妥当性確認をユーザーへ依頼中**(初版への回答は「とりあえずこのバランスで問題ない」だったが数値が変わったため)。
   - テスト 219 パス(+3)、lint / build グリーン。UI 連動は 2f のため実施なし(コアロジックのみ)。
27. **AGENT #033(今回)**: **STEP 2f = UI 最小連動 + ブラウザ確認を実施し、STEP 2 の全サブステップが完了**:
   - `App.tsx` をステートマシン駆動へ全面書き換え(**コアロジック `src/core/` は無変更**。変更は `App.tsx` + `App.css` のみ): レバーオン 1 回で `advanceGame` が 1G 進む(`initGameState` で初期化。リセットで再初期化。rng と初期状態は同一 `useMemo` でペア生成 = StrictMode の二重実行でも決定的)。
   - **ステージ自動切替 `stageForState`**: 通常時 = 背景 5 種(`STAGE_FOR_BACKGROUND`)/ AT 中 = 階層 × パート別 4 種 / **エンディング = 暫定で直前 AT 階層のバトルステージを継続表示**(`after` から導出: UPPER_AT = 通常 AT 10 連 = AT バトル、AT_END = 上位 10 連 = 上位バトル。専用演出・素材は STEP 4)。手動ステージセレクトは「自動(状態連動)」+ 9 種手動でデバッグ用に残置。BGM もステージへ自動追随。
   - **デバッグパネル**: モード / 背景(30G カウンタ `backgroundGames` 付き)/ フェーズ 1 行表示(`phaseLabel`: 前兆 種別・経過/総 G・発展先、連続演出 種別・n/4G、AT 階層・パート・n G、エンディング n/10G・行き先)/ 総 G / 差枚 / ナビ有無 + AT 中の詳細行(連チャン・継続率・V ストック・継続確定)+ エンディング詳細行(行き先・持越しストック)。
   - **発生イベントログ**: `GameEvent` 全 14 種を日本語整形(`formatEvent`)して新しい順に直近 14 件表示(確定 28 の演出層向け情報の可視化。STEP 4 のシナリオ演出はこの情報を差し替えて使う)。
   - **押し順セレクトへ「自動」を追加**(デフォルト): 通常 = 左第一(`NORMAL_PUSH_ORDER`)/ ナビ中(AT・エンディング)のベル = 中第一(`NAVI_PUSH_ORDER`)= `play.ts` の打ち方ポリシーと同じ分岐。手動 6 通りも選択可。目押しモード(適当押し / 赤7 狙い / DDT)・成立役強制指定は 1f のまま継続。
   - **オート消化ボタン**(160ms 間隔で連続レバーオン): 30G 背景移行・AT 通し・大量ゲーム消化の確認用。
   - `playGame`(`play.ts`)をそのまま使わず `resolveSpin` + `advanceGame` を直接呼ぶ構成(UI の押し順・目押し・強制役セレクトを活かすため。引継ぎで示唆されていた構成)。`NAVI_PUSH_ORDER` / `NORMAL_PUSH_ORDER` は `play.ts` から import。
   - **ブラウザ実機確認済み**(録画 + スクリーンショット): 中段チェリー強制 → 本前兆 7G → 連続演出 C 4G → 成功 → AT 突入(継続率 79%・AT ステージへ自動切替・ナビ表示)→ オート消化でセット継続 → バトル敗北 → AT 終了 → 通常復帰(ステージ復帰)の通しフロー / ハズレ強制 + オート消化で「背景移行 X→Y(30G経過)」イベントの連続発火とカウンタリセット / console エラーなし。
   - 実機確認の注意: イベントログは直近 14 件のため、AT 中の大量イベントで 30G 移行イベントが流れやすい。30G 契機の分離確認は「成立役 = ハズレ(強制)」で行うのが確実(ヘッドレス検証でも 10 万 G で ELAPSED 契機の背景変更 1463 回を確認済み)。
   - テスト 219 パス(変化なし)、lint / build グリーン。**STEP 2 完了(ROADMAP に完了マーク済み)。次は STEP 3(遊技 UI の本実装)**。
28. **AGENT #034(今回)**: **ユーザー回答 2 点の反映 + STEP 3 の細分化プラン策定**(コード変更なし。ドキュメントのみ):
   - **ユーザー回答の反映(2026-07-12)**: (1) **機械割 約 129〜131% で問題なし** → SPEC 確定事項 **32** として追記し、未確定事項の旧 5.(機械割の妥当性確認)を解決済みへ。`docs/SIMULATION_REPORT.md` の確認依頼セクションも回答済みへ更新。現状の出玉バランス(平均連チャン 約 5.5 / 上位 AT 移行率 約 13〜15% / エンディング完走率 約 7〜9%)で確定(調整指示が出るまで変更しない)。(2) **確定 30 の実装解釈(上位 AT の 10 連目も敗北あり + 勝利でエンディング 10G → AT 終了)は問題なし** → 確定 30 の注記に「ユーザー承認済み」を明記。`state.ts` の修正は不要。
   - **STEP 3 を 3a〜3e の 5 サブステップへ分割し `docs/ROADMAP.md` を更新**(1 サブステップ = 1 AGENT = 1 PR。UI 中心のため全サブステップでブラウザ実機確認 + 録画を必須とする):
     - **3a**: 遊技サイクル UI ステートマシン + 停止ボタン 3 つ(骨格。回転表示は簡易でよい)+ キーボード操作
     - **3b**: リール回転アニメーションの本実装(連続スクロール描画 + スベリの視覚化 + 押下瞬間のコマ取得の正確性)
     - **3c**: 払出・クレジット・獲得枚数・押し順ナビ表示
     - **3d**: 演出表示(前兆・連続演出・AT・エンディングの仮演出。STEP 4 のシナリオテーブルへ差し替え可能な構造)+ SE/BGM 連動強化
     - **3e**: デバッグ UI 整理(折りたたみ)+ 総合ブラウザ確認 + 動作確認手順書 `docs/STEP3_VERIFICATION.md`
   - **全体設計を ROADMAP に明記**: コアロジックは原則無変更(停止ボタンは既存 `resolveStop` を 1 リールずつ呼ぶだけ。インターフェース変更不要)/ UI 層に遊技サイクルの小さなステートマシンを新設(役抽せんはレバーオン時 = ナビ表示のため・`advanceGame` は全停止後 = 表示役が入力のため)/ 押し順は「停止ボタンを押した順」が正 / `App.tsx`(現状 620 行)は `src/ui/` へ分割してよい。
   - **実装デフォルト 5 点(推奨案)も ROADMAP に記載**: 回転速度 約 750ms/周(定数化)/ キーボード = Space + Z・X・C / BET はレバーオンに内包(3 枚掛け固定)/ オート消化維持 / ナビはリール窓上の数字表示(仮)。ユーザーから変更指示がなければこれで進める。
   - テスト 219 パス・lint グリーン(コード変更なしのため現状確認)。
29. **AGENT #035**: **STEP 3a = 遊技サイクル UI ステートマシン + 停止ボタン 3 つ + キーボード操作(骨格)を実装**(コアロジック `src/core/` は無変更):
   - **`src/ui/gameCycle.ts` 新設(React 非依存の純ロジック)**: `startSpin`(レバーオン = 回転開始)/ `pressStop`(停止ボタン押下 = 押下位置で `resolveStop` を 1 リールずつ呼ぶ。停止済みリールへの押下は無視)/ `isAllStopped` / `finishSpin`(全停止後の表示判定。`SpinResult` 互換で `advanceGame` へそのまま配線可)/ `spinningPosition`(回転の時間モデル: 回転開始位置 + 経過時間 → 現在中段のコマ。**下方向回転 = 位置は時間とともに増加**)。回転速度は `SPIN_MS_PER_REV` = 750ms/周(実装デフォルト 1。定数変更で調整可)。
   - **押し順は「停止ボタンを押した順」で確定(対話式停止)**。第一停止の時点では残り 2 リールの停止順が未確定のため、`resolveStop` へ渡す押し順は「押した順 + 未停止リールの左→右昇順」で仮確定(`provisionalPushOrder`)。第二停止以降は全順序が確定。押し順ベルの停止形は第一停止のリールだけで決まる(`bellTargetFor` が pushOrder[0] のみ参照)ため仮確定の影響なし。**残り 2 リールの順序はエンジンの評価に影響し得る**(事前検証で 22/540 ケースの停止位置が変わる)が、**出目の合法性・引き込み保証は全域で保たれる**ことを網羅テストで検証(下記)。
   - **網羅テスト `src/ui/gameCycle.test.ts` 21 件新設**: 対話式停止で全役 × 全押し順 6 通り × 全 20³ 押下位置の共通不変条件(スベリ 4 コマ以内 / 非当選図柄をどのラインにも揃えない / リプレイ・ベル 100% 引き込み / ベルの押し順別停止形(左第一=1 枚・中・右第一=13 枚)/ 取りこぼしはクリーンなハズレ目)+ 取りこぼし数の固定値(回帰用。resolveSpin 網羅テストと同水準)+ 仮押し順が実際と一致する 3 押し順([左→中→右]・[中→左→右]・[右→左→中])では `resolveSpin` と完全一致 + 回転時間モデル・サイクル進行の単体テスト。
   - **`App.tsx` を遊技サイクル駆動へ**: `SpinUi`(IDLE = レバー待ち / SPINNING = 回転中)を新設し、レバーオン(役抽せん + 回転開始)→ 停止ボタン 3 つ(回転中の未停止リールのみ有効。`performance.now()` の経過時間から押下瞬間の中段コマ = 押下位置を取得)→ 全停止で `finishSpin` → `advanceGame` → レバー待ちへ。回転中はコマ送り(37.5ms 間隔の再描画)+ CSS ぼかしの簡易表示(滑らかなスクロールとスベリの視覚化は 3b)。キーボード = Space(レバーオン)・Z・X・C(左・中・右停止。select/input フォーカス中は無効)。回転中のベル当選時はリール窓上にナビ仮表示(本表示は 3c)。履歴テーブルに押し順列を追加。
   - **目押しモード・押し順セレクトはオート消化専用へ縮退**(ラベルを「オート時の…」へ変更。手動時はタイミング押し + 押した順が正)。オート消化はレバー〜全停止を即時実行(160ms 間隔)で維持。強制役・リセット・ステージ手動切替・BGM も維持。
   - テスト 240 パス(+21)、lint / build グリーン。ブラウザ実機確認 + 録画(computerUse): ボタン・キーボード両操作での 1G 消化 / 変則押し(右→中→左)の押し順記録 / 押し順ベルの 1 枚・13 枚作り分け / 停止ボタンの有効・無効切替 / オート消化 200G / console エラーなし。
30. **AGENT #036(今回)**: **STEP 3b = リール回転アニメーションの本実装(連続スクロール描画 + スベリの視覚化)**(コアロジック `src/core/`・3a の `gameCycle.ts` は無変更):
   - **`src/ui/reelAnimation.ts` 新設(React 非依存の純ロジック)**:
     - `continuousPosition(start, elapsedMs)` = 連続位置(小数コマ。mod 20)。**`Math.floor` すると 3a の離散モデル `spinningPosition`(押下位置の判定)と常に一致**することを網羅テストで固定(`reelAnimation.test.ts`。開始 20 通り × 経過 200 コマ × 境界前後のジッタ 4 通り)。描画と押下判定は同一の時計(`performance.now()` − 回転開始時刻)を使うため、**描画で中段を最後に通過したコマ = 押下位置**(目押しの正確性の根拠)。
     - `planSlip(押下瞬間の連続位置, 停止位置)` / `slipPosition` / `isSlipDone` = スベリのアニメーション計画と進行。**スベリ 1〜4 コマ = 同じ回転速度のまま前進を継続**し停止コマが中段中央へ来た瞬間に停止(最大 150ms)/ **スベリ 0(ビタ)= 1 コマ未満だけ逆方向へ戻して中央へ収める**(実機の停止時の戻り挙動に相当。37.5ms 未満)。コマ 20 → 1 をまたぐ循環も検証済み。
     - `reelStrip(reel, 連続位置)` = コマ帯ビュー(窓 3 コマ + 上下はみ出し各 1 = 5 コマ + 下方向オフセット)。offset 0 で中央 3 コマが `windowAt` と一致・コマ境界での表示連続性をテストで固定。
   - **`App.tsx` の描画を置換**: 3a のコマ送り(37.5ms 間隔の `setInterval` + `windowAt` 3 枚描画)を、**`requestAnimationFrame` 毎フレーム再描画 + 5 コマ帯の `translateY` スクロール**(`.reel-strip`。1 コマ = 帯高さの 1/5)へ。リール窓は `overflow: hidden`。回転中・スベリ中は縦モーションブラー風のぼかし(スベリ完了で解除)。停止ボタン押下時は連続位置の floor を押下位置として `pressStop` を呼び、`planSlip` でスベリを見せる。**全リール停止後は最終リールのスベリ完了を待って 1G を締める**(`finishTimerRef`。リセットで破棄)。出目ハイライト(金枠)は帯の中央 3 コマへ従来どおり適用。
   - **実機検証(録画 + スクリーンショット)**: 検証用に一時ドライバ `src/devMezuoshi.ts`(App と同じ window keydown 経路で Space / Z・X・C を計算タイミングで発火 = タイミング目押しの再現)を追加してブラウザで確認後、**削除済み**(コミットに含まれない)。確認内容: 赤7 狙い(2 コマ手前押し)で 7 揃い(中段・右上がり)+ 金枠ハイライト / ビタ押し(0 コマ手前)でも 7 揃い / 4 コマ手前押し = **最大スベリ 4 コマの視覚化**(videoReview で「停止指示後に数コマ流れ続ける様子がはっきり見える」ことを確認)/ 8 コマ手前(わざと外す)= 取りこぼし(出目: ハズレ)/ DDT 黒バー狙い(ハズレ強制)= 左リール下段に黒バー停止 / マウスクリック(computerUse)での通常 1G 消化とぼかし表示 / console エラーなし。
   - テスト 253 パス(+13)、lint / build グリーン。
31. **AGENT #037(今回)**: **STEP 3c = 払出・クレジット・獲得枚数・押し順ナビ表示を実装**(コアロジック `src/core/`・3a/3b の `gameCycle.ts`・`reelAnimation.ts` は無変更):
   - **`src/ui/counters.ts` 新設(React 非依存の純ロジック)**: メーター状態 `MeterState`(credit / payout / autoBet / atGained)と 2 つの更新関数。
     - `meterOnLever(meter, replayCarry)` = レバーオン時: 3 枚掛け固定の BET 徴収(実装デフォルト 3)。リプレイ持越し(`GameState.replayCarry`)なら自動 BET でクレジット不変。**クレジット不足(< 3)はレバーオン時に 50 枚単位で自動補充**(`REFILL_COINS`。実機的な精算・投入は STEP 6)。払出枚数表示は 0 へリセット。
     - `meterOnFinish(meter, wasAtGame, result)` = 全停止(1G の締め)時: 払出をクレジットへ加算 + 払出枚数表示。**AT 獲得枚数 = AT 開始からの純増**で、`AT_START` イベントで 0 リセット(突入ゲーム自体は連続演出最終 G のため加算しない)、`wasAtGame`(= ゲーム開始時点のフェーズが AT / ENDING)のゲームの純増(payout.net)を加算。**上位 AT・エンディングを跨いで継続**し、AT 終了後は最終値のまま凍結(次の `AT_START` で再リセット。メーター表示自体は AT 中のみ)。
   - **`App.tsx` / `App.css`**: 筐体下に 7seg 風メーターパネル(消灯セグメント 8 を薄く敷いて実値を右詰めで重ねる `.seg`)= CREDIT(初期 50)/ BET(回転中 3・REPLAY ランプ = 自動 BET 時に緑点灯)/ WIN(直近払出)+ AT 中のみ黄色の「AT獲得」。reducer に `LEVER` アクションを追加(レバーオン・オート消化の両方で BET 徴収)。
   - **押し順ナビの本表示**: 3a の仮表示 `navi-overlay`(窓上バナー「ナビ: 中→左→右」)を廃止し、**AT・エンディング中のベル当選時、レバーオンで各リール窓の上部に「何番目に押すか」の数字**(`navi-digit`。左 2・中 1・右 3 = 中第一)を表示、**リールが停止するごとにそのリールの数字を消灯**。ナビ無視ペナルティは実装しない(確定 7 = 将来実装)。
   - 単体テスト `counters.test.ts` 9 件(BET 徴収 / 自動 BET / 不足時補充 / 払出加算 / AT 獲得のリセット・加算(ベル +10・ハズレ −3・リプレイ ±0)・通常時凍結 / 通しの収支整合 = クレジット差分と純増合計の一致)。
   - ブラウザ実機確認 + 録画(computerUse): 初期 CREDIT 50 → レバーオンで 47・BET 3 / リプレイ揃い → 次ゲームで REPLAY ランプ緑点灯 + クレジット不変 / 中段チェリー → 本前兆 → AT 突入 → **ナビ数字 2-1-3 表示 → ナビ順(中→左→右)停止で数字が 1 つずつ消灯 → ベル揃い WIN 13・CREDIT +10・AT獲得 +10/G × 2 ゲーム**(録画 `step3c_at_navi_bell_payout_demo.mp4`)/ console エラーなし。
   - テスト 262 パス(+9)、lint / build グリーン。
32. **AGENT #038(今回)**: **STEP 3d = 演出表示(前兆・連続演出・AT・エンディングの仮演出)+ SE/BGM 連動強化を実装**(コアロジック `src/core/`・3a〜3c の `gameCycle.ts` / `reelAnimation.ts` / `counters.ts` は無変更)。**ユーザー指示「BGM や SE は後から差し替えられるように」に対応**:
   - **演出マッピング層 `src/ui/direction.ts` 新設(React 非依存の純ロジック)**。出力は宣言的データのみで、**STEP 4 のシナリオ方式(確定 28)への移行は本モジュールの対応表を「シナリオテーブル → 演出」の解決へ置き換えるだけ**(型と描画側は不変。ファイルヘッダーに差し替え方針を記録):
     - `overlayForState(state)` = フェーズ由来の常時表示 `StateOverlay`: 前兆中(偽・本共通)= 液晶下部の**テロップ**(文言 `TELOP_TEXTS` を経過 G でローテーション。種別を悟らせない)/ 連続演出 = **全画面表示**(種別 A/B/C の仮タイトル・あおり文 `RENZOKU_PRESENTATION` + n/4G)/ エンディング = 上部バナー(n/10G)。通常時・AT 中は undefined。
     - `cutinsForEvents(events)` = イベント由来のワンショット `Cutin`(title / sub / style(WIN・LOSE・SPECIAL・INFO)/ videoUrl(既存仮カットイン動画)/ sound(サウンドキュー ID)/ durationMs): 連続演出の成否告知(勝利! / 敗北…)・AT突入!(継続率付き)・セット継続・Vストック獲得/発動・上位AT突入!・エンディング!・AT終了(敗北 / 完走)。**内部情報イベント(モード移行・偽前兆突入・書き換え・演出開始・背景移行)は演出にしない**。
     - `resultSoundCue(wonRole, payout)` = 1G の締めの基本 SE(レア役 > 払出 > なし)。
   - **描画 `src/ui/DirectionLayer.tsx` 新設**: 液晶(LCD_RECT)へ重ねる表示専用コンポーネント。カットインは seq(ゲーム通し番号)で新規判定してキューへ積み、先頭から durationMs ずつ順次表示 + 表示開始時に SE 再生。**キュー上限 `MAX_CUTIN_QUEUE` = 4**(オート消化 160ms/G では進行が表示より速いため、表示中の先頭を残して古い待機分から間引き = 直近の演出を優先)。リセットは親が `key` を変えて再マウント(キュー破棄)。
   - **SE のサウンドキュー層 `src/ui/sound.ts` 新設(差し替えポイント)**: 用途 ID `SoundCueId`(LEVER_ON / REEL_STOP / PAYOUT / RARE / TELOP / RENZOKU_SUCCESS / RENZOKU_FAIL / AT_CONTINUE / BIG_WIN)→ SE ファイルの対応表 `SOUND_CUES` + `playCue()`。**ゲームコードは SE ファイルを直接参照しない**(App の直接参照を全て置換)。実素材の差し替えは (1) 同名ファイル置き換え(`scripts/import_incoming_assets.py`)or (2) `SOUND_CUES` の張り替え、のどちらかで完結。BGM は従来どおり `STAGE_BGMS`(ステージ ID → ファイル)が差し替えポイント。
   - **仮 SE 2 種を追加生成**: `se_telop`(テロップ表示音)・`se_fail`(失敗告知・下降 2 音)。`scripts/gen_placeholder_assets.py` の SES へ追記して生成し `manifest.json` へ登録。専用素材が無いキューは既存 SE を仮流用(RENZOKU_SUCCESS・BIG_WIN = se_bonus / AT_CONTINUE = se_payout。sound.ts にコメント)。
   - **BGM クロスフェード**: `platform/audio.ts` の `playBgm` がステージ切替時に旧 BGM フェードアウト + 新 BGM フェードイン(`BGM_FADE_MS` = 600ms)、`stopBgm` もフェード停止。
   - 単体テスト `direction.test.ts` 12 件(フェーズ別 overlay / 全イベント → カットインの対応と非対応(内部情報)/ 成否告知の変種 / 基本 SE の優先 / SOUND_CUES の全キュー割当)。
   - **ブラウザ実機確認 + 録画**: リーチ目(強制)→ 本前兆テロップ(毎 G 文言ローテーション)→ 連続演出全画面 1/4G〜4/4G → **勝利! → AT突入!(継続率付き)カットイン** → AT 中の Vストック獲得! カットイン + ベルナビ数字。カットインの表示時間(勝利 2000ms → AT突入 2400ms)と順次表示は Playwright での DOM 監視でも確認(`.cutin-title` の遷移時刻を計測。検証スクリプトは一時的なもので削除済み)。console エラーなし。
   - **注意**: computerUse の画面録画はコマ落ちで実時間より早回しに見える(録画上カットインが一瞬に見えるのはこのため。実挙動は上記 DOM 監視で確認済み)。
   - テスト 274 パス(+12)、lint / build グリーン。
33. **AGENT #039**: **STEP 3e = デバッグ UI 整理 + 総合ブラウザ確認 + 動作確認手順書を実施し、STEP 3 の全サブステップが完了**(コアロジック `src/core/`・UI 純ロジック `src/ui/` は無変更。変更は `App.tsx` + `App.css` + ドキュメントのみ):
   - **デバッグ UI の折りたたみ**: ステータスパネル(モード / 背景 30G カウンタ / フェーズ / 総 G / 差枚 / ナビ + AT・エンディング詳細 + 直近ゲーム結果)・強制役 / オート時の押し順 / 目押しセレクト・ステージ手動切替・リセット・発生イベントログ・ゲーム履歴テーブルを `<details className="debug">`「デバッグ(開発用)」へ集約。**デフォルト閉**(開発用に全機能残置)。
   - **通常プレイ視点のレイアウト**: 2 カラム(筐体 | サイドパネル)→ **中央 1 カラム**(筐体 + 液晶 + リール窓 → メーターパネル → 停止ボタン 3 つ → レバーオン・オート消化・BGM の遊技ボタン列 `main-controls`)へ。BGM ボタンはデバッグではなく遊技パネル側に配置。リセットはデバッグ内(ステージセレクト横)へ移動。
   - **総合ブラウザ確認(録画 2 本 + スクリーンショット)**: (1) 通常プレイ視点で手動 3G 消化(ボタン + キーボード両操作・メーター反映)/ (2) **通しフロー完走**: リセット → 中段チェリー強制 + オート消化 → 本前兆テロップ → 連続演出 4G → AT 突入(ステージ切替 + AT獲得メーター)→ V ストック蓄積 + セット継続 → 10 連 → エンディング 10G(上部バナー)→ 上位AT 突入 →(成立役をハズレ強制へ変更。持越しストックで継続)→ 10 連 → 2 回目エンディング → **AT終了(エンディング完走)イベント → 通常背景へ復帰**。console エラーなし。録画は videoReview で内容検証済み。
   - **確認手順のノウハウ**: 通しフローを短時間で見るには「中段チェリー(強制)+ オート消化」(毎 G V ストック獲得で 10 連まで確実に継続)→ 上位 AT 中に「ハズレ(強制)」へ切り替えると AT終了後の通常時がクリーンに保てる(ハズレはモード維持・偽前兆なし)。強制役セレクトの変更はオート消化を一時停止してから行うのが確実。**Space キーはレバーオンに割り当てられているため、computerUse でのセレクト操作はマウスクリックで行うこと**(Space で select を開こうとすると誤ってゲームが進む)。
   - **ユーザー向け動作確認手順書 `docs/STEP3_VERIFICATION.md` を新規作成**(STEP1 版の形式踏襲): 自動チェック(`npm test` 274 件 / lint / build)+ ブラウザ確認チェックリスト 15 項目(遊技サイクル・スベリ・目押し / メーター・ナビ / 演出・音 / 通しフロー)+ STEP 4 以降のため未実装・仮のままが正しい項目の一覧。
   - テスト 274 パス(変化なし)、lint / build グリーン。**STEP 3 完了(ROADMAP に完了マーク済み)。次は STEP 4(演出の作り込み。素材・演出リスト待ちと並行)**。
34. **AGENT #040**: **ユーザーからの「STEP 4 以降の進め方のプラン提示」依頼を受け、STEP 4 を 4a〜4f の 6 サブステップへ分割し `docs/ROADMAP.md` を更新**(コード変更なし。ドキュメントのみ。PR #40):
   - **4a**: 演出仕様の整理(Excel 演出構成シートの演出 ID 一覧化)+ シナリオテーブルのスキーマ設計 + 質問リスト(推奨案付き。振分け値・バトル 8G パターン振分け・書き換え時のシナリオ扱い・エンディング尺等)。ドキュメント中心
   - **4b**: シナリオ抽せん機構(`src/core/scenario.ts` 新設 + `state.ts` スロット 3 への配線 + フェーズ/毎 G 出力へのシナリオ情報追加。乱数消費順序が変わるため flow/simulate テストのシード・固定値取り直し)
   - **4c**: 通常時・前兆中の予告演出表示(`direction.ts` を「シナリオテーブル → 演出」解決へ置換 = 3d の差し替え方針どおり)
   - **4d**: 連続演出 A/B/C の作り込み + 成否告知
   - **4e**: AT・上位 AT・エンディング演出(バトルパート 8G 構成・復活告知・エンディング描き分け)
   - **4f**: 実素材の差し替え + 総合確認 + `docs/STEP4_VERIFICATION.md`(**素材入稿に依存するため唯一順不同 = 入稿があり次第 4b〜4e のどの時点でも先行実施可**)
   - 分割の根拠: 演出リスト確定版・実素材の入稿待ち(SPEC 未確定 1・4)がブロッカーにならないよう「仮素材・仮シナリオで進める実装(4a〜4e)」と「実素材差し替え(4f)」を分離。シナリオ抽せんは乱数と振分け検算を伴うためコア(4b)と表示(4c〜4e)を分けた。全体設計(シナリオ方式 = 確定 28 / 3 層レイヤー維持 / `GameState` 骨格不変 / 未確定項目は推奨案の仮テーブルで先行)は ROADMAP「STEP 4 全体設計」に明記。
   - STEP 5(Tauri + CI)は 5a(Tauri 導入 + ローカル確認)/ 5b(CI ビルド + 配布。Windows 実機確認はユーザー依頼前提)の 2 分割想定、STEP 6 は 6a〜6d(履歴・グラフ / 永続化 / ペナルティ / 白バー・ブランク役割)へ項目整理して ROADMAP へ記載(いずれも着手時に計画 AGENT が詳細化してよい)。
   - 素材・仕様依頼リストのステップ参照を更新(演出リスト = 4a〜4e・素材 = 4f・白バー = 6d)。
   - テスト 274 パス・lint グリーン(コード変更なしのため現状確認)。
35. **AGENT #041(今回)**: **PR #40 のマージコンフリクトの原因調査と解消**:
   - **原因**: AGENT #039(PR #39 = STEP 3e)と AGENT #040(PR #40 = STEP 4 分割プラン)が並行して走り、両方が `docs/HANDOVER.md` の同じ箇所(作成者欄・経緯 33・実行プラン欄・次タスク欄)を別内容で変更。PR #40 は PR #39 マージ前の main(4f950f1)から分岐しており、PR #39 が先に main へマージされたため PR #40 が CONFLICTING になった(PR #13/#14・#16/#17・#25/#26 と同型の「並行ラン + HANDOVER 競合」。両ランがどちらも自分を #039 と名乗り、履歴 039 のファイル名も重複していた)。
   - **解消**: PR #40 ブランチへ main をマージして統合。競合は `docs/HANDOVER.md` のみ(`docs/ROADMAP.md` は自動マージ成功 = STEP 3e 完了マークと STEP 4 分割が両立)。経緯 33 = STEP 3e(main 側)・経緯 34 = STEP 4 分割プラン(PR #40 側)として並記し、次タスク欄は「STEP 3e は完了済みのため次は STEP 4a」へ更新。プラン側のエージェントを #040 へ改番し、履歴を `docs/handover/039_step4_plan.md` → `040_step4_plan.md` へ改名(ROADMAP 内の「AGENT #039 で計画」表記も #040 へ修正)。
36. **AGENT #042**: **STEP 4a = 演出仕様の整理 + シナリオテーブル設計 + 質問リストを実施**(コード変更なし。ドキュメントのみ。成果物は **`docs/DIRECTION_SPEC.md`**):
   - **着手時のユーザー指示(2026-07-13)を SPEC 確定事項 33 へ反映**: 通常 4 背景(義経/静/弁慶/夕方)の固有予告 5 + 共通予告 4 は各演出とも**弱 / 強の 2 パターン** / 前兆背景の固有予告 1・2・3 は**前兆期待度の差**(1 = 弱・2 = 中・**3 = 本前兆確定**)/ AT・上位 AT の小役パート予告は 固有 1 = **ベルナビ(押し順示唆)**・固有 2 = **レア役示唆**・固有 3 = **強予告(V ストック濃厚)** / **予告は全てムービー素材**で、小役示唆系は最終的にベル等の図柄画像を画面表示 / 演出は仮素材で進める(実素材差し替えは STEP 4f)。
   - **Excel 演出構成シート群(演出構成全体 / 各背景 5 / AT中 / 上位AT中)を精読し演出 ID を一覧化**: 通常 4 背景 = 各 12 種(固有予告 5 + 連続演出 A/B + 共通予告 4 + 連続演出 C)/ 前兆背景 = 6 種(固有予告 3 + 連続演出 A/B/C。**連続演出 C も「背景固有」として記載**されており扱いを Q19 で確認中 = 推奨は共通 C の流用)/ AT バトル 20 パターン・上位 AT バトル 17 パターン(Excel の No は歯抜けあり。No 対応で採番)。
   - **シナリオテーブルのスキーマを設計**(実装は 4b): 予告は「スロット(`KOYU_1〜5` / `KYOTSU_1〜4`)× 強度レベル L0〜L3(なし / 弱 / 強 / 確定。**L3 は本前兆のみ**)」の**背景非依存表現**で持ち、具体ムービーの解決は表示時に「現在の背景 × スロット × レベル」で行う(前兆中の滞在背景は当せん次ゲームの契機 2/4 まで確定しないため)。前兆背景ではスロットを無視しレベル → 固有 1(弱)/ 2(中)/ 3(確定)へマッピング = 確定 33 の期待度ラダーが自然に成立。型定義案(`OmenScenario` = 前兆各 G の `ScenarioStep` 列 + 連続演出 1〜3G 目のチャンスアップ列)を DIRECTION_SPEC「2.2」に記載。
   - **AT 小役予告は毎 G・成立役ベースで抽せん**(シナリオ方式ではない。レバーオン時決定 = ナビ・目押し補助のため停止前に見せる)。`AT_STRONG`(V ストック濃厚)はストック獲得率の高い成立役でのみ出現させ「濃厚」を担保。**バトル 8G はバトル開始時の継続確定状態でルート一括抽せん**(確定側 = 勝利ルート振分け / 未確定側 = 敗北寄りルート + 8G 目の第 3 リール停止時にその時点の確定状態で復活成否)。
   - **仮振分けテーブル**(強度カーブ / スロット振分け / 連続演出チャンスアップ / AT 予告 / バトルルート。全て分母 100)と**仮素材ムービーの命名規約(合計 142 本)**を DIRECTION_SPEC「3.」「4.」に記載。
   - **質問リスト Q12〜Q20(推奨案付き)を作成**(Q1〜Q11 からの連番): Q12 通常時の単発ガセ予告なし / Q13 強度 4 段階体系 / Q14 スロット振分け / Q15 強度カーブ / Q16 書き換え時シナリオ引継ぎ / Q17 AT 予告方式 + 具体図柄表示 / Q18 バトル振分け / Q19 連続演出 4G 構成 + 前兆背景 C は共通流用 / Q20 エンディング 10G × ムービー 2 種。**回答までは推奨案 = 仮テーブルで 4b〜4e を進める**(STEP 2 の Q1〜Q11 と同方式)。
   - テスト 274 パス・lint グリーン(コード変更なしのため現状確認)。
37. **AGENT #043(今回)**: **質問リスト Q12〜Q20 の回答反映(SPEC 確定 34)+ STEP 4b = シナリオ抽せんコアを実装**(PR #42):
   - **Q12〜Q20 は全問回答済み(2026-07-13)**。SPEC 確定事項 **34** へ追記し、DIRECTION_SPEC を全面更新。Q13〜Q17・Q19・Q20 は推奨どおり。**回答に伴う重要な訂正 2 点**:
     - **予告の 2 系統化(Q12 + 予告役割の訂正)**: 通常 4 背景の**固有予告 1〜3 + 共通予告 4 つのうち 2 つ**(実装割当: 共通 1・2)の主な役割は**小役示唆**(例: 主人公が 1 アクションするとベルが画面表示される等)で、抽せんは「**成立小役が決定した後にどの演出を出すか**」= 毎 G・成立役ベース(通常時にも出す)。**前兆系の予告は固有 4・5 + 共通 3・4 のみ**でシナリオ方式の対象(前兆系の単発ガセは出さない = 従来どおり)。
     - **バトル 8G(Q18)**: 「各分岐にそって約 20 ルートを作成。勝利時・敗北時・復活時および継続率でそれぞれ振分けを持つ」→ DIRECTION_SPEC「3.6」にルート表を新設(AT = 勝利 W1〜W8 + 敗北寄り U1〜U6 = 14 ルート / 上位 = W1〜W7 + U1〜U5 = 12 ルート + 復活告知 R 系列。チャンスアップはルートへ焼き込み・振分けは継続率別の列で保持)。
   - **STEP 4b 実装**: `src/core/scenario.ts` 新設。
     - `drawOmenScenario(rng, kind, totalGames)` = 前兆シナリオ一括抽せん(各 G「レベル L0〜L3 →(L1 以上で)スロット」→ 連続演出チャンスアップ 3 つ、の乱数消費順で固定)。強度カーブ・スロット振分け・チャンスアップは DIRECTION_SPEC 3.1〜3.4 の仮テーブル(分母 100)。
     - **`state.ts` 配線**: `scheduleOmen` 内でシナリオを抽せんし **`OmenPhase.scenario`** に保持(initGameState・AT 終了時の本前兆リドローも同経路)。連続演出発展時に **`RenzokuPhase.chanceUps`** へ引継ぎ。**偽→本書き換え時はシナリオも再抽せんせず引継ぎ(Q16)**をテストで固定(乱数消費数の検証)。骨格(型のフィールド追加のみ)・処理順序は無変更。
     - **独立関数(advanceGame の乱数列を汚さない。UI が別 rng で呼ぶ)**: `drawKoyakuHint`(小役示唆予告。役 → なし/弱/強 → スロット)/ `drawAtYokoku`(AT 小役予告。ベル = ナビ・レア役 = 示唆/強)/ `drawBattleRoute`(バトル開始時の継続確定状態 + 継続率でルート一括)/ `drawRevival`(復活告知)。表示への配線は 4c〜4e。
   - **テスト**: `scenario.test.ts` 19 件新設(全テーブル合計 100 / 乱数消費順序の固定値 / 偽前兆に L3 なし / 分布 4σ / バトルルート・復活告知の固定値と分布)。`state.test.ts` はシナリオ分の乱数供給(`scenarioDraws` ヘルパー)と `omenPhase` / `renzokuPhase` ヘルパーで更新。**乱数消費順序が変わったため `flow.test.ts` をシード 62 → 30 / 450G → 520G へ、`simulate.test.ts` の固定値を取り直し**(既知の注意点どおり)。
   - **出玉への影響なしを確認**: 1M G シミュレーション(シード 1)で機械割 130.60%・通常時純増 −1.86・AT 純増 +6.37・コイン持ち 26.9G = 確定 32 の範囲を維持(シナリオ抽せんは乱数列の消費が増えるだけで抽せん確率は不変)。`docs/SIMULATION_REPORT.md` は更新不要と判断。
   - テスト 293 パス(+19)、lint / build グリーン。ブラウザ実機確認(録画 + スクリーンショット): リーチ目強制 → 本前兆(テロップ)→ 連続演出 4G(全画面 1/4G〜4/4G)→ 勝利/AT突入カットイン(継続率表示)→ AT ステージ切替、を手動プレイで確認(console エラーなし)。※液晶の演出表示自体は 3d の仮演出のまま(シナリオテーブルからの表示解決は 4c)。
38. **AGENT #044**: **STEP 4c = 通常時・前兆中の予告演出表示を実装**(コアロジック `src/core/` は無変更。変更は `src/ui/direction.ts` / `DirectionLayer.tsx` / `App.tsx` / `App.css` / `src/assets/` + 生成スクリプト):
   - **予告ムービー仮素材 51 本を生成**: 通常 4 背景の固有予告 1〜5 × 弱/強(40)+ 背景共通予告 1〜4 × 弱/強(8)+ 前兆背景の期待度ラダー 1〜3(3)。命名は DIRECTION_SPEC「4.」の規約どおり(`yokoku_<bg>_koyu<n>_<weak|strong>` / `yokoku_common<n>_<weak|strong>` / `yokoku_zencho<n>`)。`scripts/gen_placeholder_assets.py` へ `yokoku` 生成対象と**対象指定引数**を追加(`python3 scripts/gen_placeholder_assets.py yokoku` で予告のみ再生成。ffmpeg 出力はバイト再現しないため、追加分だけ指定して無関係な仮素材の差分を防ぐ)。`manifest.json` へ 51 件登録。`src/assets/index.ts` の `YOKOKU_VIDEOS`(`import.meta.glob` で一括解決。キー = ファイル名 stem)から参照。
   - **`direction.ts` を「シナリオ → 演出」解決へ置換**(3d の差し替え方針どおり。`Cutin` / 連続演出・エンディングの `StateOverlay` と描画側の仕組みは不変):
     - **前兆シナリオ予告**(固有 4・5 / 共通 3・4): `scenarioYokokuAtLeverOn(state)` が**レバーオン時に「これから回すゲーム」のステップ(`stepAt(scenario, phase.game + 1)`)を「現在の背景 × スロット × レベル」で具体ムービーへ解決**。通常 4 背景 = L1 → 弱素材 / L2・L3 → 強素材(通常背景に確定素材はない)。**前兆背景 = スロット無視でレベル → 固有 1/2/3 の期待度ラダー**(確定 33)。表示は液晶中央パネル(レベル別枠色: L1 灰 / L2 金 / L3 赤)+ 表示開始時に TELOP キュー音。
     - **表示タイミングの実装解釈**: 引継ぎ指示の `stepAt(scenario, phase.game)`(消化後 state 基準)ではなく「レバーオン時に次ゲーム分を解決」とした(実機の「予告はレバーオンから始まる」+ 小役示唆予告と同一時点で決定でき競合規約が素直になる)。表示される内容は同じシナリオステップで、背景もレバーオン時点 = 表示中のステージ動画と常に一致(前兆 1G 目の契機 2/4 の背景移行はそのゲームの終了時に反映されるため)。
     - **小役示唆予告**(固有 1〜3 / 共通 1・2): レバーオン時に成立役から `drawKoyakuHint` を **UI 専用 rng**(`createRng(randomSeed())` を App で 1 本保持)で抽せん(通常時も出す = Q12。`advanceGame` の乱数列は不変)。`koyakuHintView` が固有 = 滞在背景の素材 / 共通 = 共通素材へ解決し、右下小パネルにムービー + **約 1.5 秒後に成立役の図柄画像を CSS 遅延フェードイン**(確定 33。チャンス目はスイカ図柄で示唆・リーチ目は赤7)。
     - **競合規約(DIRECTION_SPEC 2.1)**: シナリオ予告がある G は小役示唆を出さない(UI は `scenarioYokokuAtLeverOn` が undefined のときだけ `koyakuHintAllowed` を確認して抽せん)/ 前兆背景滞在中は小役示唆なし / 連続演出・AT・エンディング中はどちらも出さない(AT 中予告は 4e の `drawAtYokoku`)。
     - **3d の仮テロップ(`TELOP_TEXTS`)は廃止**(予告のない G は静かに進む = 予告が出た時だけ前兆を匂わせる)。`StateOverlay` から TELOP を削除。
   - **UI**: `DirectionLayer` に `lever` prop(`LeverDirection` = seq + yokoku + hint)を追加し、レバーオンごとに差し替え表示(次のレバーオンまで)。デバッグパネルへ「予告演出」行(現在表示中の予告のラベル)と、前兆中の**シナリオ一括表示**(`シナリオ: L1固4 ・ L2共3 …` + 連続演出チャンスアップ)・連続演出中のチャンスアップ表示を追加(実機確認でシナリオと表示の一致を照合するため)。
   - テスト 304 パス(+30。direction.test.ts を書き直し = 解決規約・競合規約・期待度ラダー・51 キーの存在検証)、lint / build グリーン。ブラウザ実機確認 + 録画: 本前兆 10G をハズレ強制で消化し**シナリオ表示どおりのゲームで予告パネルが出る / L0 ゲームは出ない**こと、前兆背景移行後は**期待度ラダー(前兆予告 1/2)**になること、通常時の強スイカで**小役示唆予告 + スイカ図柄フェードイン**、連続演出 → 勝利! → AT突入! の回帰、console エラーなしを確認。
39. **AGENT #045(今回)**: **STEP 4d = 連続演出 A/B/C の作り込み + 成否告知を実装**(コアロジック `src/core/` は無変更。変更は `src/ui/direction.ts` / `DirectionLayer.tsx` / `App.tsx` / `App.css` / `src/assets/` + 生成スクリプト):
   - **連続演出ムービー仮素材 46 本を生成**: A/B × 5 背景(前兆背景含む)× G1〜4(40)+ C 共通 × G1〜4(4)+ 成否告知 win/lose(2)。命名は DIRECTION_SPEC「4.」の規約どおり(`renzoku_<a|b>_<bg>_g<1-4>` / `renzoku_c_g<1-4>` / `renzoku_result_<win|lose>`)。`scripts/gen_placeholder_assets.py` へ `renzoku` 生成対象を追加(`python3 scripts/gen_placeholder_assets.py renzoku` で連続演出のみ再生成)。`manifest.json` へ 46 件登録。`src/assets/index.ts` の `RENZOKU_VIDEOS`(`import.meta.glob`。キー = ファイル名 stem)から参照し、解決は `direction.ts` の `renzokuVideoUrl`(存在しないキーは即エラー)。
   - **3d の全画面仮表示を 4G 構成の実演出へ置換**: `StateOverlay` から RENZOKU を削除(`RENZOKU_PRESENTATION` 廃止。overlay は ENDING バナーのみに)し、**予告(4c)と同じ「レバーオン時に 1G 分を解決」方式の `renzokuAtLeverOn(state)`** を新設。連続演出 1G 目 = フェーズ `OMEN` の最終 G 消化済み(`phase.game >= totalGames`)、2〜4G 目 = フェーズ `RENZOKU`(`phase.game + 1` G 目)で判定し、`RenzokuView`(videoUrl / title / n/4G / 段階名 = G1 導入・G2 展開・G3 あおり・G4 決着 / chanceUp)を返す。**A/B = 発展時の滞在背景の素材(前兆背景含む 5 種)/ C = 背景共通**(Q19。連続演出中に背景は変わらないため滞在背景で解決してよい)。
   - **チャンスアップ**: `RenzokuPhase.chanceUps`(1G 目は `OmenPhase.scenario.renzokuSteps`)を参照し、チャンス G は **CHANCE UP! バッジ + 金枠**の表示差分で表現(仮素材ではムービー共通 = DIRECTION_SPEC「4.」の方針。G4 は成否告知 G のため常に通常)。
   - **成否告知**: `RENZOKU_RESULT` カットインへ専用ムービーを配線(勝利 = `renzoku_result_win` 2200ms / 敗北 = `renzoku_result_lose` 2000ms。SE は従来の RENZOKU_SUCCESS / RENZOKU_FAIL)。連続演出の全画面表示は次のレバーオンまで残るため、告知カットインは決着画面の上に重なって表示される。
   - **偽→本書き換え(確定 21(c))**: 演出の見た目(`RenzokuView`)は前兆種別に依存しない(kind で分岐しない)ため書き換えが起きても演出は自然に継続し、G4 の告知だけが勝利になる。FAKE/REAL の view 同値性をテストで固定 + 実機で偽前兆中の中段チェリー → 「偽→本前兆 書き換え」イベント + シナリオ・演出種別の引継ぎ(再抽せんなし)を確認。
   - **UI 配線**: `LeverDirection` に `renzoku` を追加(`drawLeverDirection` で `renzokuAtLeverOn` を最優先。連続演出中はシナリオ予告・小役示唆予告を出さない = 従来の競合規約どおり)。`DirectionLayer` が全画面ムービー(ループ再生)+ ヘッダー(タイトル・n/4G)+ フッター(段階名・CHANCE UP! バッジ)を描画。デバッグパネルの「予告演出:」行に連続演出のラベル(`連続演出B G3 あおり(チャンス)` 等)を追加。
   - テスト 311 パス(+7。direction.test.ts に `renzokuAtLeverOn` の解決規約・46 キーの存在検証・成否告知ムービーを追加)、lint / build グリーン。ブラウザ実機確認 + 録画 2 本: (1) 本前兆(リーチ目強制)→ 連続演出 B 4G(G3 チャンスアップ)→ 勝利! → AT突入!(継続率 66%)→ AT ステージ切替 / (2) 偽前兆(強スイカ強制)→ 連続演出 B 4G(G1・G2 チャンスアップ)→ 敗北… → 通常復帰。A(前兆背景滞在 = `renzoku_a_zencho_g1`)と C(共通素材)の G1 全画面もスクリーンショットで確認。console エラーなし。
   - 実機確認のコツ: **強スイカ(強制)1G は本前兆非当選時 100% 偽前兆**(確定 3)なので、偽前兆の再現は「強スイカ 1G → ハズレで消化」が最短(約 90%。残り 10% は本前兆になるのでリセットして引き直す)。
40. **AGENT #046**: **STEP 4e = AT・上位 AT・エンディング演出を実装**(コアロジック `src/core/` は無変更。変更は `src/ui/direction.ts` / `DirectionLayer.tsx` / `App.tsx` / `App.css` / `src/assets/` + 生成スクリプト):
   - **AT・エンディング演出ムービー仮素材 45 本を生成**: AT 小役予告 6(`at_koyaku_<navi|rare|strong>` / `uat_koyaku_<...>`)+ バトル AT 20(`battle_at_<01-20>` = Excel「AT中」シートのパターン No)+ 上位 17(`battle_uat_<no>` = Excel「上位AT中」の No。**13・15・16・19 は歯抜けのまま採番** = 01-12, 14, 17, 18, 20, 21)+ エンディング 2(`ending_<to_upper|complete>`)。`scripts/gen_placeholder_assets.py` へ `at` 生成対象を追加(`python3 scripts/gen_placeholder_assets.py at` で再生成)。`manifest.json` へ 45 件登録。参照は `src/assets/index.ts` の `AT_VIDEOS`(`import.meta.glob`)+ `direction.ts` の `atVideoUrl`(存在しないキーは即エラー)。
   - **AT 小役パート予告(確定 33・Q17)**: 4c と同じ「レバーオン時に UI 専用 rng で抽せん」の枠で `drawAtYokoku` を配線(`LeverDirection.atYokoku`)。`atYokokuAllowed`(次に回すゲームが AT 小役パートの G のときのみ。バトル・エンディング中は出さない = DIRECTION_SPEC 2.3)+ `atYokokuView`(AT/上位で `at_koyaku_*` / `uat_koyaku_*` を解決。**NAVI = ベル図柄 + 押し順テキスト「中→左→右」** / **RARE = 成立役の図柄画像**(`HINT_SYMBOLS` 流用)/ **STRONG = 図柄なし + 金枠**)。表示は小役示唆予告と同じ右下パネル(`koyaku-hint` 流用 + `hint-navi-order`)。
   - **バトルパート 8G 構成(Q18)**: バトル 1G 目のレバーオン(= フェーズ AT・小役 10G 消化済み)で `drawBattleRoute` を一括抽せんし **`App.tsx` の `battleRef`(useRef)に保持**(演出用 rng のため state 不要。再レンダーは leverDirection が担う)。毎 G `battleView` が「ルート × G → Excel パターン No」の対応表(`AT_ROUTE_PATTERN_NOS` / `UPPER_ROUTE_PATTERN_NOS`。G1〜3 = 通常/チャンスのペア No 1/2・3/4・5/6、G4〜8 = ルート分岐)でムービーを解決し、連続演出と同じ全画面構成(タイトル「BATTLE — 頼朝との一戦」/「共闘BATTLE — 敵軍との決戦」+ n/8G + 役割ラベル + CHANCE UP! バッジ)で表示。
   - **継続確定状態の実装解釈(DIRECTION_SPEC 2.5 に記録)**: バトル開始時の継続率抽せん(確定 29)は core ではバトル 1G 目の `advanceGame`(= 全停止時)で行われるため、レバーオン時点で既知の確定は V ストックのみ。ルートは「確定 = vStock > 0」で仮抽せんし、**1G 目の全停止で率当せん(continueConfirmed)が判明したら勝利ルートへ引き直す**(`finishGame` 内)。2G 目以降の小役継続当せんでは引き直さない(敗北寄りルートのまま 8G 目の復活告知で見せる)。実機でも「U1 G1 → 率当せん → W2 G2〜」の引き直しを確認済み。
   - **復活告知**: 敗北寄りルートの 8G 目全停止で `AT_SET_CONTINUE` / `ENDING_START` が発生していたら `drawRevival`(UI 専用 rng)で告知パターンを抽せんし、`revivalCutin`(「復活!」+ パターン名。SPECIAL)を**カットイン列の先頭へ差し込む**(FINISH アクションに `cutins` を渡す形へ変更 = reducer では組み立てない)。
   - **エンディング 2 種(Q20)**: `overlayForState` の ENDING に `videoUrl` を追加し、`after` で `ending_to_upper`(通常 AT 10 連)/ `ending_complete`(上位 完走)の**全画面ムービー**を描き分け(`ending-screen`。上部バナーは従来どおり)。`ENDING_GAMES` = 10 は変更なし。エンディング 0G 目(突入ゲームの直後)は直前のバトル G8 全画面が次のレバーオンまで上に残る(4d の重なり規約と同じ)。
   - **デバッグパネルへ「1G消化(オート)」ボタンを追加**(リセットの左)。1 クリックで 1 ゲームだけ即時消化(レバー〜全停止)。オート消化(連続 160ms/G)は速すぎて実機確認の狙った状態で止められないため、今後の確認はこのボタン推奨。
   - テスト 323 パス(+12。AT_VIDEOS 45 キーの存在検証 / atYokoku の許可条件と解決 / battleView の No 対応(AT・上位・歯抜け)/ revivalCutin / ENDING overlay の videoUrl)、lint / build グリーン。ブラウザ実機確認 + 録画(AT 予告 2 種・バトル 8G・復活告知・エンディング 2 種・上位 AT 突入・完走 → 通常復帰。console エラーなし)。
41. **AGENT #047(今回)**: **ユーザーからの「STEP 4 完了。STEP 5 は細分化必要か?」への回答 + STEP 5 の詳細計画確定**(コード変更なし。ドキュメントのみ):
   - **回答: STEP 1〜4 のような追加細分化は不要。既存想定どおりの 2 分割(5a / 5b)で十分**。根拠: (1) 作業領域が「Tauri シェル導入 = ローカル(VM 内)完結」と「CI ビルド = GitHub Actions 側」で自然に 2 分割でき、依存は 5a → 5b の一方向のみ / (2) 既存の `src/core/`・`src/ui/` は原則無変更で追加は `src-tauri/` + CI ワークフローのみ = ERROR 終了を招いた STEP 1 の巨大スコープとは規模が違う / (3) これ以上割ると「グリーン + 動作確認済みで終わる」1 サブステップの完了条件を満たせない。
   - **`docs/ROADMAP.md` の STEP 5 を詳細化**: 全体設計(Tauri 2.x 採用 / 既存 Vite ビルドを `frontendDist` でそのまま利用 = コア・UI・テスト無変更 / `src/platform/` ラッパーは Web API のままで動く想定・問題時のみ差し替え / 既存 npm scripts は維持し `tauri:dev` / `tauri:build` を追加)+ 5a・5b の作業項目・完了条件・リスクを確定。
   - **5a(Tauri 導入 + ローカル動作確認)の既知リスク**: Linux の WebKitGTK は WebM(VP9)再生に gstreamer プラグインが必要な場合がある(背景・演出ムービーは全て WebM)。Linux で再生不可でも Windows の WebView2(Chromium 系)は別系統のため切り分けて記録する。Rust toolchain 等の環境構築が多いため、5a 完了時に Cursor Cloud 環境セットアップ更新の要否を HANDOVER へ記録すること。
   - **5b(CI ビルド + 配布)**: タグ push で Release 添付 + `workflow_dispatch`(検証用)の 2 本立て。Windows 実機確認は VM では不可能なためユーザー依頼前提で `docs/STEP5_VERIFICATION.md` を作成する。
   - STEP 4 の完了状況の整理: 4a〜4e 完了済み。**4f(実素材差し替え + 総合確認 + `docs/STEP4_VERIFICATION.md`)は素材入稿待ちの順不同タスク**として残置(`incoming/` に新規入稿なしを確認済み)。入稿があり次第 STEP 5 と並行して実施してよい(従来ルールどおり)。
   - テスト 323 パス・lint グリーン(コード変更なしのため現状確認)。
42. **AGENT #048(今回)**: **STEP 5a を依頼したラン「Roadmap step5a」が ERROR 終了した原因を Cloud Agent 診断 API で調査**(コード変更なし。ドキュメントのみ):
   - **ユーザーが確認したログの ERROR(`Failed to create recording directory /opt/cursor/artifacts/`)は失敗原因ではない**。これは Cursor Cloud VM 起動時の既知の無害な警告で、正常完了した直近ラン(AGENT #046 = STEP 4e / AGENT #047 = STEP 5 計画)の起動ログにも全く同じ ERROR が 2 件ずつ記録されている。VM セットアップの `create-artifacts-dir` ステップと exec-daemon 起動が並行して走る際の一時的な競合で、直後に「Artifacts assets directory created/verified」と成功しており、エージェントの動作(画面録画・アーティファクトのアップロード)にも影響していない。
   - **実際の障害**: 当該ラン(bc-11a6fcd6。21:32:20 作成)は 36 秒後の 21:32:56 に ERROR となり、**トランスクリプトが完全に空(メッセージ 0 件)**・ブランチ作成なし・コード変更なし。エージェントが最初の応答を生成する前(会話が始まる前)に Cursor Cloud サービス側でランが打ち切られた形。リポジトリ・環境・依頼文の問題ではない(環境セットアップは `npm install` まで exit code 0 で正常完了していた)。#014 で調査した事例(巨大スコープによる思考肥大化 → 応答生成の繰り返し失敗)とも異なり、今回は開始直後の即死 = **サービス側の一過性障害**と判断。
   - **失われた成果物はゼロで、再試行は安全**。現 main(96c4432)でテスト 323 パス・lint グリーンを確認済み。同じ依頼文で STEP 5a を再依頼すればよい(スコープ分割等の対策は不要)。
43. **AGENT #049(今回)**: **STEP 5a = Tauri 導入 + ローカル動作確認を実施**(コアロジック `src/core/`・UI `src/ui/`・テストは無変更。追加は `src-tauri/` + npm scripts + `.gitignore` のみ):
   - **環境構築**: Rust toolchain を stable 1.97.0 へ更新(VM 既存の 1.83.0 では依存の `edition2024` 要求でビルド不可 → `rustup update stable && rustup default stable`)+ apt で WebKitGTK 開発依存(`libwebkit2gtk-4.1-dev` `libgtk-3-dev` `libayatana-appindicator3-dev` `librsvg2-dev` 等)を導入。`@tauri-apps/cli` を devDependencies へ追加し、`tauri` / `tauri:dev` / `tauri:build` スクリプトを `package.json` へ追加(**既存の `dev` / `build` / `test` / `lint` は無変更 = Web 版フローを壊していない**)。
   - **`src-tauri/` scaffold**: `tauri.conf.json`(productName = yoshitsune-monogatari / identifier = jp.cloudagent.yoshitsune / `frontendDist` = `../dist` / アイコンは Tauri 既定の仮)。ウィンドウは conf ではなく **Rust 側(`lib.rs` の `WebviewWindowBuilder`)で生成**(760×1000・最小 520×720。筐体 1 カラム UI に合わせたサイズ。Linux リリースビルドだけ URL を切り替える条件分岐のため)。
   - **Linux(WebKitGTK)のメディア再生問題 2 件を実測・解決**(`lib.rs` に理由コメント付き。**Windows の WebView2(Chromium 系)は別系統でどちらの問題も無い**):
     - (1) **GStreamer に `tauri://` の URI ハンドラが無い**: リリースビルドの背景・演出ムービー(WebM)/ BGM・SE(Ogg)が「No URI handler implemented for "tauri"」で全滅(tauri-apps/tauri#3725)→ **Linux リリースビルドのみ `tauri-plugin-localhost`(+ `portpicker` で空きポート)でフロントエンドを http://localhost:<port>/index.html から配信**。プラグインは "/" を index.html へ解決しない(500)ため URL に `/index.html` を明示。
     - (2) **既定の playbin(2)では HTTP 配信の WebM(VP9)がストール**: 非ループ動画 = 最初のフレームで停止 / loop 動画 = 1 周目終端でフリーズ(MP4/H.264 は正常 = WebM + matroskademux 固有。素の WebKitGTK 単体でも再現 = Tauri 起因ではない)→ **`WEBKIT_GST_USE_PLAYBIN3=1` を WebView 初期化前に設定**で解消。
   - **デスクトップ実機確認(録画 = `step5a_demo_zencho_renzoku_at.mp4`)**: リリースビルドのバイナリ(`src-tauri/target/release/yoshitsune-monogatari`)を起動し、背景動画ループ再生 / リール回転・停止(キーボード Space・Z/X/C)/ 前兆シナリオ予告・小役示唆予告ムービー / 本前兆 → 連続演出 4G → 勝利 → AT 突入の通しフロー / console エラーなしを確認。
   - **既知の VM 限定の制約(コード起因ではない)**: (a) AT 突入後のステージ動画が黒画面になることがある(GPU 無し = llvmpipe ソフトウェアレンダリング + 音声デバイス無しの VM で、動画プレイヤーを多数生成した後の新規 `<video>` 要素で発生する WebKitGTK 固有事象。ロジック・UI・メーターは正常動作)/ (b) 音声はダミーシンク(PulseAudio null sink)で再生 = 実音は VM では確認不可。**どちらも Windows(WebView2)では別系統のため 5b の実機確認チェックリストで確認する**。
   - **Cursor Cloud 環境セットアップ更新の要否 = 要**(下記「次の AGENT へのタスク」参照): Rust stable 1.97 + apt の WebKitGTK 依存 + `cargo build` の初回コンパイル(約 4 分)が毎回必要になるため、env setup agent での更新を推奨。
   - テスト 323 パス(既存に影響なし)・lint / build グリーン。`tauri:build` で Linux リリースバイナリ + deb/rpm 生成を確認。**STEP 5a 完了(ROADMAP にマーク済み)。次は STEP 5b(CI ビルド + 配布)**。
44. **AGENT #050(今回)**: **STEP 5b 前の Tauri ビルド環境整備**(ユーザー指示。コード変更は網羅テスト 1 件のタイムアウト延長のみ):
   - **VM に `npm run tauri:build` が通る環境を構築**: Rust stable **1.97.0** へ更新(`rustup update stable && rustup default stable`。VM 既存の 1.83.0 は依存の `edition2024` 要求で不可)+ apt で `libwebkit2gtk-4.1-dev build-essential libxdo-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev` を導入。追加で `pulseaudio pulseaudio-utils` も導入(実機確認用の null sink。VM は音声デバイス無し)。
   - **初回 `npm run tauri:build` 実行でキャッシュ作成済み**: クリーンビルド約 2 分 22 秒で Linux バイナリ + deb/rpm/AppImage の 3 バンドル生成に成功。キャッシュ(`src-tauri/target` 1.9GB + `$CARGO_HOME` = `/usr/local/cargo` 223MB)により **2 回目以降の `cargo build --release` は約 23 秒**。
   - **ビルド成果物の実機確認済み**: リリースバイナリを起動し、筐体描画・背景動画ループ・レバーオン → 停止ボタン 3 つの 1G 消化・メーター表示を computerUse で確認(console 致命エラーなし)。
   - **テスト 1 件の修正**: `gameCycle.test.ts` の「仮押し順が一致する押し順では resolveSpin と完全一致」テスト(9 役 × 3 押し順 × 20³ の全数比較)がフルスイート並列実行時にデフォルト 5000ms をわずかに超えて flaky だったため(単独実行では約 3 秒でパス)、明示的に `{ timeout: 20_000 }` を指定。テスト内容は無変更。
   - **注意: VM への環境導入はこのランの VM 限り**。他のエージェントが `tauri:build` を使う場合は同じセットアップが再度必要(AGENTS.md の Cursor Cloud instructions に手順記載済み)。恒久化するには cursor.com/onboard の env setup agent で環境イメージを更新する(#049 から提案済み・下記参照)。
   - テスト 323 パス(タイムアウト延長後)、lint / build グリーン。
45. **AGENT #051(今回)**: **Cursor Cloud 環境イメージの Tauri ビルド対応更新**(env setup 依頼。コード変更なし。ドキュメント = HANDOVER/AGENTS.md 更新のみ):
   - env setup フローの VM 上で、`npm run tauri:build` に必要な環境一式を導入: Rust stable **1.97.0**(`rustup update stable && rustup default stable`。約 10 秒)+ apt で `libwebkit2gtk-4.1-dev build-essential libxdo-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev pulseaudio pulseaudio-utils`(約 17 秒)。
   - 初回 `npm run tauri:build` をこの VM で実行済み(約 2 分 14 秒。Linux バイナリ + deb/rpm/AppImage の 3 バンドル生成成功)。ビルドキャッシュは **`src-tauri/target` 約 2.0GB + `$CARGO_HOME`(`/usr/local/cargo`)約 223MB** としてこの VM に存在し、環境イメージのスナップショットに含まれれば以後のエージェントは再セットアップ不要。
   - キャッシュ有効性の実測: `touch src/main.rs` 後の 2 回目 `cargo build --release` は **22.3 秒**(#050 の実測 約 23 秒どおり)。
   - 既存フローへの影響なしを確認: テスト 323 パス / lint グリーン。
   - `AGENTS.md` の Cursor Cloud instructions を更新(環境イメージ更新後の前提 = Tauri 依存導入済み・キャッシュあり、を反映)。
46. **AGENT #052(今回)**: **STEP 5b 着手前の事前確認**(ユーザー依頼「STEP 5b に問題無く進める状態か確認」。コード変更なし。ドキュメント = HANDOVER 更新 + 履歴 052 のみ):
   - **結論: STEP 5b に進める状態**。main(8066f09)クリーン・未マージ作業なしで、テスト 323 パス / lint / build グリーンを現 main で確認済み。`incoming/` は README のみで新規入稿なし = 4f のブロッカーは継続(順不同タスクのまま)。
   - **5b の最大リスク「ワークフローファイルの push 権限」を実測で確認 = 問題なし**: Cloud Agent の GitHub App トークンで `.github/workflows/*.yml` を含むコミットをテスト用ブランチ(`cursor/workflow-push-test-c6de`)へ push でき、拒否されないことを確認(GitHub App に `workflows` 権限がない場合ここで失敗するため事前検証した)。検証後ブランチはリモート・ローカルとも削除済み。リポジトリは public・既存ワークフロー 0 本・Actions API(読み取り)も応答あり。
   - **注意: この VM は Tauri 対応の環境イメージが反映されていなかった**(rustc 1.83.0 のまま / WebKitGTK 開発依存なし / `src-tauri/target` キャッシュなし)。#051 の env setup 後もランによっては旧イメージで起動する場合がある模様。**ただし 5b の成果物(GitHub Actions ワークフロー + `docs/STEP5_VERIFICATION.md`)はローカルの Tauri ビルドを必須としない**(ビルドは CI の `windows-latest` 側で実行)。5b AGENT がローカルで Linux ビルドの検証をしたい場合のみ、HANDOVER 記載の手動セットアップ(Rust stable 更新 + apt 一式。約 3 分 + 初回ビルド約 2 分半)を行うこと。
   - **5b 実施時の確認事項**: `workflow_dispatch` の実行はエージェントの `gh` が読み取り専用のため**自分ではトリガーできない**。PR 作成後に (a) ユーザーへ Actions の手動実行を依頼する、または (b) ワークフローに `pull_request`(paths: `.github/workflows/**` 等)や `push`(ブランチ)トリガーを検証用に含めて push 時に自動実行させ `gh run view --log` で結果を確認する、のどちらかで CI グリーンを確認すること(ROADMAP の「Actions の実行はユーザーへ依頼が必要な場合がある」に対応する具体策)。

- Phase 1〜2 完了 + Phase 3 の抽せんテーブル層が完了。
- **背景動画・リール図柄はユーザー入稿の実素材**。BGM/SE・液晶フォールバック・カットイン演出動画は仮素材のまま。
- **STEP 1(リール制御の本対応)は 1a〜1f 全サブステップ完了**: `reel.ts` は図柄 8 種 + Excel 20 コマ配列(左コマ 14 のみリプレイへ変更 = SPEC 確定事項 17)、表示判定・停止制御とも有効 5 ライン対応。全役 × 全 20³ 押下位置 × 押し順 6 通りの網羅テスト済み(skip・TODO 残ゼロ)。リーチ目は赤7 狙いで 100% 7 揃い(外せば取りこぼし)、DDT 黒バー選好あり。
- **質問リスト Q1〜Q11 はユーザー回答済み(全問推奨案どおり)で SPEC 確定事項 18〜28 へ反映済み**(AGENT #027)。STEP 2 のブロッカーはすべて解消。
- **STEP 2a 完了(AGENT #027)**: `src/core/state.ts` に `GameState` / `Phase`(判別可能ユニオン)/ `advanceGame`(純関数)/ `initGameState` の骨格を実装(経緯 21 参照)。**2b〜2f はこの骨格(型・処理順序・予約フラグ)を変えずに挿入スロットへ実装を足していく**こと。
- **STEP 2b 完了(AGENT #028)**: 前兆管理(偽前兆突入・スケジュール・進行・偽→本書き換え・解決)を `advanceGame` スロット (3)(4) へ実装(経緯 22 参照)。前兆タイムラインの実装規約は `state.ts` ヘッダーコメントに記録。
- **STEP 2c 完了(AGENT #029)**: 背景移行(予約契機 2/3/4 の発火・30G 契機・優先順位・カウンタリセット・`BACKGROUND_CHANGE` イベント)を `advanceGame` スロット (5) へ実装(経緯 23 参照)。背景移行の実装規約は `state.ts` ヘッダーコメントに記録。
- **STEP 2d 完了(AGENT #030)+ 実装解釈 4 点はユーザー回答で確定 29〜31 へ反映済み(AGENT #032)**: AT 管理(継続率・V ストック・セット遷移・10 連・エンディング・AT 終了処理 + `isNaviActive`)を `advanceGame` スロット (4) へ実装(経緯 24・26 参照)。現仕様: V ストックはバトル開始時に先消化 / 10 連目も敗北あり / 10 連勝利でエンディング 10G(ナビあり)経由 → 通常 AT = 必ず上位 AT(ストック持越し)・上位 AT = AT 終了。AT 管理の実装規約は `state.ts` ヘッダーコメントに記録。**スタブ・TODO 残ゼロ**。
- **STEP 2e 完了(AGENT #031)**: ヘッドレス 1G 実行(`src/core/play.ts`)+ 大量試行シミュレーション(`src/core/simulate.ts`・`npm run sim`)+ 通しフロー統合テスト・固定シード回帰統計テストを実装(経緯 25 参照)。**計測レポートは `docs/SIMULATION_REPORT.md`(確定 29〜31 反映後に再計測済み。機械割 約 129〜131% は 2026-07-12 のユーザー回答で「問題なし」= SPEC 確定事項 32。出玉バランス確定)**。
- **STEP 2f 完了(AGENT #033)= STEP 2 全サブステップ完了**: `App.tsx` がステートマシン駆動(ステージ自動切替 + デバッグパネル + イベントログ + オート消化。経緯 27 参照)。UI 操作だけで「レア役 → 前兆 → AT 突入 → 終了 → 通常復帰」を再現できる。
- **予告演出の役割・体系は確定 33 + 34(2026-07-13。Q12〜Q20 全問回答済み)で確定**: 予告は **2 系統** = 小役示唆系(固有 1〜3 / 共通 1・2。毎 G・成立役ベース。通常時も出す)+ 前兆系(固有 4・5 / 共通 3・4。シナリオ方式。前兆中のみ)/ 前兆背景の固有 1・2・3 は期待度(弱・中・本前兆確定)/ AT 小役予告は ベルナビ・レア役示唆・強予告(V ストック濃厚)/ バトル 8G は約 20 ルート + 勝利/敗北/復活/継続率別振分け / 予告は全てムービー。**演出 ID 一覧・シナリオスキーマ・振分けテーブルは `docs/DIRECTION_SPEC.md` が正**。振分けの具体数値は仮値で OK(実機で見て調整 = Q14・Q15 回答)。
- **STEP 4b 完了(AGENT #043)**: シナリオ抽せんコアは `src/core/scenario.ts`(`drawOmenScenario` = scheduleOmen 内で消費 / `drawKoyakuHint`・`drawAtYokoku`・`drawBattleRoute`・`drawRevival` = advanceGame の乱数列を汚さない独立関数)。シナリオは `OmenPhase.scenario`・チャンスアップは `RenzokuPhase.chanceUps` に保持(書き換え時は引継ぎ = Q16)。
- **STEP 4c 完了(AGENT #044)**: 予告演出の表示は `src/ui/direction.ts` の `scenarioYokokuAtLeverOn`(前兆シナリオ予告)+ `koyakuHintAllowed` / `koyakuHintView`(小役示唆予告)で解決し、`App.tsx` の `drawLeverDirection`(レバーオン時)→ `DirectionLayer` の `lever` prop で表示する。予告ムービーは `src/assets/video/yokoku/` の仮素材 51 本(`YOKOKU_VIDEOS`。命名 = DIRECTION_SPEC「4.」)。詳細は経緯 38 参照。
- **STEP 4d 完了(AGENT #045)**: 連続演出の表示は `direction.ts` の `renzokuAtLeverOn`(レバーオン時に「種別 × 滞在背景 × G」で 4G 構成 = 導入/展開/あおり/決着を解決。チャンスアップは `chanceUps` 参照で CHANCE UP! バッジ)+ `RENZOKU_RESULT` カットインの専用ムービー(`renzoku_result_<win|lose>`)。連続演出ムービーは `src/assets/video/renzoku/` の仮素材 46 本(`RENZOKU_VIDEOS`)。詳細は経緯 39 参照。
- **STEP 4e 完了(AGENT #046)**: AT 中・エンディングの演出は `direction.ts` の `atYokokuAllowed` / `atYokokuView`(AT 小役予告)+ `battleGameAtLeverOn` / `battleView`(バトル 8G。ルートは `App.tsx` の `battleRef` に保持)+ `revivalCutin`(復活告知)+ `overlayForState` の ENDING videoUrl(エンディング 2 種)。AT 演出ムービーは `src/assets/video/at/` の仮素材 45 本(`AT_VIDEOS`)。継続確定の実装解釈は DIRECTION_SPEC 2.5。詳細は経緯 40 参照。
- 仕様の未確定事項は `docs/SPEC.md`「14.」の 4 件(演出ムービー実素材の入稿・白バー/ブランクの役割・30G の暫定値・シナリオ振分けの具体数値の調整)。いずれも 4f のブロッカーではない(実素材入稿のみ 4f の前提)。
- **完成までの実行プランは `docs/ROADMAP.md`**(STEP 1〜6 とユーザーへの素材・仕様依頼リスト)。**STEP 1・STEP 2・STEP 3(3a〜3e)・STEP 4a〜4e・STEP 5a 完了。STEP 4 残り**: 4f(実素材差し替え + 総合確認 + `docs/STEP4_VERIFICATION.md`。素材入稿に依存・順不同で先行実施可)。**次は STEP 5b(CI ビルド + 配布。Windows 実機確認はユーザー依頼前提で `docs/STEP5_VERIFICATION.md` を作成)**。
- **STEP 5a 完了(AGENT #049)**: Tauri 2.x シェルは `src-tauri/`(`tauri:dev` / `tauri:build`)。Linux(WebKitGTK)のメディア再生対策 2 件(localhost 配信 + playbin3)は `src-tauri/src/lib.rs` の理由コメントが正。**Windows ビルド(5b)にはこの対策は不要だが、条件コンパイル(`cfg(target_os = "linux")`)で自動的に除外される**ため 5b 側での変更は不要。詳細は経緯 43 参照。
- **STEP 3a 完了(AGENT #035)**: 遊技サイクルの純ロジックは `src/ui/gameCycle.ts`(レバーオン → 停止ボタンで 1 リールずつ `resolveStop` → 全停止 → `advanceGame`。押し順 = 押した順、仮押し順の扱いはファイルヘッダー参照)。
- **STEP 3b 完了(AGENT #036)**: リール回転アニメーションの純ロジックは `src/ui/reelAnimation.ts`(連続位置 `continuousPosition` / スベリ計画 `planSlip`・`slipPosition` / コマ帯 `reelStrip`)。描画は `App.tsx` の `requestAnimationFrame` + `.reel-strip`(App.css)の translateY スクロール。**「描画に使う時刻」と「押下位置の計算に使う時刻」は同一**(floor(連続位置) = `spinningPosition` をテストで固定)なので、以後の UI 変更でもこの関係を壊さないこと。
- **STEP 3c 完了(AGENT #037)**: メーター管理の純ロジックは `src/ui/counters.ts`(`meterOnLever` = BET 徴収・自動補充 / `meterOnFinish` = 払出加算 + AT 獲得枚数。呼び出しタイミングはファイルヘッダー参照)。UI は 7seg 風メーターパネル(CREDIT / BET + REPLAY ランプ / WIN / AT 中のみ AT獲得)+ リール窓上のナビ数字(`navi-digit`。停止で消灯)。
- **STEP 3d 完了(AGENT #038)**: 演出マッピング層は `src/ui/direction.ts`(state → `StateOverlay` / events → `Cutin` の対応表。**STEP 4 のシナリオテーブルへの差し替えはこのファイルの置き換えだけ**)、描画は `src/ui/DirectionLayer.tsx`(液晶へ重ねる表示専用 + カットインキュー)。**SE はサウンドキュー `src/ui/sound.ts`(`playCue`)経由・BGM は `STAGE_BGMS` + クロスフェード(`platform/audio.ts`)で、実素材の差し替えは対応表 or 同名ファイル置き換えで完結**(ユーザー指示による要件)。
- **STEP 3e 完了(AGENT #039)= STEP 3 全サブステップ完了**: デバッグ系 UI は `App.tsx` の `<details className="debug">` へ集約(デフォルト閉)。通常プレイ視点は中央 1 カラム(筐体 → メーター → 停止ボタン → レバーオン・オート消化・BGM)。通しフロー(通常 → 前兆 → AT → 上位AT → エンディング → 通常復帰)のブラウザ総合確認・録画済み(経緯 33 参照)。
- **ユーザーが自分でアプリの動作確認をする手順は `docs/STEP3_VERIFICATION.md`**(STEP 3 完了時点版。AGENT #039 が同手順の実機確認済み)。STEP 1 時点版は `docs/STEP1_VERIFICATION.md`。
- **未マージ PR の扱い**: PR #15(Step1 ラン ERROR 1 回目の調査ドキュメント)は main と競合中だが、内容は履歴 014 が包含するため**クローズしてよい**。

## STEP 2 着手前の確認事項(質問リスト Q1〜Q11)— 回答済み・解消

**2026-07-11 にユーザーが全 11 問へ回答(全問推奨案どおり + 補足)し、`docs/SPEC.md`「13. 確定事項」の 18〜28 へ反映済み**(AGENT #027)。質問の全文と推奨案は履歴 `docs/handover/025_step2a_preflight_questions.md` を参照。対応表:

| 質問 | 確定事項 | 内容(要旨) | 使うサブステップ |
|---|---|---|---|
| Q1 | 18 | 前兆 1G 目 = 当せんの次ゲーム(背景移行契機 2/4 と同期) | 2b |
| Q2 | 19 | 通しタイムライン(当せん → 前兆 7〜10G → 連続演出 4G → 成否告知 → AT / 背景移行) | 2b |
| Q3 | 20 | リーチ目も同一フロー(即告知の特例なし) | 2b |
| Q4 | 21 | 偽→本書き換え: G 数引継ぎ / A・B 引継ぎ / 演出中も有効 / 契機 4 は発生させない | 2b |
| Q5 | 22 | 前兆中の新規偽前兆当せんは無視 | 2b |
| Q6 | 23 | 連続演出 4G 中もモード移行抽せん実施(偽のみ。本は停止) | 2b |
| Q7 | 24 | 自背景維持でも 30G カウンタリセット | 2c |
| Q8 | 25 | 優先順位は契機 2/3/4 > 30G。前兆中は 30G 契機停止 | 2c |
| Q9 | 26 | 設計想定値なし → シミュレーション結果を報告しユーザー判断(まずは動作すれば OK)。打ち方 = 通常時 左第一適当押し / AT 中 ナビ遵守 | 2e |
| Q10 | 27 | セットは小役 10G → バトル 8G。リプレイ・レア役も 1G 消化 | 2d |
| Q11 | 28 | 演出はシナリオ方式。毎 G 出力に演出層向け情報を全部含める(2a で設計済み) | 2a・STEP 4 |

## 次の AGENT へのタスク

ユーザーの指示内容を最優先とした上で、次を実施:

1. **実素材が `incoming/` に入稿されていたら STEP 4f(実素材の差し替え + 総合確認)に着手**(`docs/ROADMAP.md` の「STEP 4f」参照): `scripts/import_incoming_assets.py` の対応表を拡張して取り込み、`manifest.json` へ出所登録。差し替えポイントは SE = `SOUND_CUES` / BGM = `STAGE_BGMS` / 予告 = `YOKOKU_VIDEOS` / 連続演出 = `RENZOKU_VIDEOS` / AT・エンディング = `AT_VIDEOS`(いずれも同名ファイル置き換え or 対応表の張り替え)。総合ブラウザ確認 + `docs/STEP4_VERIFICATION.md`(STEP1/3 版の形式)を作成し、STEP 4 完了マークを付ける。
2. 入稿がまだの場合は **STEP 5b(CI ビルド + 配布)に着手**(ROADMAP の STEP 5b 参照): GitHub Actions(`windows-latest`)で Windows exe(NSIS インストーラ等)を自動ビルド。トリガーはタグ push(Release 添付)+ `workflow_dispatch`(検証用)の 2 本立て。Windows 実機確認は VM では不可能なため、ユーザー依頼前提の手順書 `docs/STEP5_VERIFICATION.md`(exe 入手方法・起動手順・チェックリスト = STEP3 版の形式)を作成する。**着手前の事前確認は AGENT #052 が完了済み(経緯 46)**: ワークフロー push 権限あり / main グリーン / CI 実行確認の具体策は経緯 46 参照。
3. 作業終了時に本ファイルを更新し、`docs/handover/053_*.md` に履歴を残す(052 は使用済み)。

STEP 5b の実装 AGENT への注意(AGENT #049 = 5a の実装より):

- **Linux 特有のメディア対策(localhost 配信 + playbin3)は `cfg(target_os = "linux")` の条件コンパイルで Windows ビルドから自動除外される**。5b 側で `lib.rs` の変更は不要。`tauri-plugin-localhost` / `portpicker` も Linux 専用依存(`[target.'cfg(target_os = "linux")'.dependencies]`)にしてある。
- Windows の実機確認チェックリストには **(a) AT 突入後のステージ動画の再生(VM の Linux では黒画面の既知事象)/ (b) BGM・SE の実音再生(VM は音声デバイス無しで未確認)** を必ず含めること(経緯 43 の VM 限定制約の切り分け)。
- CI では `npm ci && npm run build` → `tauri build` の順(`beforeBuildCommand` は tauri.conf.json に設定済み = `npm run build`)。Rust は stable でよい(1.97 で動作確認済み。古い 1.83 は依存の `edition2024` 要求で不可)。
- **`src-tauri/target` は `.gitignore` 済み**。CI キャッシュには `~/.cargo` + `src-tauri/target` を使うとビルド時間を短縮できる。

**Cursor Cloud 環境セットアップ更新 = AGENT #051 が env setup フローで実施済み(経緯 45)**: 下記一式を VM へ導入し、初回 `npm run tauri:build` 済みのキャッシュ(`src-tauri/target` 約 2.0GB + `/usr/local/cargo` 約 223MB)ごと環境イメージへ反映。**更新後の環境で起動するエージェントは再セットアップ不要で、`cargo build --release` は約 23 秒**。万一イメージ反映前の古い環境で起動した場合の手動セットアップ手順は以下(#050 実測):

- Rust toolchain stable(1.97 以上。`rustup update stable && rustup default stable`。約 11 秒)
- apt: `libwebkit2gtk-4.1-dev build-essential curl wget file libxdo-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev`(約 23 秒)
- (実機確認する場合のみ)apt: `pulseaudio pulseaudio-utils` + PulseAudio null sink(`pulseaudio --start` + `pactl load-module module-null-sink`。音声デバイス無しの VM で GStreamer のオーディオパイプラインが失敗しないようにするため)
- 初回 `npm run tauri:build` は約 2 分半(クリーンビルド + バンドル 3 種)。`src-tauri/target`(約 1.9GB)と `$CARGO_HOME`(`/usr/local/cargo`。約 220MB)をイメージへ含めれば 2 回目以降の `cargo build --release` は約 23 秒

STEP 4e の実装で注意した点(4f 以降も維持すること):
- **バトルルートの保持は `App.tsx` の `battleRef`(useRef)**。設定 = バトル 1G 目のレバーオン(`drawLeverDirection` 内)/ 引き直し・破棄 = 1G の締め(`finishGame` 内)/ リセットで破棄。演出用 rng(`hintRng`)を使うため出玉には影響しない。
- **継続確定状態の実装解釈**(率当せんはバトル 1G 目の全停止で判明 → 勝利ルートへ引き直し。2G 目以降の小役継続は引き直さず 8G 目の復活告知で見せる)は DIRECTION_SPEC 2.5 と `direction.ts` ヘッダーに記録。core の乱数消費順序を変えずに Q18 のルート方式を実現するための解釈なので、変える場合は core(`advanceAt` のバトル開始処理)ごと設計し直すこと。
- **カットイン列は `finishGame` で組み立てて FINISH アクションに渡す**(復活告知の差し込みがあるため reducer 内では `cutinsForEvents` を呼ばない)。
- **実機確認は「1G消化(オート)」ボタン(デバッグパネル)を使う**: 1 クリック = 1 ゲーム即時消化。連続の「オート消化」(160ms/G)は速すぎて狙ったフェーズで止められない(computerUse でのバースト操作も不確実)ため、状態を作る目的では使わないこと。役の強制(中段チェリー = V ストック確実 / ハズレ = 抽せんなし)との組み合わせで AT の任意状態を再現できる。バトルの敗北寄りルート(U)は「ハズレでバトル 7/8G まで進めて予告演出行のルート ID を確認 → U なら 8G 目を中段チェリーで消化 = 復活告知」が最短(継続率 66% の AT なら 1 バトルあたり約 34% で U が残る)。
- エンディング 0G 目(突入ゲームの直後)は直前のバトル G8 全画面が次のレバーオンまで上に残る(4d の「カットインは全画面表示の上に重なる」と同じ規約)。エンディングムービーは 1G 目のレバーオンから見える。

STEP 4d の実装で注意した点(4e 以降も維持すること):
- **連続演出の表示解決はレバーオン時**(`renzokuAtLeverOn`)で、「連続演出 1G 目のレバーオン = フェーズ `OMEN` の最終 G 消化済み(`phase.game >= totalGames`)」という判定を使っている。前兆のタイムライン規約(state.phase.game = 消化済み G 数)を変えるとここも崩れるので注意。
- **仮素材の生成は対象指定で**: `python3 scripts/gen_placeholder_assets.py renzoku` のように追加分の対象だけ指定(4c の注意と同じ。4e では `at` などの対象を新設して足すのが素直)。
- **成否告知カットインは連続演出の全画面表示の上に重なる**(全画面表示は次のレバーオンまで残る)。4e のバトル敗北 → AT 終了カットインも同じ重なり方になる想定。
- 実機確認の再現手順: 本前兆 = 「リーチ目(強制)1G → ハズレ(強制)で消化」/ 偽前兆 = 「**強スイカ(強制)1G**(本前兆非当選時 100% 偽前兆 = 確定 3。約 10% で本前兆になるためリセットして引き直す)→ ハズレで消化」/ 書き換え = 偽前兆中に中段チェリー(強制)。連続演出種別(A/B/C)は当せんゲームのフェーズ表示「(演出Xへ)」で確認できる(C は 20% なので出るまで引き直す)。

STEP 4c の実装で注意した点(4e 以降も維持すること):
- **レバーオン時に決まる演出は `LeverDirection`(direction.ts)+ `App.tsx` の `drawLeverDirection` に集約**した。4d・4e でレバーオン契機の演出(AT 中予告など)を足すときもこの枠へ足す(`DirectionLayer` の `lever` prop で描画)。全停止契機の演出は従来どおり `cutinsForEvents`(キュー表示)。
- **演出用の乱数は UI 専用 rng(`hintRng`)を使う**(出玉に影響する `advanceGame` の rng と分離 = DIRECTION_SPEC「6.」)。オート消化とレバーオンの両方で同じ `drawLeverDirection` を通すこと。
- **予告ムービーの追加生成は対象指定で行う**: `python3 scripts/gen_placeholder_assets.py yokoku` のように追加分の対象だけ指定(全生成すると ffmpeg 出力の揺らぎで既存仮素材まで差分が出る)。生成には `pip install pillow` + `sudo apt-get install -y fonts-noto-cjk` が必要(AGENTS.md 参照)。
- **ムービー素材の参照は `import.meta.glob`(`YOKOKU_VIDEOS`)+ 存在検証テスト**の形を踏襲してよい(51 本の個別 import は不要。direction.test.ts の「全キーが揃っている」テストが生成漏れを検知する)。
- 前兆シナリオ予告の表示タイミングは「レバーオン時に次ゲーム分(`phase.game + 1`)を解決」(経緯 38 の実装解釈)。ここを変えると小役示唆予告との競合判定(同一 G)も見直しになるので注意。
- 実機確認のコツ: デバッグパネルの「シナリオ:」行(前兆中)と「予告演出:」行で、シナリオと実際の表示の一致をゲーム単位で照合できる。本前兆は「中段チェリー(強制)」1G → 「ハズレ(強制)」で消化、が最短(3e のノウハウどおり)。

STEP 4b の実装で注意した点(4d 以降も維持すること):
- **`drawOmenScenario` は `scheduleOmen` の乱数列の一部**(前兆 G 数 → 連続演出 → シナリオの順)。ここに抽せんを足す/減らすと `flow.test.ts` のシード・`simulate.test.ts` の固定値・`state.test.ts` の `scenarioDraws` 供給数が崩れる(意図した変更なら取り直す)。
- **小役示唆・AT 予告・バトルルート・復活告知は `advanceGame` の乱数列を汚さない独立関数**。UI から呼ぶときは別の rng(例: `createRng(seed)` を UI 専用に 1 本)を使うこと。出玉に影響する乱数列と演出の乱数列を分離しておくのが狙い。
- 振分けテーブルは全て分母 100 の整数で、合計 = 100 を `scenario.test.ts` が検証している。数値調整の指示が来たらテーブルと DIRECTION_SPEC「3.」をセットで更新する(構造は変えない)。

解決済み(2026-07-13 ユーザー回答。AGENT #043 で反映済み):
- 質問リスト Q12〜Q20 は全問回答済み → SPEC 確定事項 34 + DIRECTION_SPEC 更新。予告は小役示唆系 / 前兆系の 2 系統(固有 1〜3 + 共通 1・2 = 小役示唆)。バトルは約 20 ルート + 勝利/敗北/復活/継続率別振分け。

STEP 3e の実装で注意した点(STEP 4 以降も維持すること):
- **デバッグ系 UI は「デバッグ(開発用)」の折りたたみ内に置く**(`App.tsx` の `<details className="debug">`)。STEP 4 で演出デバッグ機能を足す場合もこの中へ。通常プレイ視点(筐体 + メーター + 停止ボタン + 遊技ボタン列)には開発用要素を出さない。
- 通しフローのブラウザ確認ノウハウ(強制役の使い分け・Space キーの注意)は経緯 33 参照。手順書 `docs/STEP3_VERIFICATION.md` のチェックリストが回帰確認にそのまま使える。

STEP 3d の実装で注意した点(3e 以降も維持すること):
- **SE を鳴らすときは必ず `playCue(SoundCueId)`(`src/ui/sound.ts`)を使う**。SE ファイル(`assets` の `SE`)を UI から直接参照しない(BGM/SE の後日差し替えを対応表 1 箇所で完結させるためのユーザー要件)。新しい音が必要になったらキュー ID を追加して対応表に登録する。
- **演出の中身(文言・スタイル・時間・SE)は `direction.ts` に集約**し、`DirectionLayer` は宣言的データの描画だけに保つ。STEP 4 のシナリオテーブル(確定 28)は direction.ts の対応表を置き換える形で導入する(ファイルヘッダーの差し替え方針参照)。
- **カットインは全停止(1G の締め)後に表示される**(reducer の FINISH で `cutinFrame` 更新 → DirectionLayer がキュー表示)。表示タイミングの規約(最終リールのスベリ完了後)は 3b・3c と同じ。
- **オート消化(160ms/G)はカットイン表示より速い**ため、キュー上限 4 で古い待機分を間引く(`MAX_CUTIN_QUEUE`)。3e でデモをするときは、演出を見せたい区間は手動消化(またはオート停止)が確実。
- computerUse の画面録画はコマ落ちにより早回しに見えることがある(実測はカットイン 2000〜2400ms 表示で正常。疑わしいときは DOM 監視(`.cutin-title` の遷移計測)で確認した実績がある = 履歴 038 参照)。
- エンディングの常時バナー(`ending-banner`)は上部配置。エンディング専用演出・素材は STEP 4。

STEP 3c の実装で注意した点(3d 以降も維持すること):
- **メーターの更新は 2 タイミングに固定**: レバーオン(`LEVER` アクション → `meterOnLever`)と全停止の 1G 締め(`FINISH` アクション → `meterOnFinish`)。オート消化も同じ経路を通る。演出(3d)で払出表示のタイミングを変えたい場合もこの 2 関数の呼び出し位置で調整する(全停止 = 最終リールのスベリ完了後 = 3b の `finishTimerRef` 以後、の規約は維持)。
- **AT 獲得枚数の加算判定は「ゲーム開始時点のフェーズ」**(`advanceGame` に渡す前の state)。`AT_START` のゲーム(連続演出最終 G)は加算されない。ENDING も加算対象。
- ナビ数字の表示条件は `naviShown`(回転中 + `isNaviActive` + ベル当選)で、リールごとの消灯は `spinUi.cycle.stopped` を参照。ナビの見た目(`navi-digit`)は仮デザインで STEP 4 で差し替え可。

STEP 3a・3b の実装で注意した点(3d 以降も維持すること):
- **対話式停止の仮押し順**(`provisionalPushOrder` = 押した順 + 未停止リールの左→右昇順)は 3a で確定した規約。変更すると `gameCycle.test.ts` の取りこぼし固定値が変わり得る(意図した変更ならテストを取り直す)。
- **「描画に使う時刻」と「押下位置の計算に使う時刻」は同一にする**(3b の規約)。回転描画は `continuousPosition`(小数)、押下位置はその floor(= `spinningPosition` と同値)で、同じ `performance.now()` 基準を使う(`reelAnimation.ts` ヘッダー参照)。ズレると見た目と違うコマで止まり目押しが破綻する。floor(連続位置) = `spinningPosition` の一致は `reelAnimation.test.ts` で固定済み。
- **全リール停止後は最終リールのスベリ完了(≤ 150ms)を待ってから 1G を締める**(`App.tsx` の `finishTimerRef`)。払出表示・演出(3c・3d)もこのタイミング以後に出すこと。リセット時はタイマー破棄済み。
- 目押しの実機検証を再現したい場合: 一時ドライバ(App と同じ window keydown 経路で Space / Z・X・C を「(狙いコマ − 手前コマ数 − 開始位置) × 37.5ms + コマ中央 0.5 コマ分」のタイミングで発火する)を `src/devMezuoshi.ts` として作った実績がある(検証後削除済み。作り直す場合は履歴 036 とこの説明を参照)。コマ帯の img alt 5 個から現在の停止位置を逆引きできる。
- `App.tsx` の肥大化(現状約 700 行)が気になる場合、ROADMAP 全体設計のとおり `src/ui/` へのコンポーネント分割を行ってよい(分割単位は実装 AGENT が決めて記録)。

STEP 2 の実装で注意した点(STEP 3 以降も維持すること):
- `playGame` の乱数消費順序(役 1 → 押下位置 3 → `advanceGame` 内部)を変えると `flow.test.ts` のシード(現在 30。4b で取り直し)・`simulate.test.ts` の固定値が崩れる。意図した変更ならシード・固定値を取り直してテストを更新する。
- シミュレーションの実行は `npm run sim -- <ゲーム数> <シード>`(100 万 G 約 13 秒)。レポート更新時は 100 万 G 以上で計測すること。
- エンディングの UI 表示(STEP 4): `EndingPhase.after` で「上位 AT 突入前のエンディング」と「完走エンディング(AT 終了)」を描き分けられる。`ENDING_GAMES` = 10 は STEP 4 の演出尺確定で変更可(定数 1 箇所)。2f では暫定で直前 AT 階層のバトルステージを継続表示している(`App.tsx` の `stageForState`)。
- UI のステージ・BGM 切替は `stageForState(state)` に集約済み(STEP 3 でリール演出を足すときもこの関数を通すこと)。
- タイミング目押しの停止ボタン化(STEP 3)では、現行の目押しモードセレクト(`pickPushes` / `aimedPush`)を「押下瞬間のコマ取得」へ置き換える。`resolveSpin` のインターフェース(押下位置 3 つ + 押し順)は変更不要。

## 注意事項

- ブランチは `cursor/<説明>-<suffix>` 形式で作成し、**PR は必ず base を `main` にして作成すること**(suffix は環境から指示される)。
- **PR は 1 本ずつマージすること**(本ファイルが競合しやすい)。競合したら main をブランチへマージして解消する。
- **同じタスクを複数のエージェントへ並行依頼しないこと**(PR #13 / #14 のコンフリクトの原因。同一ファイルを別内容で変更した PR が複数できてしまう)。
- `AGENTS.md` のルール(テスト実行、不要ファイル削除禁止、PR 用の簡潔なまとめ)を守ること。
- **確率・配当・配列などの数値は SPEC.md(Excel + 確定事項)と `src/core/` の実装を必ず一致させる**。抽せんテーブルは分母付き整数(10000 / 100 / 1000)で持ち、行合計 = 分母をテストで検証している。
- **背景移行の 30G は `BACKGROUND_ELAPSED_GAMES`(`src/core/background.ts`)で定数化済み**。値の変更指示が来たら定数と SPEC・テストをセットで更新する。
- 実在機種の素材・画像の流用は禁止(リール配列シートの図柄画像は参考イメージ)。
- ブラウザ API(音声・動画再生・永続化)は `src/platform/` のラッパー経由で使うこと(exe 対応のため)。
- 素材追加時は `docs/ASSET_GUIDELINES.md` に従い `manifest.json` に出所を登録すること。素材変換は `scripts/import_incoming_assets.py`(入稿素材)/ `scripts/gen_placeholder_assets.py`(仮素材)を使い、再生成可能な状態を保つこと。
