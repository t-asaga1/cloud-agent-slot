# AGENT #082: 弁慶背景 固有予告 1 の静止画 4 枚を生成(原本を incoming/ へ確保。組込みはユーザー確認待ち)

- 作成日: 2026-07-17
- ユーザー指示: 「静の背景固有予告の処理は上手く行っていた。同じ様に進める。次は弁慶の
  背景固有予告の 1。1 枚目がチェストアップぐらいの弁慶で背景は弁慶背景の滝、滝の前に
  立っている。2 枚目は弱は左を向いて、強はバストアップで右を向く。3 枚目は背景の森を映す。
  実機ではそこで小役示唆を実施」
- アプリのコード変更なし。生成原本の `incoming/yokoku/` コミット + ドキュメント更新のみ。

## 1. 生成した 4 枚(弁慶背景予告 1)

いずれも内蔵 GenerateImage(FAL 課金なし)・1536x1024(3:2。組込み時に中央 16:9 クロップ):

| # | incoming のファイル | 用途 | 内容 |
|---|---|---|---|
| 1 | `弁慶_予告1_1_共通_正面.png` | 弱・強共通 1 枚目(レバーオン) | 比叡山の滝の前に弁慶が立つ。正面向き・チェストアップ |
| 2 | `弁慶_予告1_2_弱_左向き.png` | 弱 2 枚目(第 1 停止) | 同構図のまま弁慶が左(画面左)を向く |
| 3 | `弁慶_予告1_2_強_バストアップ右向き.png` | 強 2 枚目(第 1 停止) | バストアップへズームし右(画面右)を向き鋭い眼光 |
| 4 | `弁慶_予告1_3_共通_森.png` | 弱・強共通 3 枚目(第 3 停止 + 小役図柄) | 比叡山の森。人物なし・中央に霧の抜け(図柄合成スペース) |

## 2. 生成手順(静版 = 12.3 のコツをそのまま適用)

1. 参照画像: 設定資料ペア(`incoming/reference/設定資料/弁慶_顔.png` / `弁慶_全身.png`)+
   背景参考(`incoming/reference/背景/弁慶背景/比叡山滝.png`)→ 1 枚目を生成。
2. 2 枚目弱/強は **1 枚目の生成画像を参照に連鎖**(「参照と同一、変更点は向き(+ 強はズーム)のみ」)。
3. 3 枚目(森)は 1 枚目 + `比叡山滝.png` を参照し「同ロケーションの森・人物なし・
   中央を空けて図柄を後から合成できる構図」を明記。
4. 4 枚とも顔・衣装(白頭巾・金の額当て・緑の大数珠・紫の袖なし羽織・藍の着物)・
   滝背景・ライティングの一貫性を確認済み。

## 3. 再発防止の実施(経緯 75 の教訓)

- AGENT #080 ではアーティファクトのみでラン終了後に現物が消え、#081 で再生成が必要になった
  (ピクセル不一致)。今回から**生成ランのうちに原本を `incoming/yokoku/` へコミット**する運用に変更。
- 採用時はこの原本から WebP 化して組込むため再生成不要。組込み後は原本を削除する(Git 履歴には残る)。

## 4. テスト・確認

- コード変更なしの現状確認: テスト 406 パス・lint グリーン。

## 5. 次の AGENT へ

1. **ユーザーの画像確認の結果を待つ**。
   - OK なら: `incoming/yokoku/` の原本 4 枚を中央 16:9 クロップ + 1280x720 WebP 化して
     `src/assets/images/yokoku/yokoku_benkei_koyu1_still1/_still2_weak/_still2_strong/_still3.webp`
     へ入稿 → `manifest.json` 登録 → `KOYAKU_HINT_STILLS`(direction.ts)へ
     KOYU_1 × BENKEI の 1 行追加 → direction.test.ts の YOKOKU_IMAGES 存在検証へキー追加
     → `incoming/yokoku/` の原本を削除(静版 = 経緯 75 / YOKOKU_PRODUCTION_PLAN 12.4 と同一手順)。
   - 修正指示なら: 該当枚のみ再生成して原本を差し替え(1 枚目を直す場合は 2 枚目弱/強も
     連鎖参照のため作り直しが必要)。
2. 残りの予告(義経・夕方の固有 1、各背景の固有 2・3 / 共通 1・2 など)も同フローで
   1 予告ずつ進める(生成のコツ = YOKOKU_PRODUCTION_PLAN 12.3 / 組込み手順 = 12.4)。
