# 引継ぎ資料(最新)

- 作成者: AGENT #009(Excel 本仕様書の取り込み + 全シートレビュー + 質問リスト作成)
- 作成日: 2026-07-09
- 履歴コピー: `docs/handover/009_excel_spec_intake_and_review.md`

## プロジェクト概要

パチスロアプリケーション「**義経物語**」を開発するプロジェクト。
**1 AGENT につきユーザーとのやり取りは 1 回のみ**。作業終了時に必ずこのファイルを更新し、`docs/handover/` に履歴コピーを残すこと(詳細ルールは `AGENTS.md` 参照)。

## 最重要: 仕様の正はユーザー入稿 Excel

- **`docs/specs/義経物語_仕様_260709.xlsx` が本スロットの正式仕様(マスター)**。ユーザー指示により、以前の簡易仕様(叩き台)と食い違う部分は全て Excel が正。
- 内容は `docs/SPEC.md` に全シート転記済み(確率・モード移行・背景移行・前兆・AT・上位 AT の全数値テーブル、リール配列、停止挙動)。
- xlsx は openpyxl(`pip3 install --user openpyxl`)で読める。シート内埋め込み画像は `unzip` で `xl/media/*.png` を展開して確認できる(背景イメージ 14 枚 + リール図柄参考画像 1 枚)。
- 注意: リール配列シートの図柄画像は**既存機種の参考画像**(そのまま流用禁止)。図柄はテキストの配列表(SPEC.md 転記済み)が正。

## これまでの経緯

1. AGENT #001〜#005: 開発計画策定(TS + React + Vite + Vitest)、Web + exe(Tauri)両対応方針、素材入稿フロー(`incoming/`)、Phase 1 コアロジック(rng/lottery/payout)、main への統合。
2. AGENT #006: Excel 仕様書の入稿方法を整備(PR #9)。
3. AGENT #007: 筐体画像取り込み(`src/assets/layout.ts`)、仮素材 40 件生成、Phase 2 リール制御(`src/core/reel.ts` + 網羅テスト)実装。
4. AGENT #008: PR #7 / #8 の HANDOVER 競合解消。※PR #7 と #8 は同タスクの並行 PR で、**どちらか一方のみマージ・他方クローズ**の注意があった(現 main には #8 系がマージ済み)。
5. **AGENT #009(今回)**: ユーザーが `incoming/義経物語_仕様_260709.xlsx`(全 16 シート)を入稿。全シートを読み取り・検算し、以下を実施:
   - xlsx を恒久置き場 `docs/specs/` へ移動(仕様マスターとして保持)。
   - `docs/SPEC.md` を Excel 準拠に全面書き換え(全数値テーブル転記 + 配列分析 + 旧叩き台との差分表 + 未確定事項 17 件)。
   - 検算で見つけた仕様の不整合・質問 17 件をユーザーへ送付(`docs/SPEC.md` の「13. 未確定事項」参照)。主な指摘: AT 終了後モード振分け合計 0.9999 / 本前兆時の背景移行テーブル行合計 1.25 / 偽前兆突入率の記述矛盾(1/10 vs 1/5)/ 左リールリプレイ間隔 6 コマ問題 / 通常時の押し順ベル変則押し。

## 現在の状態

- **`src/core/` は旧叩き台のままで Excel 未整合**(テスト 42 件は旧数値でグリーン)。`docs/SPEC.md` の「11. 差分」「12. 実装状態」に修正が必要な点を整理済み。
- 演出リスト(各背景の予告・連続演出)は Excel 上ほぼ「仮」。確定版の入稿待ち。
- 背景・キャラのイメージ画像(KlingAI 生成静止画)が Excel に埋め込まれている。動画素材・図柄素材は未入稿。

## 次の AGENT へのタスク

ユーザーの指示内容を最優先とした上で、次を実施:

1. **ユーザーからの質問回答(SPEC.md「13. 未確定事項」)を確認し、SPEC.md に反映**する。回答が一部でも、確定した部分から実装を進めてよい。
2. **`src/core/` を Excel 仕様へ整合させる**(回答がなくても着手可能な範囲が多い):
   - `roles.ts`: 役を Excel 準拠に変更(リプレイ / 押し順ベル / 角チェリー / 中段チェリー / 弱スイカ / 強スイカ / チャンス目 / リーチ目。BIG/REG 廃止)。
   - `lottery.ts`: 単一テーブル(65536 分母、SPEC.md「2.」の当選個数)へ変更。
   - `payout.ts`: 払出変更(ベル 1/13、スイカ 3、チェリー 2、チャンス目 3、リーチ目 3)。押し順ベルは押し順で払出が変わる点に注意。
   - `reel.ts`: 配列を Excel 準拠へ差し替え + 停止挙動(SPEC.md「3.」)の実装。**有効ライン(横 3+斜め 2)前提の書き直しになるため網羅テストも再設計が必要**。
3. **Phase 3(遊技状態管理)**: モード移行・背景移行・前兆・AT・上位 AT の抽せんを SPEC.md のテーブルどおりデータ駆動で実装しテストする(行合計=1 の静的検証テストを入れると Excel 側の誤植も検出できる)。
4. 作業終了時に本ファイルを更新し、`docs/handover/010_*.md` に履歴を残す。

## 注意事項

- ブランチは `cursor/<説明>-<suffix>` 形式で作成し、**PR は必ず base を `main` にして作成すること**(suffix は環境から指示される)。
- **PR は 1 本ずつマージすること**(本ファイルが競合しやすい)。競合したら main をブランチへマージして解消する。
- `AGENTS.md` のルール(テスト実行、不要ファイル削除禁止、PR 用の簡潔なまとめ)を守ること。
- **確率・配当・配列などの数値は Excel(=`docs/SPEC.md`)と `src/core/` の実装を必ず一致させる**。現在は過渡期で未整合(SPEC.md に明記済み)。実装を更新したら SPEC.md「12.」の記述も更新すること。
- 実在機種の素材・画像の流用は禁止(リール配列シートの図柄画像は参考イメージ)。
- ブラウザ API(音声・動画再生・永続化)は `src/platform/` のラッパー経由で使うこと(exe 対応のため)。
- 素材追加時は `docs/ASSET_GUIDELINES.md` に従い `manifest.json` に出所を登録すること。
