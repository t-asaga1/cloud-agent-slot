# 引継ぎ資料(最新)

- 作成者: AGENT #010(仕様質問へのユーザー回答反映 + `src/core/` の Excel 仕様整合 + Phase 3 抽せんテーブル実装)
- 作成日: 2026-07-10
- 履歴コピー: `docs/handover/010_spec_answers_and_core_alignment.md`

## プロジェクト概要

パチスロアプリケーション「**義経物語**」を開発するプロジェクト。
**1 AGENT につきユーザーとのやり取りは 1 回のみ**。作業終了時に必ずこのファイルを更新し、`docs/handover/` に履歴コピーを残すこと(詳細ルールは `AGENTS.md` 参照)。

## 最重要: 仕様の正

- **`docs/specs/義経物語_仕様_260709.xlsx` が正式仕様(マスター)**。
- ただし **2026-07-10 のユーザー回答 13 件(`docs/SPEC.md`「13. 確定事項」)は Excel より優先**(Excel 誤植の訂正を含む)。主な訂正:
  - AT 終了後モード振分けの残り 0.0001 は地獄へ加算(地獄 0.3156)。
  - 本前兆移行時の背景テーブルの静・弁慶行は自背景 0.25(Excel の 0.5 は誤植)。
  - 偽前兆の突入条件はモード移行シートが正(レア役 1/10。「天国アップ時の 1/5」は不採用)。
- 有効ラインは横 3 + 斜め 2 の 5 ライン。タイミング目押しあり。チェリー・スイカは取りこぼし許容。設定差なし。

## これまでの経緯

1. AGENT #001〜#005: 開発計画策定(TS + React + Vite + Vitest)、Web + exe(Tauri)両対応方針、素材入稿フロー(`incoming/`)、Phase 1 コアロジック、main への統合。
2. AGENT #006: Excel 仕様書の入稿方法を整備(PR #9)。
3. AGENT #007〜#008: 筐体画像取り込み、仮素材 40 件生成、Phase 2 リール制御(旧叩き台)、PR 競合解消。
4. AGENT #009: ユーザー入稿の Excel 本仕様書(全 16 シート)を読み取り・検算し `docs/SPEC.md` へ全転記。不整合・質問 17 件をユーザーへ送付(PR #10 ブランチ)。
5. **AGENT #010(今回)**: ユーザーから質問への回答 13 件を受領し、以下を実施(PR #10 ブランチの続きとして作業):
   - `docs/SPEC.md` に回答を反映(「13. 確定事項」に整理、残る未確定は「14. 未確定事項」の 4 件)。
   - **`src/core/` を Excel 仕様へ整合**:
     - `roles.ts`: 役 8 種 + ハズレ(リプレイ / 押し順ベル / 角・中段チェリー / 弱・強スイカ / チャンス目 / リーチ目)。設定(1〜6)概念を廃止。
     - `lottery.ts`: 単一テーブル(65536 分母)。
     - `payout.ts`: 押し順ベル 13/1 枚、チェリー 2、スイカ・チャンス目・リーチ目 3。通常時 −1.8 枚/G・AT 中 +6.4 枚/G の検算テスト付き。
   - **Phase 3 の抽せんテーブルをデータ駆動で新規実装**(全て単体テスト付き。行合計 = 分母の静的検証で Excel 誤植を検出可能):
     - `mode.ts`: モード 4 種・モード移行・モード初期設定・偽前兆突入抽せん。
     - `background.ts`: 背景 5 種・背景初期設定・背景移行(モード × 契機 4 種)。
     - `omen.ts`: 前兆ゲーム数(7〜10G)・連続演出振分け(A/B/C、4G 固定)。
     - `at.ts`: 継続率振分け・V ストック / バトル継続抽せん・上位 AT 93% 等の定数。
   - `reel.ts`: 役名のみ新仕様へ暫定適合(配列・ラインは旧叩き台のまま。下記参照)。
   - `incoming/README.md` に容量制限と拡張手段(GitHub 100 MiB 制限・LFS・Releases)を追記。
   - テスト 80 件全パス、lint / build 成功。

## 現在の状態

- Phase 1〜2 完了 + **Phase 3 の抽せんテーブル層が完了**(状態遷移の通しフロー = ステートマシンは未実装)。
- **`reel.ts` は暫定**: 旧叩き台の配列(白7 あり)+ 中段 1 ラインのまま、役名だけ新仕様に適合(弱・強スイカは同一停止形、リーチ目は赤7 中段揃いで暫定表現、チャンス目はハズレと同じ制御)。
- 演出リスト(各背景の予告・連続演出)は「仮」のまま。背景動画・図柄素材は未入稿(ユーザーがこの後アップ予定と回答)。素材が大きい場合の運用は `incoming/README.md` の「容量の目安と拡張手段」参照。

## 次の AGENT へのタスク

ユーザーの指示内容を最優先とした上で、次を実施:

1. **`incoming/` に素材(背景動画等)が入稿されていたら最優先で取り込む**(`docs/ASSET_GUIDELINES.md`)。大きい動画は WebM(VP9)720p ループへ再圧縮して `src/assets/` へ。背景 9 種(SPEC「5.」)に合わせて仮素材の構成変更も必要。
2. **`reel.ts` の本対応**: Excel 配列(SPEC「3.」)への差し替え + 有効ライン横 3 + 斜め 2 + 確定した停止挙動(リプレイ 5 ライン 100% 引き込み、押し順ベルの上段/斜め停止、DDT 黒バー制御、リーチ目 7 揃い)。網羅テストの再設計込みで大きめの作業。
3. **Phase 3 の通しフロー実装**: `mode.ts` / `background.ts` / `omen.ts` / `at.ts` の部品を組み合わせた 1 ゲーム消化のステートマシン(`state.ts`)。確定事項(SPEC「13.」)の 9〜12 (前兆中の挙動、偽前兆→本前兆書き換え、AT セット管理・10 連・上位 AT・エンディング)を実装しテストする。
4. 残る未確定事項(SPEC「14.」: リーチ目の目押し失敗時の扱い / 背景移行の優先順位と 30G カウンタ / 純増想定の確認)をユーザーへ確認。
5. 作業終了時に本ファイルを更新し、`docs/handover/011_*.md` に履歴を残す。

## 注意事項

- ブランチは `cursor/<説明>-<suffix>` 形式で作成し、**PR は必ず base を `main` にして作成すること**(suffix は環境から指示される)。
- **PR は 1 本ずつマージすること**(本ファイルが競合しやすい)。競合したら main をブランチへマージして解消する。
- 今回の作業は **PR #10(`cursor/yoshitsune-excel-spec-review-6852`)の内容を含むブランチ**で行った。PR #10 とどちらか一方をマージし、もう一方はクローズすること(本 PR は #10 の上位互換)。
- `AGENTS.md` のルール(テスト実行、不要ファイル削除禁止、PR 用の簡潔なまとめ)を守ること。
- **確率・配当・配列などの数値は SPEC.md(Excel + 確定事項)と `src/core/` の実装を必ず一致させる**。抽せんテーブルは分母付き整数(10000 / 100 / 1000)で持ち、行合計 = 分母をテストで検証している。
- 実在機種の素材・画像の流用は禁止(リール配列シートの図柄画像は参考イメージ)。
- ブラウザ API(音声・動画再生・永続化)は `src/platform/` のラッパー経由で使うこと(exe 対応のため)。
- 素材追加時は `docs/ASSET_GUIDELINES.md` に従い `manifest.json` に出所を登録すること。
